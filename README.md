# 全市场基金雷达前端

基金雷达的 Vue 前端，用于查询基金资料、查看历史走势、管理个人关注和查看预测状态。前端只调用 Java 核心服务的 `/api/v1/*`，不直接访问 FastAPI AI 服务，也不保存外部数据源或支付凭证。

## 项目现状

以下数据来自 [2026-09-09 完成情况检查](docs_zhx/reviews/project-status-2026-09-09.md)，统计截至当天北京时间 15:22；这是有日期的检查快照，不是实时计数。

- 基金市场已登记 43 只启用基金，已有真实历史净值、基金档案、经理、份额规模和分红数据；尚未证明覆盖全市场。同步范围从数据库中 `ACTIVE + TUSHARE_PRO_FUND` 的基金份额动态读取，不固定为 43 只，也不由个人关注列表决定。
- 已提供注册、登录、角色权限、按登录用户隔离的个人关注、关注积分、个人信息和管理员用户管理。
- 数据同步中心提供净值增量、完整资料、免费数据补齐、股票型特征快照四类任务。定时计划存在，但检查时未发现本机 Celery Beat/Worker 进程，不能据此宣称自动同步正在运行。
- 我的关注详情已有 20 交易日预测卡。单周期研究和页面工程阶段已完成；真实模型尚未发布，当前实测状态为 `MODEL_NOT_RELEASED`，概率为空。5/10/40 交易日扩展、综合输入和自动预测提醒尚未完成。
- 新闻与公告采集暂停。`/portfolio` 只读展示本人已确认的持仓快照，检查时快照表为空；缺失的日期、份额或成本保持未知，不提供支付宝登录或自动交易。

## 文档从哪里看

- 想知道项目做到了哪一步：先读 [完成情况检查](docs_zhx/reviews/project-status-2026-09-09.md)。
- 想继续开发：读 [总进度看板](docs_zhx/implementation/fund-radar.md)的“当前状态”，再进入对应专项手册。
- 想理解目标与分工：读 [需求说明](docs_zhx/requirements/fund-radar.md)和 [总体设计](docs_zhx/design/fund-radar.md)。目标不等于已实现功能，历史章节中的样本数和测试结果仅代表当时。

## 本地启动

1. 本项目已配置本地 `.env`；留空时由 Vite 将 `/api` 代理到 `http://localhost:8080`。部署环境可显式设置 `VITE_API_BASE_URL`。
2. 执行 `npm install`。
3. 执行 `npm run dev`，浏览器打开 Vite 输出的地址。

## 质量检查

```powershell
npm run lint
npm run type-check
npm run build
```
