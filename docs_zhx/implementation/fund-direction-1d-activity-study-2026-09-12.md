# 1日浅层树成交活跃度固定对照方案（2026-09-12）

用户要求先提交推送，再继续下一步训练。基金自身1日幅度成果已推送Python `70e5970`、Vue文档 `8a10102`，Java `70c0816`无新改动。本方案写入早于本轮成交额请求和真实基金拟合；结果另记主实施文档第16.11节。新阶段暂留本地。

## 1. 唯一候选

以原TREE11为主要对照，只增加沪深300的成交活跃度，候选TREE_ACTIVITY12。新增`hs300_amount_relative_previous_20d` = T日成交额 / 前20个交易日成交额均值 − 1；分母不含T，固定使用21个连续交易日，不平滑、不取对数、不截尾、不调整窗口。数值0.2表示比前20日均值高20%。使用沪深300成交额作为共同市场输入，不声称代表全市场成交额或基金真实交易量。

原25只、5704道2024考题、四季度/FINAL的504日FIT成员、原11项特征、家族/日期权重、原11维Scaler和严格大于0.5阈值不变。新增一维Scaler只用相同FIT和原权重拟合；不同时加入近期权重、基金自身1日幅度或新类型。

树参数保持深度2、4叶、100轮、学习率0.05、每叶最少100样本、L2=10、63分箱、seed=0，关闭随机验证和早停。原11项树与线性复用完成结果，不重新拟合。只允许本候选5次主拟合及5次唯一复现，不按成绩追加特征、深度或阈值。

## 2. 先核验再补数据

只读检查未发现本地同时具有index_code与amount列的持久化表，也未找到已有成交活跃度数据包；现有指数价格研究只保留收盘。当前TUSHARE_PRO_FUND启用，已有index_basic/index_daily授权、每分钟200次限制及365日留存；000300.SH已核验为沪深300，不新增购买、权限或来源。

仅通过既有客户端`list_index_activity`补000300.SH的2021—2024历史，按年度最多4次index_daily请求。先请求2021年并核验身份、日期、成交额与旧收盘一致，再继续另外3年。每次先写独占预约，失败/空数据不重试或换指数，未完成预约不冒充成功；按来源限速，拒绝截断或重复日期，完整年度响应立即保存，重入不重复取数。异常仅保留类型与权限失败标记，不保存密钥或完整供应商错误。

[Tushare指数日线说明](https://tushare.pro/document/1?doc_id=95)将amount定义为千元；本特征为同比例相除，仍保留原单位元数据。[官方接口更新说明](https://tushare.pro/document/1?doc_id=108)给出指数日线通常15—17点更新，本轮仍保守假设T日18:00可用，不将今日下载证明为历史当时已收到。

四年应覆盖既有日历969个交易日；每条成交额必须有限且正数、指数身份/日期唯一，每条返回收盘必须与旧冻结HS300收盘逐值相等。任何不满足均说明并停止，不覆盖旧收盘、不删基金或考题。只保存独立本地研究数据，不写业务数据库。新数据保留实际下载起点，整个新研究的到期不晚于所有旧来源到期；复制不得重置旧留存。

## 3. 冻结、恢复和验收

冻结方案、数据审计、授权/身份、4次请求预约与响应、旧收盘、新特征输入、原/扩展FIT hash、权重hash、源码及日历。特征只使用到T为止的成交额，不读取U日；须早于旧FIT截止/旧考题U日08:00假设截止。原训练标签成熟和全部旧时点检查保留，2025/2026不参与本轮训练或考题。

新12维树保存为不可执行数值JSON，检验全部FIT及对应考题与原估计器分数最大差≤1e-12；主训练及唯一复现的完整模型和5704条分数须严格相等。独立归档源码实际运行verify，不改旧源码或弱化冻结指纹；输入、模型或预算不完整即拒绝。

主要指标是相同家族/目标日权重的准确率差，按同目标日所有基金联合5日块重采样2000次，seed=20260912。完整报告原始正确数、均衡准确率、上涨/非上涨召回、所有季度/基金、原线性/七特征分组与四种简单基线。2021—2024已经多次用于开发，不能作为独立测试。没有改善则如实结束这一套配方，不追加搜索。

完成已有156项相关测试及新增成交额/时间/恢复测试、Ruff/格式、归档verify及旧研究/完整本人预测保护核对。保留43只本人关注范围、29份9月14日真实预测、FIXED/WEEKLY、周更与20日成果；未到期继续待核对。不改Java/Vue业务、数据库结构或后台任务，不自动注册或正式发布模型，不交易、生产部署或通知。方案和计划不冒充已完成训练。
