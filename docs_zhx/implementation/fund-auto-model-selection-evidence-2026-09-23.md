# 自动选用模型：本轮实施证据与契约映射

## 开工核对

2026-09-23北京时间，三仓库初始工作区均干净。Vue HEAD 88b7da8；Java f1271d1；Python 19331b2。递归排除依赖和构建产物后未发现仓库AGENTS.md，采用当前会话全局基线和Java/Python/Vue工程、系统设计治理技能。

常用端口：8080为IDEA调试PID49032（09:46启动，健康UP）；8000为PyCharm调试PID6348（09:43启动，模型接口200）；5173为Vite PID11144（09:49启动）。服务没有构建SHA接口，源文件/HEAD不能证明运行字节码。本轮未结束这些进程，S6另行验证加载身份。

Python本机fund_ai迁移20260922_26，研究4条、多周期原文373条、到期答案0条。只读审计原始证据：Python `.local-runs/auto-baseline.json`（忽略文件，禁止作为部署包）。routes整对象比较包含时间序列化差异，不据此判定版本不一致。

## S0-02 现有链与缺口

- 唯一调度：Java MultiPredictionPipeline.tick，每30分钟维护、分页所有账号关注基金去重、生成、本人引用、AdviceScheduler；后者已无独立定时注解。
- Python create_research只由人工POST触发；recover_research只能恢复，不能建立每周新任务。run_research逐周期compare_and_activate会提前发布；旧平分规则可因训练更新日期替换。需改为只保存评价与候选，再统一完整组合决策。
- 已有预测任务一次freeze_routes，但没有releaseId；保存period_key冲突复用原文，不能靠新taskId同日覆盖。建议predictionSnapshot/modelRefs保存实际输入，可追加发布引用字段，旧原文不回写。
- 真实回放已复用Python实际包推理及Java StrategyReplayEngine/DecisionPolicyV2；目前人工按单基金日期运行。后台必须冻结全范围分片、共同日期和失败分母，不能只取一只基金或方向分数代替扣费比较。
- UI研发组件直接位于SyncCenterPage；普通结果有周期/理由/失败。效果缺少真实预测聚合和按已发出建议的版本序列账本。

## S0-03 冻结的实现协议

Python `app/data/auto_model_policy_v1.json`为自动流程权威配置，包含训练/验证/选优窗口、日程、预算、候选和采用规则及回放费用；周期/策略特征仍引用既有prediction_policy_v1，合并hash进入周期身份。

Java现有30分钟入口发送所有有效关注基金集合，Python快速建立幂等周期并由共享单研究worker推进。每50只固定分片，失败保留原分母。训练结果仅形成候选，历史回放输入在Python冻结，Java同一个账本执行器负责比较；不新增独立定时器。成熟水位变化触发独立真实评价，未到期与失败分列。

完整发布在Python事务内锁定涉及路由并CAS，批次冻结发布身份。采用后先等待实际调用，Java保存预测/建议回读回执后确认使用；同日期已有预测须等合法新期次。回退记录实际模型，保留原请求身份与旧发布档案。

接口边界：`/internal/v1/multi-predictions/auto/*`仍要求服务Token且拒绝Origin；Java管理员技术详情要求RESEARCH_RUN_ADMIN，业务摘要由Java查本人关注后限定公共基金列表；个人报告与版本序列账本只在Java读取，不向Python传用户身份/个人仓位。

验收证据逐项登记进度表；真实窗口、自然日程、五日/二十日/半年到期独立等待。临时服务/隔离schema/受控夹具与常用真实链分开，不借用旧验收数。

## 本轮实现与验收结果（2026-09-23）

### 已落地的链路

- Python新增自动周期、不可变输入/事件、完整发布清单、采用回执、包故障隔离及路由发布引用。Alembic `20260923_27` 已在本机应用。
- Java新增本人范围摘要、管理员技术接口、单线程回放协调与真实建议版本序列账本。Flyway `V22__advice_effect_evidence.sql` 已在本机应用。所有新接口沿用原有登录、权限、Origin/CSRF边界。
- 唯一30分钟维护入口覆盖初次、周日及新成熟/修订答案；同步完成也检查。Python完成训练只通知原Java协调器，不新增定时器。数据库保留待处理任务，进程内研究只接纳一件；日常预测使用独立线程。
- 当前组合加最多三组固定替换组合，齐全五日/二十日/半年。Python冻住题目、基金家族、模型及行情，Java使用原决策与成交账本比较。方向不劣、净收益严格改善、回撤恶化不超2个百分点才采用；平分、覆盖下降或数据缺口保留原因。
- 采用通过完整路由事务CAS；同一生成批次固定路由。回退显示实际执行模型，不能继续宣称请求的新发布身份。实际使用回执须对应保存的三周期原预测及Java建议引用。
- 普通界面保留日期、方向、原因、明确动作及失败重试。研发表单移入后台工作台折叠区，模型编号只对管理员显示。效果页默认看真实发出预测及“当时建议/一直持有”两本模拟账。

### 本轮排查中修正的边界

1. 标签净值与输入特征同样遵守当时公开/修订边界，防止只保护特征却倒灌晚修订答案。
2. 新实现hash进入协议；旧实现空闲周期不能交给新实现接着采用。Java实际加载的账本、内部类型和策略配置取hash，同周期不混运行版本。
3. 取消后旧worker不能写回；失去租约和已进入终态的任务不能被迟到结果复活。
4. 最后一条回放结果已提交、决策未提交的崩溃点可恢复；已完成基金不整批重算。
5. 失败输入也被冻结，恢复不能静默读取修订后来源；全轮样本预算按周期分配，计划题和成熟样本均受限。资源不足明确失败，不能悄悄删基金提高成绩。
6. JSON持久化后的路由对比只比较真实模型ID和revision，避免日期/UUID序列化差异触发重复训练。
7. 采用核验只取待确认发布，每周期取实际建议引用，避免历史回执挤满分页后漏掉新发布。
8. 到期效果将未到期、待答案、核验失败分列；最新答案修订不增加预测数量，基金数不以模型条数代替。

### 检查与真实证据

| 范围 | 实际结果 | 限制 |
| --- | --- | --- |
| Vue | `npm run lint`、`npm run type-check`（build内）、`npm run build`通过 | 既有ECharts大chunk提示；不扩展本轮为打包优化 |
| Python | 本轮相关7个测试文件48项通过；最后资源预算修改后的相关28项再通过；变更文件Ruff通过 | TestClient依赖弃用提示，没有因此升级依赖；测试含明确受控行情/时间及真实隔离PostgreSQL |
| Java | 决策、账本、比较、日期契约、客户端、实库事务共22项通过；打包成功 | 实库事务中的公共行情与仓位为明确测试替身，不改真实持仓 |
| 真实行情回放 | `StrategyModelRealIntegrationTests`另1项通过，006730，176个估值日，2个完整模型组合与BUY_HOLD；保存读回一致 | Java不新开监听端口；读取原18000服务；不等同修正版44基金自动闭环 |
| 真实自动触发 | 周期 `12b58a53-ea76-48ba-84a4-db9fa306e067`；研究 `28fec635-3fae-4f7a-945c-9257b774b4d9` | 首轮为标签修正前实现，已取消，禁止当作修正版采用证据 |
| 同时服务 | 14:57:11创建44基金初始研究；14:58:08日常预测132项完成，21项跨境日历失败；14:59:07研究完成 | 证明初版研究与日常任务同时执行，不替代最终版本重启验收 |
| 实际模型包 | 4组完整组合共12个引用，逐项读取登记包、核对hash并用已有公共特征实际推理通过 | 半年选优期缺成熟样本，不能据包能运行推断效果合格 |
| 真实HTTP | 本人关注200、无认证401、普通研究403、未关注基金404；Python无Token/带Origin403；空账号不泄漏他人统计 | 最新接口错误补充由TestClient/Java事务校验；常用8080仍为旧运行版本 |
| 实际页面 | 5173普通账号三周期日期/方向/依据，未持仓/无订阅生成“不建议买入”，生成时刻15:33:37；管理员技术区折叠；390px无横向溢出 | 新摘要/效果成功态尚未接入常用新后端；接口失败明确报错，没有伪造成功数字 |

真实单基金回放运行ID：`c02c5f2f-a2ed-4e12-bc8e-03136e0a1860`。该研究事务回滚，原始结果在Java忽略目录保存。历史区间中BUY_HOLD约11.08%，两模型组合约-9.57%与9.20%；这是单基金历史开发实验，不是完整范围采用结论或未来收益承诺。

本机证据文件（均为忽略文件，不随代码提交；复核时先确认存在）：

- Python `.local-runs/auto-baseline.json`、`auto-final-evidence.json`、`auto-http-auth-results.json`、`auto-cleanup-result.json`、`auto-runtime.stderr.log`。
- Java `.local-runs/auto-runtime.stdout.log`、`auto-final-tests.log`、`auto-adoption-tests.log`、`auto-real-comparison-test.log`、`model-comparison-real-result.json`，以及 `target/surefire-reports`。
- 浏览器页面及步骤、对应时间/边界已摘录到本文件和进度表，未把模拟页面作为真实API证据。

### 当前数据与运行版本（15:46复核）

本机公共预测原文595条（含历史/并行候选，不能说成595只基金），真实答案0条、完整发布0条、采用回执0条。本轮旧实现周期已通过内部取消API返回200/CANCELLED，原训练证据与包保留。

| 服务 | 当前状态 | 含义 |
| --- | --- | --- |
| 5173 / PID11144 | 原Vite继续运行，HMR显示本轮前端 | 原进程未重启 |
| 8080 / PID49032 | 原IDE Java继续运行 | 未加载本轮新增自动接口；不宣称已部署 |
| 8000 / PID6348 | 原IDE Python继续运行 | 原进程未停止 |
| 18000 / PID40488，wrapper52404 | 本轮首版独立Python仍在 | 不是最终修正版；不能将新Java指向它并声称最终闭环 |
| 18081 / 原PID22048 | 已停止；最新JAR已打包 | 停止的是本轮验证进程，未碰IDE；后续Python重启受阻后未继续混版本运行 |
| 18073 | 未启动 | 新Vite启动被自动审批拒绝 |

自动审批两次分别拒绝“启动新Vite 18073”和“重启本轮Python 18000”，只返回 `blocked by policy`，没有更具体原因；没有换命令或端口绕过。第一次阻止完整新后端浏览器联通，第二次阻止修正版自然运行复验。拒绝发生后继续完成了所有独立检查。

最终Python源码实现指纹：`82620af587c160d6aaa5f9f94048e95186fd51fbe0c39767f151eadf76c1a476`。这是源码快照指纹，不是18000当前加载版本证明。Java新版本实际启动后可从管理员周期响应的 `runtimeEngineHash` 核对账本运行指纹。

本轮已打包Java JAR的SHA-256：`72770fe468843b2f1b56d7ccc18356f950bd5e48cc9098736aba35d6eb7d08f0`；路径 `C:\ideaProject\workSpace12\target\fund-core-0.1.0-SNAPSHOT.jar`。JAR构建成功不等于常用8080已经加载。

仅本轮两个临时验收账号已停用，7个测试登录会话已撤销，1份实际生成建议原文保留；刷新浏览器已返回登录页。其余账号、真实持仓和交易未修改。浏览器视口已恢复，临时标签页已关闭。

### 未完成项与继续执行顺序

1. 工具允许加载运行版本后，先确认本文件三个原IDE PID及本轮18000归属；保护其他窗口，按正常开发启动方式让Java与Python加载同一修正版。不要把当前18000当最终服务。本轮不自动继续执行已被拒绝的启动/重启动作。
2. Python配置继续用既有本机数据库和服务Token，`CORE_SERVICE_BASE_URL`须指向配套Java；Java `ai.service.base-url`须指向同版Python。核验Alembic27/Flyway22、Python协议实现hash和Java实际引擎hash。添加式迁移保留，不删除预测/报告/发布证据。
3. 让既有自然维护入口建立新周期；旧取消周期不可恢复采用。核对44基金或届时实际全关注范围、冻结窗口、完整组合、失败分母、扣费比较和真实KEEP_CURRENT/ACTIVATE原因。候选没改善就保持，不能为了验收强制切换。
4. 有合格新发布时等待合法新预测期次；同一期次原文必须复用。核对真实推理、保存、API/页面、建议modelRefs/releaseIds及ACTUAL_USE回执；无真实赢家时保留该条件，不伪造通过。
5. 补管理员详情/取消恢复、运行摘要全过程和效果成功态浏览器检查，并做真实进程中断恢复。更新S3-02/S3-04/S4-03/S4-04/S5-03/S6-02/S6-03/S6-04及V10/V12/V14/V22/V24。
6. 周日持续日程、五日/二十日与半年成熟效果按照进度表W01—W04等待；V15须等实际新答案/修订再核验。没有自动创建新提醒、外部通知或收费数据接入。

应用回退继续使用本轮前HEAD对应应用，保留添加式表和不可变档案；本轮真实发布数0，无需回写路由回滚。若未来已发布，再通过既定完整清单CAS恢复兼容组合，不能删除或重写历史原文。

### Git交付边界

截至本轮15:50收尾时，三库HEAD仍为Vue `88b7da8`、Java `f1271d1`、Python `19331b2`；本轮代码和文档均未提交、未推送。开始时三库干净，最终改动集中在本任务文件；未发现需覆盖的其他窗口源码改动。`.local-runs`、`.env`、模型产物、日志和测试账户材料均不进入交付提交。

## 用户追加展示调整与Git交付（2026-09-23 16:07:39 +08:00）

用户明确要求不展示同步页“预测运行状态”整块卡片，并授权三库提交推送。已移除SyncCenterPage挂载、未引用的AutomaticPredictionStatus组件和前端摘要请求/类型，后台自动任务与管理技术记录继续按原契约执行。此前关于该卡片状态流的验收要求已被本次指令替代；效果成功态等其余运行验收继续保留。

本轮Vue lint、类型检查、构建通过；源码及构建产物无卡片标题、组件和摘要请求残留。Python Ruff及相关7文件48项测试通过；Java决策、账本、比较、客户端及实库事务五组18项测试报告失败/错误均0。构建产物与检查日志保存在忽略目录。当前浏览器连接没有已登录标签页，本次展示调整以源码与构建验证，不冒称页面实测。

其他窗口新增的README三态入口与fund-three-state-prediction-plan-2026-09-23.md保持原样，不纳入本轮提交。三库原有未推送提交均为相同预测链的前置实现，将随本次正常推送；不强推、不变更服务运行状态。Git交付完成情况继续更新进度表。

Git代码交付于2026-09-23 16:10:57 +08:00完成远端核验：

| 仓库 | 本轮代码提交 | 远端分支核验 |
| --- | --- | --- |
| Vue | `3ff1a3284c59659e5eb0000a73fffe9764fa4f0c` | origin/main直读SHA一致，0 / 0 |
| Java | `2c748c701e2887e0c8798d8919727dd1cf71eb5d` | origin/main直读SHA一致，0 / 0 |
| Python | `e370b2359259cfc565a69ef49ab4651f4166e6fa` | origin/main直读SHA一致，0 / 0 |

默认Git TLS连接失败，使用单次命令的Windows证书后端完成fetch/push，证书校验保持开启，未修改全局Git配置。Java/Python工作区干净；Vue仅保留其他窗口两项文档修改。本交付记录在代码推送核验后追加，随单独文档提交推送。
