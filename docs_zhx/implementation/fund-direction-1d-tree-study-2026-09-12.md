# 1日固定输入浅层树对照方案（2026-09-12）

用户在第16.7节说明后授权“继续进行训练”。本方案写入时间早于本轮任何真实基金树模型拟合。先检查数据与旧错误，再仅改变模型表达形式；保留43只本人覆盖、29条9月14日真实预测、FIXED/WEEKLY、周更配方及20日/1日旧研究。完成事实另记结果和主实施文档第16.8节。

## 1. 训练前诊断

只读数据库事务限定原25只股票/混合基金及TUSHARE_PRO_FUND的2021—2024数据，不触碰他人关注。21,947条单位净值、公告日、内容hash与冻结训练历史逐行一致；5,704道原考题的线性分数可重新核算，未发现需要先修复的差异。本地现金分红事件5条；复权/单位净值因子变化超过0.1%的5处均与其匹配，不能由此证明全部折算事件覆盖。目标日无已记录现金分红，47道题的61日输入窗口包含已有现金分红，原模型25/47正确，未发现错误特别集中于该子集。保留全部题，不按诊断成绩筛选。

所有考题的最晚输入公告日均为T后1个自然日；历史08:00可用仍是研究假设，数据库source_published_at有值的记录为0，不能宣称已证明历史盘前实际可得。现金分红/复权字段仅用于事后诊断，未作为本次新特征，单位净值目标不改为总回报。

原模型在沪深300近5日上涨/非上涨状态下，原始准确率分别约49.96%/53.57%；在以首个FIT波动中位数0.01062279分组的高/低波动题上约50.37%/55.54%。这些是描述性分层，未控制全部其他因素，不构成“市场状态导致错误”的因果结论，也不据此挑参数。

首个FIT中，基金5日涨跌与沪深3005日涨跌相关系数约0.639，与对应指数相对沪深300的5日差值约0.614；波动/回撤约−0.758。确有信息重叠，仍不能断定新增指数完全无用。最终诊断输入、程序hash和结果在Python `.local-runs/direction-1d-tree-audit-20260912-final/`，之前诊断和补充调整因子检查保留。

## 2. 唯一模型假设与固定预算

问题：现有11项输入，在不同指标条件组合下可能有不同含义；浅层树是否比固定线性权重更有效？不是为了尝试多种模型挑最高分。

- 人口、输入、标签、日期、权重全部沿用上一轮对应指数SPECIFIC11：25只、11项输入、5,704道2024开发题。普通债基、转债、未核验对应指数的基金不混入，其他18只的既有覆盖状态保留；清单从冻结原包读取，不硬编码25个基金代码。
- LINEAR11：复用原SPECIFIC11的四季度和FINAL模型及原同题分数。其真实训练和唯一复现已完成，本轮重新校验FIT/权重/Scaler/分数，没有再拟合线性模型。
- TREE11：scikit-learn 1.9.0 HistGradientBoostingClassifier，loss=log_loss、learning_rate=0.05、max_iter=100、max_depth=2、max_leaf_nodes=4、min_samples_leaf=100、l2_regularization=10.0、max_bins=63、max_features=1.0、random_state=0；class_weight/categorical_features/monotonic_cst/interaction_cst均None，warm_start=False、early_stopping=False、validation_fraction=None。不创建随机验证集、不早停选轮数。
- 标准化仍只在同一FIT上拟合，并要求mean/scale与原线性模型逐值相同；家族/日期权重及严格大于0.5方向阈值保持不变。四季度及FINAL的FIT数为10,406/11,046/11,525/11,909/12,194，截止时刻继承原模型。
- 新树仅四季度＋FINAL共5次主拟合，以及同配方唯一5次复现；已用预算不可重复启动。单测仅含独立合成数据的一次工程拟合，未读取真实基金考题，不属于候选搜索。预算外不增加模型、调参或重新抽样训练。
- 树以不可执行的数值JSON保存真实阈值、拓扑、叶值和初始分数。每个模型在全部FIT及对应考题上与scikit-learn原预测比较，误差必须≤1e-12；节点、尺度、树数、配方或输入缺失时拒绝评分。复现模型及保存分数必须严格相等。冻结源码、依赖、输入、原线性完成记录和诊断证据，提供独立代码归档verify。

## 3. 评价和使用边界

主要指标为家族/目标日加权准确率，TREE11与LINEAR11逐题配对；同时报告原始正确数、均衡准确率、上涨/非上涨召回、四季度、逐基金结果、旧分组和恒上涨/恒非上涨/初始多数类/昨日延续基线。5目标日块重采样2,000次，seed=20260912，给出差值95%区间；不把5,704题当成5,704个相互独立的市场日。

2021—2024是已使用开发数据；2025/2026保护边界不解封，本轮不是独立验证。即使历史改善也只记录研究结果，不自动注册或替换运行模型；准确率不佳则如实结束这一套配方，不继续追加。9月14日真实净值未公布时，核对仍待到期。

实际验收需包含5次主拟合＋5次唯一复现、101项现有相关测试及必要新增检查、Ruff/格式、归档verify、真实预测与旧研究保护核对；不修改Java/Vue业务、数据库结构或后台任务。本轮不默认提交推送。下一轮是否值得继续应由完整结果决定，不能把本方案写成已经训练成功。
