# Chronos-2 补充验证：先查原因，再换一段数据比较

2026-09-09 用户确认执行两件事。上一轮 C00—C10 结果保留，本轮单独记录进度。

- [x] 第一步：核对两套校准器的收敛、拒绝规则及分基金分数与答案关系。已查明两套拟合均收敛，是项目的正斜率要求拒绝了结果；原因数据已写入独立运行包的 `diagnosis.json`。
- [x] 第二步：固定两套旧模型，在未用于上一轮比较的 2026 年数据上运行、复核并交付易读结论。226 条共同评分，自训练方向 63.64%、Chronos 45.57%；第二个独立环境全部复跑一致。

当前：2/2 完成。[先看结论](../reviews/chronos2-followup-2026-09-09.md)。继续保留自训练研究主线；两套旧校准概率都未超过简单历史比例，正式预测未发布。这里的完成表示用户要求的排查和新时段比较已经交付，不表示模型已经合格。

用户追加的规则调整也已完成，见文末。下表及原运行包保留补测时的约定，不用今天的新规则改写旧记录。

这轮在计算新成绩前固定以下条件：

| 内容 | 固定规则 |
| --- | --- |
| 基金 | 001632、006730、008888 |
| 预测日期 | 2026-04-09 至 2026-08-10，共 84 个交易日、252 条预定预测 |
| 答案观察截止 | 2026-09-08；预测未来第 20 个交易日累计收益是否为正 |
| 模型 | 原自训练模型、Chronos 权重及两套校准参数逐字节复用；不重训、不调参 |
| 主要比较 | 原始涨跌判断；与始终看涨、旧历史上涨频率、20 日动量及固定动量分数比较 |
| 校准诊断 | 单独计算旧负斜率公式在新时段的概率误差；不改变上一轮的拒绝状态或上线规则 |
| 顺序 | 只读导出历史输入 → 保存全部预测及摘要 → 读取答案 → 评分；失败也保留在 252 条分母内 |
| 判断边界 | 沿用至少 5 个完整 20 日时间块的标准；本段日历最多只有 4 块，因此不能据此确定稳定赢家 |

这是新的历史留出验证，不是事前发出预测后等待市场发生的未来实测。数据库保存的是现有版本，历史首次发布版本仍未获证。2025 年数据不读取，原服务不修改。

协议与原始证据保存在 Python 仓库 `.local-runs/chronos-followup-291d79f0-88d9-4f9a-8eb9-3094b994f75c/`，不提交原始资料或权重。执行入口为 `scripts/chronos_followup.py`，依次使用 `freeze`、`run --run <编号>`、`verify --run <编号>`；`freeze` 生成新编号，已有包不可覆盖。

## 完成证据

- 协议 SHA256：`75b7659a88a67e1b65856ccadb206adb34863e9c46373f9fbe1dc8c1f19d0b37`。新包 12 个文件摘要及评分重算通过；上一轮 28 个文件保持原样。
- 原定 252 条全部保留，226 条输入/预测/答案成功，26 条输入因 `NOT_KNOWN_AT_CUTOFF` 被拒绝，无模型推理失败。库中三只基金 2026-03-31 的净值公告日期分别为 4 月 22 日、21 日、22 日；本轮没有把这些公告前不可用的价格回填进历史输入。
- 实际覆盖率 89.68%，未达到协议记录的 90%；每只基金完整 20 日时间块为 3 个，未达到 5 个。正式选型未获确认，不降低门槛。
- 原服务环境和独立研究环境分别运行相关 214 项测试全部通过，其中新增边界测试 8 项。覆盖同日/滞后一日、20 日答案、未来答案变化不影响输入、先冻结预测再开库读答案，以及原 2025 保护边界。
- 第二个独立环境复跑全部 226 条，原始分数和诊断概率最大差为 0，Chronos 的全部分位数及输出一致；再算所有指标一致。回执为 `.local-runs/chronos-followup-replay-291d79f0.json`，复跑不连接数据库。
- 实际推理阶段约 7.81 秒，数据库仅本地只读事务；原 API、原模型及权重、原校准参数均未改动。

阅读原始指标时，`raw_direction` 下复用的 Brier 是 0/1 方向错误率，分块差值应读作“Chronos 方向错误率减自训练方向错误率”；其中继承的 `delta_convention` 字段仍写有 calibrated，不代表该分支实际使用了校准概率。`signed_calibration_diagnostic` 才是旧校准公式的概率诊断。两个分支都因时间块不足未获正式赢家资格；报告没有拿探索区间宣布稳定胜出。

## 校准规则调整（2026-09-09，用户追加）

**已将负斜率从直接拒绝改成风险提示，继续离线评分；上线仍须满足原有的数据、成绩、独立验证和发布要求。** 这一规则适用于后续新研究，旧报告、旧规则快照及其他方案草稿中沿用的正斜率描述不能覆盖它。

- [x] R01：统一比较适配器、历史研究与内部计算的方向说明。负斜率显示“反向校准，需要验证”，零斜率显示“常数校准”；非法数值、指纹错误、未收敛、数据越界仍拒绝。
- [x] R02：发布审查使用新 `CASH_RELEASE_POLICY_V2`，方向说明单列在 `calibration_diagnostics`，不伪装成检查通过项。所有其他检查不变；规则审批状态仍为原有 DRAFT，没有签发发布许可。
- [x] R03：完成测试、旧包复核、真实模型数值复算及文档同步。相关 335 项测试通过；两套原模型在 226 条记录上的概率与补测诊断差值为 0。

新建比较默认使用 `CHRONOS2_CASH_COMPARISON_V2`；加载 V1 时仍执行旧版拒绝逻辑，不能只改布尔值而保留旧版本号。新 V2 概率参与原 Brier、对数损失及可靠性评估；原始方向列继续独立保留，模型或校准器不重新拟合。

内部计算结果新增 `calibration`（斜率、状态、中文说明）。发布审查新增 `calibration_diagnostics`（窗口、斜率、状态、说明）。正式研究通过、规则批准/冻结和模型发布仍是不同步骤；旧 V1 冻结快照可原样读回，但不能当作 V2 的审批依据。本次没有数据库写入或迁移。

验证回执：Python `.local-runs/calibration-policy-v2-check-fb39f3f9-4a6b-46d7-96b6-7e5ab98579a1.json`。旧首轮 28 个文件、补测包 12 个文件及原评分复核通过。226 条复算使用既有输入、原模型和已封存的 Chronos 分数，属于规则回归验证，不是新增样本、重训或再次证明预测有效。

旧包的严格原版推理复跑仍核对原始源码摘要；需要使用 Python 提交 `09d740c6cb83c2fe1903a7bbc43ff3a1a03b51e9` 的源码及旧环境。当前代码可核验旧文件及重算原评分，新源码不能冒充当时源码。
