# 新窗口接手：后台自动选用模型与结果展示

## 交接结论

用户已明确：自己不需要理解或选择新旧方法、候选模型。系统后台负责训练、公平比较、自动采用和回退；用户日常使用预测、持仓建议、实际效果和运行状态。

之前已经把回放改成一直持有与真实多周期模型组合对照，并完成相关代码提交。本轮新方向不是再给候选模型改名，而是把研发操作移出日常界面，并补齐持续自动选用流程。

## 必须先读

1. 当前进度：`C:\WebStormProject\workSpace05\docs_zhx\implementation\fund-auto-model-selection-progress-2026-09-23.md`
2. 实施方案：`C:\WebStormProject\workSpace05\docs_zhx\implementation\fund-auto-model-selection-plan-2026-09-23.md`
3. 本交接文件。

实际目录为`docs_zhx\implementation`。旧2026-09-22方案、进度、交付文件只用于查已有实现和历史证据，不从旧P0重新做一遍。

## 当前提交与进度

| 仓库 | 工作目录 | 本轮交接前代码提交 |
| --- | --- | --- |
| Vue | `C:\WebStormProject\workSpace05` | `dabfe4f551fe73de2c7ec67af31a54f2eaac200c` |
| Java | `C:\ideaProject\workSpace12` | `f1271d19f11e20b6ba46fa61cca992f86fc98eff` |
| Python | `C:\pythonProject\workSpace06` | `19331b2801109a98000c2fe00fb5aaf9727ce2c1` |

以上为本地提交，未推送。Vue后续还有本组文档提交，以接手时`git log`为准；不要重置或回退。提交时三仓库没有发现无关脏改动，开工前仍要重新检查。

新方案文档3/3准备完成；**功能实施0/30，验收0/24，下一步S0-01**。本窗口没有实施新方案、没有新增自动日程或模型切换。旧工程35步完成不等于新自动闭环完成。

## 可复用的已有工作

- 全部关注基金预测和一键同步；已移除一日总开关与个人实验订阅限制。
- 多周期预测记录、模型登记/路由/实际身份、实验研究队列、选优函数、到期核验、综合建议与历史报告。
- 日期默认当年1月1日至今天；回放结束选今天时实际截至昨天，界面区分选择与计算日期；表头统一“最大回撤”，已补“最后账户价值”。
- 已撤下旧二十日买卖规则、去公告两组；真实回放读取当前/候选多周期包，验证完整周期和模型指纹，同账本比较。
- 对照工具本轮转入后台评价/管理员排查，不删掉已做的模型推理、证据及测试。

## 运行与验证注意事项

- 上一轮Vue lint/type/build，Python21项，Java16项+1项真实集成通过。这是既有验证，不能抄到新验收表宣称新功能通过。
- 上次8080 Java和8000 Python为用户IDE调试服务，未确认加载最新模型对照代码；5173是前端服务。开工重新核对真实启动方式、服务版本和端口，不沿用旧PID。
- 临时Python18000验证服务已停止。尝试启动独立Java验证服务曾被自动审批以“blocked by policy”拒绝；之后通过无新增Java监听端口的测试完成真实Python HTTP、模型、行情和数据库事务读写核验。不能据此说常用8080完整链已经验证。
- 本地真实结果JSON在三仓库的`.local-runs`，被Git忽略，可能缺失；必要时按有界范围重新验证。认证/Token/数据库密码从原有环境配置获取，不打印、不提交。
- 不擅自结束其他窗口的调试进程、覆盖无关修改或推送。本地实现和可逆检查继续自主完成；运行切换根据现场证据和既有授权处理，工具拦截如实记录，不绕过。

## 执行要求

1. 读取目标仓库适用的AGENTS.md，使用Vue、Java、Python工程技能及系统设计治理；沿用现有架构与组件。
2. 从S0-01开始，按新方案S0—S6的30步持续实施，不停在计划、不逐步询问已明确事项。
3. 每开始、完成、受阻或等待一步，立即更新进度页总览、步骤状态/证据/开始及完成时间、下一步和日志。
4. V01—V24逐项记录实际结果，静态、替身、真实模型/数据库、真实API和浏览器证据分开；代码写好不等于通过。
5. 真实窗口或半年到期等待单独记录，并继续其他独立工程工作。不能提前宣称长期效果通过。
6. 新模型可在实验阶段采用，不要求80%或长期优势；采用按固定协议、相同条件及实际调用证据，保留失败和回退。
7. 不让用户选择模型或手动发起训练才能获得预测。日常效果默认看系统建议与一直持有，真实效果和当前模型历史回看分开。
8. 不自动交易、不新增收费数据源、不执行未授权外部写入；保护已有权限和个人数据隔离。
9. 中文沟通及必要中文业务注释；Git标题严格`[zhx]中文说明`。本次接手不默认包含推送授权。
10. 实现完所有可执行工程工作后，交付实际检查结果、还在等待的条件及下一步；不要把未实现事项标为完成。

## 可直接复制到新窗口的指令

```text
请接手基金雷达“后台自动选用模型与结果展示”实施任务，直接按已完成的文档开发，不重新讨论需求，也不要只输出计划。

先读：
1. C:\WebStormProject\workSpace05\docs_zhx\implementation\fund-auto-model-selection-progress-2026-09-23.md
2. C:\WebStormProject\workSpace05\docs_zhx\implementation\fund-auto-model-selection-plan-2026-09-23.md
3. C:\WebStormProject\workSpace05\docs_zhx\implementation\fund-auto-model-selection-handoff-2026-09-23.md

三个仓库：Vue C:\WebStormProject\workSpace05；Java C:\ideaProject\workSpace12；Python C:\pythonProject\workSpace06。

目标：后台自动训练、公平比较、采用当前更合适的可运行模型并保留回退；用户日常只看预测、建议、效果及运行状态，不需要挑模型或手动训练。当前新方案实施0/30、验收0/24，从S0-01开始。

先核对AGENTS.md、Git及真实运行版本，保护其他窗口修改，复用已有能力，不从旧方案重新开发。每开始、完成、受阻或等待一步立即更新新进度页，填写证据、时间和下一步；V01—V24逐项验证，替身测试不能冒充真实运行。

持续完成所有可执行工程工作，不每做一步就询问。遇到真实预测窗口/长期到期等待，记录具体条件并推进独立工作，不提前勾选。常规本地实现和可逆检查直接进行；不自动交易、不新增收费源、不进行未授权外部写入。Git标题用[zhx]中文说明。本次未授权推送。
```
