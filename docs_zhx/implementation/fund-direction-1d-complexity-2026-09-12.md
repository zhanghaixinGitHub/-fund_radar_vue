# 一日模型错误诊断与复杂度对照（2026-09-12，拟合前方案）

用户已授权按“错误诊断 → 有限复杂度对照 → 根据证据决定未来观察”的路线推进。本轮交付诊断、训练、唯一复现和比较结果；满足研究观察条件仅表示值得进一步验证，不自动发布或替换页面模型。

## 1. 诊断依据和本轮问题

复用已完成的完整样本 ACTIVITY12（成交额12项树），只读重算16个既有窗口的训练/随后成绩，未新增拟合。四季度训练题准确率60.81%、60.18%、59.81%、60.74%，对应随后题54.90%、56.75%、56.69%、50.07%。训练和随后数据存在差距，尚不能断言唯一原因是欠拟合或过拟合。

按目标月份看，2024年10月预测上涨92.44%，实际上涨51.33%；12月预测上涨7.45%，实际上涨52.36%。这些是待解释的判断偏移，不是已证实的因果关系。原总体54.62%、上涨召回37.50%、非上涨召回70.72%。诊断同时保留各基金、分组、市场T日方向、恢复题及月度成绩。

本轮检验：保持输入不变，简化或适度增加树的表达能力，并用每季度之前的表现选择复杂度，能否改善随后的完整样本成绩。

## 2. 固定比较范围

- 本人43只关注范围只读核验；沿用共同25只、6,025题、241个目标日。其余18只原因保留，不因成绩删除基金或日期。
- 复用 `.local-runs/direction-1d-full-threshold-20260912` 的 universe、exam、16个 schedule 窗口、既有深度2模型及原分数。旧模型不重训。
- 输入仍为原12项，净值可用性沿用独立V2研究假设，原公告日期/输入/答案来源hash保留；历史首次发布时间和首版本仍未证明。
- 保持504交易T日窗口、家族/日期权重、原尺度、季度截止、100轮、学习率0.05、叶最少100样本、L2=10、63分箱、seed=0、严格分数大于0.5判上涨。无随机拆分、早停、最近样本加权或阈值搜索。
- 三个候选：旧深度2/最多4叶；新增深度1/最多2叶；新增深度3/最多8叶。只改变深度与配套叶数，不能将结果归因于其他参数。
- 2021—2024均为反复使用的开发资料。2025/2026保护不变，不用其分数选模型；本轮不新增外部请求、数据库写入、FINAL模型、模型登记、真实预测或交易。

## 3. 选择顺序和拟合上限

每季度沿用此前63个已成熟目标日，按连续21日分成三个时间块，各块只使用更早且答案已成熟的数据训练。同日所有基金一起划分，不跨训练/验证。

先生成两种新复杂度的12个较早窗口模型（24次拟合），与原深度2在相同63日上的分数比较。以家族/日期加权准确率最高者选择；差距在1e-12以内时，依次优先旧深度2、深度1、深度3。选择不读取外层考题答案，不按2024总成绩选事后赢家。

选择结果先写入不可覆盖回执，再完成两个新复杂度的四季度模型（8次拟合）。每个季度由此前选定的版本回答随后题，形成主候选 SELECTED_DEPTH。两个固定深度的全年结果完整披露，作为诊断对照，不用它们事后替换主候选。

主拟合上限32次，唯一复现上限32次。每次拟合前独占预算记录，重复调用不覆盖旧记录；失败保留证据，不静默追加试验。每个模型保存JSON数值树并与原生输出核对，主训练和复现模型、分数、选择及诊断必须一致。

## 4. 研究观察条件与验收

主比较为 SELECTED_DEPTH 对原 ACTIVITY12。事前沿用以下观察条件，完成后不按结果调整：

1. 整体家族/日期加权准确率提高。
2. 所有基金按目标日一起进行5日块重采样，2,000次、seed=20260912；配对差值95%区间下限大于0。
3. 至少3/4季度、17/25只基金改善，单基金下降不超过5个百分点。
4. 上涨和非上涨召回均不下降。

这些条件只是反复使用开发题上的研究筛选，不是正式发布标准。未通过则记录 NO_STABLE_GAIN，不替换在用模型。通过也只进入后续未来观察的设计；本轮不直接接入。

工程验收覆盖：未来输入/答案拒绝、相同题目和时间窗口、选择并列规则、损坏模型拒绝、数值恢复、原模型及旧预测不变、32+32次训练预算、旧文件hash不变。结果报告区分静态/测试、历史研究、真实预测现场状态。
