# 全市场基金雷达 — 详细实施步骤与进度看板

> 关联需求：`docs_zhx/requirements/fund-radar.md`
> 技术参考：`docs_zhx/design/fund-radar.md`
> 验收参考：`docs_zhx/testcase/fund-radar.md`
> 规则：本文件是跨会话的唯一实施进度入口。任何一步真实完成后，必须勾选对应任务并更新“当前状态”。

## 0. 当前状态（下次会话从这里开始）

| 项目 | 当前值 |
| --- | --- |
| 最近更新 | 2026-08-28 |
| 总体状态 | 基金市场已有 43 个启用基金份额、43 个精确 Tushare 来源代码与 67,870 条日净值。M1 的同步范围已从历史固定样本迁移为数据库基金市场中全部启用的 Tushare 基金；“我的关注”只保留为用户关系，不参与同步选取。迁移、真实同步与三端构建/测试均已完成 |
| 已完成的实施任务 | M1 基金市场动态同步改造：保存精确来源代码、按市场范围读取、历史同步审计命名迁移、手动/定时任务/API/前端统一改名、清除旧六条人工样本初始化入口、基金市场默认每页 10 条；Alembic `20260828_05` 已实际应用，真实 `MARKET_NAV_INCREMENTAL` 已新增 5 条净值；M0、M2/M3、M4 既有完成项保持不变 |
| 当前阶段 | M1 基金市场同步链路已在数据库验证；同步只处理已入库、`ACTIVE` 且来源为 `TUSHARE_PRO_FUND` 的份额，不将外部接口的部分目录描述为完整市场 |
| 当前任务 | 将运行中的 Python、Java 服务替换为本次构建版本，再以具备后台权限的浏览器会话打开 `/admin/sync`，确认新文案、任务路径、43 支进度与基金市场默认每页 10 条 |
| 下一步操作 | 在不打断现有服务的前提下启动候选 Python/Java 进程并完成健康检查，之后切换并执行 TC-25 浏览器冒烟；连续观察后续工作日定时 `MARKET_NAV_INCREMENTAL`。M2/M3 仍等待数据授权和模型准入口径 |
| 当前阻塞 | 代码和数据库已就绪，但运行中的 Java/Python 进程尚未切换，因此现有浏览器页面可能仍使用旧接口；切换会影响本机正在使用的服务，需要在用户允许的维护窗口进行。M2 新闻/公告授权、M3 模型历史数据与准入口径、M4 数据日期仍未确认；M5 继续永久禁止自动交易 |

### 已确认的原则

- Java 负责对前端的业务接口、用户关注、提醒、审计和 Java → Python 内部服务的调用。
- Python 使用 FastAPI + Pydantic + SQLAlchemy/Alembic + Celery，负责数据采集、AI 分析、回测和内部接口。
- 一期先做全市场基金雷达；不要求导入支付宝持仓。
- 不保存支付宝认证信息，不做网页模拟下单，不做自动交易。

### 每次恢复工作的方式

1. 先阅读本文件的“当前状态”和未勾选的当前任务。
2. 再阅读关联需求文档；涉及接口、数据或安全时，读取关联设计和测试文档的对应章节。
3. 只实施当前任务；完成代码和本任务规定的验证后，才将 `[ ]` 改为 `[x]`。
4. 更新本节的“最近更新、已完成的实施任务、当前任务、下一步操作、当前阻塞”。
5. 若失败或需要用户决定，不勾选任务；在“变更/阻塞记录”追加事实、影响和恢复条件。

### 完成标记规则

- `[ ]`：未开始或未达到验收条件。
- `[~]`：进行中。Markdown 不会自动渲染为勾选框，但用于明确显示“正在做”。
- `[x]`：代码、配置/迁移和本任务验证均已完成，并有对应记录。
- `[!]`：被阻塞；必须写明阻塞原因和下一步所需决定。

不得仅因创建了文件、执行过命令或编译通过，就勾选涉及数据、接口或模型效果的任务。

## 1. M0｜工程基线与接口契约

目标：创建可独立启动的三套工程骨架，确定跨服务边界；不接真实基金数据、不制作业务页面。

### M0-01｜锁定工程基线与实施范围

- [x] 确认前端采用 Vue 3.5.41 + TypeScript + Vite 8.2.2 + ECharts 6.1.0。
- [x] 确认数据层目标采用 PostgreSQL 16 + Redis 7；M0 提供 Docker Compose，当前不依赖已启动的本地容器。
- [x] 确认一期为单用户个人部署；数据模型保留 `user_id` 扩展位，但暂不实现注册和多租户。
- [x] 确认 M0 不接入任何真实数据源，仅使用 Mock 数据；M1 开始前必须逐个确认数据来源授权和接入方式。

完成标准：四项决定记录在本文件“变更/阻塞记录”，并同步更新需求/设计文档中受影响的部分。

### M0-01A｜确定 Python AI 服务框架

- [x] 评估 FastAPI 与 Django 是否符合“Java 负责业务接口，Python 只负责 AI/采集/内部接口”的边界。
- [x] 确认 Python 使用 FastAPI，并同步修改需求、设计、依赖和启动文档中的相关表述。

完成标准：仅保留 FastAPI 作为 Python Web 框架，不混用 Django ORM/Admin 与 FastAPI 路由；选型理由记录在本文件。

### M0-02｜创建三项目工程骨架

- [x] 在 `C:\WebStormProject\workSpace05` 初始化前端工程、基础目录、环境变量示例和健康页；已通过 lint、类型检查、构建和 Vite 启动。
- [x] 在 `C:\ideaProject\workSpace12` 初始化 Spring Boot 工程、模块包结构、统一响应和异常处理；已通过 Maven 测试和实际启动。
- [x] 在 `C:\pythonProject\workSpace06` 初始化 FastAPI、Pydantic、Celery、SQLAlchemy/Alembic 与应用目录；已通过 Ruff、pytest 和实际启动。
- [x] 三个项目已补充 README、启动命令、精确依赖清单/版本声明和 UTF-8 `.editorconfig`。

完成标准：三个项目能独立安装依赖并启动健康检查；不包含真实密钥。

### M0-03｜建立本地基础设施与分环境配置

- [x] 已通过 Compose 启动 PostgreSQL 16.14 和 Redis 7.4.7；两个容器均为 `healthy`，首次初始化已创建 `fund_core`、`fund_ai` 及最小权限账号。
- [x] 已验证 Redis 响应 `PONG`，并按 Java `/0`、Celery 队列 `/1`、任务结果 `/2` 的隔离约定运行。
- [x] 已创建三端实际本地 `.env`、基础设施实际本地 `.env` 与 Git 忽略规则；项目只保留 `local` 一套配置。
- [x] Java/FastAPI 已实现健康检查、TraceID、超时和优雅关闭；Celery Worker 使用 Windows 兼容的 `--pool=solo` 启动，`health_probe` 已真实投递并成功回收结果。

完成标准：无真实凭证提交到仓库；任一服务失效时健康检查能准确失败。

### M0-04｜冻结 v1 接口与数据契约

- [x] 已定义前端 → Java 的基金列表、基金详情、信号、事件、关注列表接口 DTO；列表/详情已用 M0 Mock 真实联调。
- [x] 已定义 Java → FastAPI 的内部基金、信号、事件、任务触发接口 DTO；健康、列表和详情已实现，其他路径冻结至对应阶段。
- [x] 已定义服务身份认证、短超时、TraceID、游标、幂等键规则和错误码；M0 真实验证了 403、404、503 契约分支中的前两项。
- [x] 已产出 `docs_zhx/design/fund-radar-api-v1.md` 作为 v0.1 可版本化契约；前端已定义并通过 TypeScript 校验 `FundSummary`、`FundDetail`、`FundPage` 和 Java API 调用封装。

完成标准：三方均可用 Mock 数据完成一次列表和详情调用；前端无 FastAPI 直连请求。

### M0-05｜建立安全、审计与质量基线

- [~] Java 已完成单用户本机模式下的 TraceID、全局异常、CORS 限制和服务调用日志；正式登录、资源授权和持久审计须在 M1 用户域落地。
- [x] FastAPI 内部 API 默认拒绝浏览器访问，仅接受服务身份请求；已实测无 Token 和含 `Origin` 的请求均为 403。
- [x] 前端已配置 Java 唯一请求入口、统一错误处理、404 路由兜底和不记录服务 Token 的边界。
- [x] 已通过前端 lint/类型检查/构建、Java Maven 测试、Python Ruff/pytest/启动自检。

完成标准：通过 TC-08、TC-10 的基础部分；日志中无密码、Token、Cookie、验证码或支付信息。

### M0 验收

- [x] 三个工程均已独立启动，且使用明确版本的依赖和配置。
- [x] 前端仅调用 Java；Java 已用 Mock 调用 FastAPI 内部接口。
- [x] 环境变量、内部鉴权、日志、TraceID、健康检查、PostgreSQL、Redis 和 Celery Worker 均已通过 M0 基础验证。

## 2. M1｜全市场基金目录、净值与基础看板

目标：在不需要个人持仓的前提下，完成基金目录、日频数据、筛选、详情与走势图。

### M1-01｜设计并落地数据迁移

- [x] FastAPI 使用 SQLAlchemy/Alembic 为 `fund_master`、`fund_share_class`、`nav_daily`、`source_registry` 建立迁移、唯一约束和日期分区策略；v3 已增加 `fund_share_class.source_code` 与不保存原始响应的 `source_sync_run`。
- [x] Java 使用 Flyway 为关注、信号快照、审计、提醒和通知等当前所需表建立迁移；未创建交易执行表。
- [x] 已为关键查询建立索引；在事务回滚的测试数据上，`EXPLAIN` 已命中 `nav_daily` 按基金代码和日期的分区索引。

完成标准：对应迁移可在空库重复执行；TC-02 的约束与幂等验证通过。

### M1-02｜实现基金目录与日净值同步

- [x] 已实现 HTTPS Tushare 适配器：Token 仅从 `SecretStr` 配置读取，连接/读取/连接池均有超时；`fund_company` 规范化管理人简称，`fund_basic` 按市场/状态分片，`fund_nav` 仅按指定日期批量请求。
- [~] 已以 `MANUAL_PUBLISHER_VERIFIED_SAMPLE` 登记一次性手工核验样本（默认禁用自动同步），并幂等写入 6 个基金主实体和 6 个份额类别；这不是全市场目录同步。
- [~] 目录同步在 2000 积分下安全失败：`fund_basic(market=O,status=L)` 达到 15000 条配置上限时，不写入部分目录并记录失败运行；需完整返回条件后才能完成全市场目录。
- [x] 已实现显式日期的日净值同步、500 条分批提交、内容哈希去重、失败记录和 Celery/本机命令入口。2026-08-25 实测首次写入 5 条、第二次新增/更新均为 0；第 6 条样本当日无返回净值，保持 `NOT_SYNCED`。
- [x] 来源诊断仍为受服务身份保护的只读接口；`source_sync_run` 保存同步类型、日期、统计、状态与脱敏错误摘要，不保存 Token 或原始响应，不引入 Django Admin。

完成标准：可从首个确认来源同步一批真实基金数据；失败任务可安全重试且不重复写入。

### M1-03｜实现 Java 基金读模型与关注接口

- [~] FastAPI 列表/详情已从持久化目录读取 6 条真实份额样本，Java 转发与 Redis 降级链路已实测；详情将同一条最新净值记录的 `unitNav`、`accumulatedNav`、`asOfDate` 与 `dataSource` 一并返回，Vue 展示最新净值快照，`TUSHARE_PRO_FUND` 显示为 Tushare 数据接口。5 条已同步样本返回 `asOfDate=2026-08-25`，另 1 条仍为 `NOT_SYNCED`/手工目录来源。全市场目录、类型筛选和走势图仍依赖 M1-02 的完整目录条件。
- [x] 已实现本机单用户的关注列表、添加、取消接口；重复操作幂等并写入 `audit_log`。尚未具备登录后的多用户隔离，TC-09 继续待完成。
- [x] Java 对列表和详情使用 Redis 保存最后成功读模型；FastAPI 不可用时返回带 `stale=true`、`cachedAt` 的缓存结果，缓存未命中才返回 503。

完成标准：TC-01、TC-09 的相关场景通过；接口响应带 `traceId` 和 `dataAsOf`。

### M1-04｜实现前端市场基础页面

- [~] 已实现市场入口、关键词查询、服务端游标分页、列表、详情路由以及加载、空数据和请求失败状态；类型筛选仍待 M1-02/03 的真实目录字段。
- [ ] 实现净值走势图、最大回撤/基础风险展示和数据截至时间标签。
- [~] 已接入关注/取消关注及 `stale` 陈旧状态展示；旧 Java 服务未切换时页面明确提示需要重启，不伪造关注结果；真实数据状态仍待 M1-02。

完成标准：用户无需导入个人持仓即可完成 TC-01；前端不出现交易或支付宝登录入口。

### M1 验收

- [ ] 已能浏览、搜索和关注可用基金；目录、份额类别和日净值不混淆。
- [ ] 数据缺失或来源故障时正确降级，不伪造当日净值或信号。

## 3. M2｜公告、政策与资讯事件分析

目标：将可授权的外部资讯转化为可追溯的基金相关事件，不将新闻直接转成买卖指令。

### M2-01｜建立来源治理和采集任务

- [ ] 在 `source_registry` 配置每个来源的授权范围、限频、超时、保留期限和开关。
- [ ] 为每个来源实现独立适配器、退避重试、失败隔离和运行指标。
- [ ] 仅保存允许保存的标题、链接、时间、摘要、哈希和模型标签。

完成标准：任何单一来源异常不会拖垮其他任务；可追溯每条数据的来源和抓取时间。

### M2-02｜实现去重、实体关联和事件分类

- [~] 已建立 `news_item`、来源引用、`market_event` 和 `event_relation` 的迁移、哈希唯一约束、保留期和审核状态；尚未在无授权来源时运行归一化或写入事件。
- [ ] 对资讯做哈希/相似度去重，保留来源引用关系。
- [ ] 提取基金、基金管理人、行业、指数、公司、政策主题等实体。
- [ ] 先关联实体再关联基金，保存相关性分数、可信度、时效和解释。
- [ ] 对社交数据仅生成低可信舆情标签，不生成直接交易动作。

完成标准：TC-04、TC-05 的资讯关联部分通过；页面能打开原始来源链接。

### M2-03｜实现资讯前后端查询与解释

- [x] FastAPI 提供仅服务身份可读的已审核、未过保留期、按基金关联的事件游标接口；空库返回空页且不触发采集。
- [x] Java 建立事件读模型并缓存；前端展示时间线、来源、可信度和关联理由，并明确“关联不代表因果”。
- [ ] 前端实现来源异常、无关联资讯和过期资讯状态。

完成标准：每一条展示资讯均包含发布时间、来源链接、可信度和非因果提示。

### M2 验收

- [ ] 资讯采集受授权、频率和保留策略约束。
- [ ] 同一事件不会在页面重复轰炸；相关性可解释且可追溯。

## 4. M3｜类别化信号、回测与提醒

目标：在数据完整、模型可回溯、回测有效的前提下，提供概率型信号和风险提醒。

### M3-01｜建立特征与风险指标

- [~] 已建立 `feature_snapshot` 的类别、版本、完整度、不可评分原因与 JSON 特征契约；尚未在没有真实日频数据时构建特征。
- [ ] 按基金类别实现不同特征：权益/指数、债券、QDII、FOF、货币基金不得混用。
- [ ] 记录特征版本、数据完整度、截至日期和异常原因。
- [ ] 对缺失、停牌、成立时间过短或不适用类型标记“不可评分”。

完成标准：TC-05 通过；货币基金不输出上涨/下跌预测。

### M3-02｜实现信号模型与解释

- [~] 已建立 `forecast_result` 的可重现字段与数据库约束：非 `SCORED` 状态强制没有方向、概率和置信度；内部/公开只读接口与页面会按状态展示，尚未运行评分模型。
- [ ] 先实现可解释基线规则/统计模型，再评估复杂模型。
- [ ] 输出方向、概率、置信度、风险等级、最大回撤估计、依据、模型/特征版本。
- [ ] 对每次评分建立可重现的结果记录，Java 幂等接收信号快照。

完成标准：TC-06 通过；无模型或数据不足时不产生方向性结论。

### M3-03｜实现滚动回测与模型准入

- [~] 已建立 `backtest_run` 的版本、费用、指标/基线和训练/验证/测试时间顺序约束；尚未在无真实样本时创建或执行回测。
- [ ] 实现按时间滚动的训练、验证、测试切分，禁止未来数据泄漏。
- [ ] 与长期持有、定投和业绩基准对比，并纳入合理费用假设。
- [ ] 记录收益、最大回撤、波动率、命中率、样本区间和失败原因。

完成标准：TC-07 通过；未优于基线或样本不足的模型不可发布。

### M3-04｜实现关注信号与提醒

- [x] 本机单用户可幂等设置 `RISK_LEVEL`、`SIGNAL_CHANGE`、`EVENT` 三类提醒规则；风险阈值为 0–1，操作写入审计，前端明确其仅为站内信息提醒。
- [ ] Java 根据新快照幂等生成站内提醒，支持查看历史和已读状态。
- [ ] 前端明确展示“分析提示，不构成交易指令”。

完成标准：同一事件/信号不重复提醒；没有任何自动交易接口或按钮。

### M3 验收

- [ ] 所有信号可追溯数据、特征、模型与回测；模型失效时自动降级。
- [ ] 提醒只提供风险和解释，不生成或执行买卖动作。

## 5. M4｜可选个人持仓增强

目标：在用户主动授权的前提下，提供关注之外的个人收益与仓位分析；不是一期前置条件。

### M4-01｜确定录入方式与隐私边界

- [x] 用户已确认以截图中的完整可见行作为本机导入样本；不接入支付宝登录、不上传原图、不保存原图路径或认证信息。导入文件仅保留在 Git 忽略的本机文件中，入库后保存字段、来源说明和内容哈希。
- [ ] 设计上传文件白名单、大小限制、病毒扫描、加密存储、保留期限和删除机制。
- [x] 本轮由用户确认的字段手工录入；未来截图 OCR 结果仍必须经用户确认后才写入持仓。

完成标准：上传链路不收集密码、验证码、Cookie 或支付凭证；有完整删除能力。

### M4-02｜实现持仓分析

- [~] 已建立 `portfolio_snapshot` 与 `portfolio_holding_snapshot`，以 `NUMERIC` 保存截图展示金额、占比和收益，并以内容哈希幂等导入；当前没有份额、成本、交易记录或数据日期，禁止推算真实收益率。
- [x] Java 提供仅本机单用户使用的 `GET /api/v1/portfolio/current`，Vue 持仓快照页只经该接口读取，标注“日期未知/非实时”，不提供写接口、交易或支付宝入口。
- [ ] 实现仓位集中度、行业暴露、回撤和关注基金风险联动。
- [ ] 补充本人数据范围校验、审计和删除/导出能力。

完成标准：持仓数据仅本人可见；所有分析不触发外部交易。

## 6. M5｜未来能力闸门（当前不实施）

- [x] 不开始实现本阶段；Java 回归测试验证运行时不存在凭证采集、支付宝、买卖、申购、赎回或自动交易路由。
- [ ] 仅当取得基金销售/投顾所需合规结论，以及平台书面交易 API 授权后，重新发起独立 L3 评审。
- [ ] 未满足上述条件，禁止创建自动申购、赎回、买入、卖出、模拟点击或支付宝登录功能。

## 7. 变更/阻塞记录

> 新增记录按时间倒序追加。只记录已发生的范围变更、真实阻塞、实施结果或恢复条件；不要记录对话流水。

### 2026-08-27｜数据同步中心与可观察进度

状态：已完成三端实现；Python 自动化验证和 Vue 生产构建通过，待 JDK 17 下重新编译 Java 与用户在本机执行一次真实同步。
实现：Vue 新增 `/sync-center` 与顶部“数据同步”入口，基金详情页不再承载同步按钮。页面创建任务后每 1.5 秒轮询任务阶段、当前基金、完成步数和最终读取/新增/更新/跳过统计；任务执行中禁止重复创建。Java 新增 `/api/v1/sync-jobs` 统一代理接口；Python 以单线程 `LocalSyncJobManager` 创建后台任务，逐基金回调进度，真实写入仍复用 `TushareFundSyncService`、`source_sync_run`、幂等写入与 PostgreSQL 咨询锁。
一致性：手动任务和 Celery 增量任务继续共用数据库咨询锁；同步中心额外限制当前 Python 进程只有一个活动手动任务。创建任务不再保持 5 分钟 HTTP 长连接，普通 Java → Python 请求仍为 3 秒；缺少历史基线、Tushare 失败或配置问题都写入任务的 `FAILED` 安全摘要，不覆盖既有净值。
验证：Python `ruff` 与 27 项 pytest 通过；Vue lint、类型检查和生产构建通过。Java Maven 因当前终端使用 JDK 8 而无法解析项目已有的 Java 17 `record` 语法，未能重新编译；编译机切换到 JDK 17 后应先执行 `mvn test`。真实点击会访问 Tushare 并写入运行记录，未由自动化测试触发。
边界：任务进度是当前 Python 服务进程的内存安全摘要；页面刷新可继续观察同进程的最近任务，但 Python 重启后状态失效并提示重新发起。当前只登记六只重点基金净值同步，不开放任意代码、全市场目录、资讯、评分、买卖建议或交易。

### 2026-08-27｜详情页手动重点基金净值同步

状态：已完成三端实现与自动化验证，待用户在本机页面执行一次真实同步。
实现：Vue 在基金详情页提供“同步六只重点基金净值”按钮，禁用重复点击并显示安全统计；Java 新增 `POST /api/v1/funds/sync/focused-nav-incremental`，保持浏览器不直连 Python；Python 新增受服务令牌保护的内部同步入口，直接复用 `TushareFundSyncService`，因此本机错过 20:00 时不需要另启 Beat 或 Worker。人工调用使用独立 5 分钟读取超时，普通读模型仍维持 3 秒。
一致性：手动入口与 Celery 任务共用 PostgreSQL 咨询锁；已有同步时返回 `409`，不创建重复运行或重复调用 Tushare。同步仍按同源水位、窗口校验、幂等写入和失败关闭执行，统计写入既有 `source_sync_run`；异常不覆盖历史净值，不返回 Token 或原始响应。
验证：Python Ruff 与 25 项 pytest 通过；Java Maven 6 项测试通过；Vue lint、类型检查和生产构建通过。真实按钮调用会写入外部同步运行记录，因此未在自动化验证中执行，等待用户在本机确认服务可用后手动触发。
边界：只同步配置的六只重点基金，不开放代码/日期参数，不启用全市场目录、资讯、评分、回测、买卖建议或交易。

### 2026-08-27｜六只重点基金工作日增量同步

状态：已完成实现、离线测试与一次真实零变更运行。
实现：Python 增加 `FOCUSED_NAV_INCREMENTAL` 运行类型，以 Tushare 来源和基金代码读取每只基金的最新净值水位，从次日开始按基金请求最小日期窗口。首次回填缺失、窗口外/非目标代码和冲突净值失败关闭；没有新净值时不发起无效写入、以零变更成功结束。Celery Beat 默认工作日 20:00 投递任务，时间与开关可配置；Windows Worker 继续使用 `--pool=solo`，同一环境只能运行一个 Beat。
验证：Ruff 通过，pytest 23 项通过。六只基金的 Tushare 同源水位均为 2026-08-26；执行 `focused-incremental --as-of-date 2026-08-27`，运行 `9cd6bdcb-2738-4adc-8214-0e9345eec74c` 为 `SUCCEEDED`，读取、创建、更新、跳过均为 0，符合当晚尚未发布新增净值的事实。
边界：不回拉完整历史、不接入中国交易日历、不启动全市场目录、资讯、评分、回测、买卖建议或交易。若某重点基金没有同源历史基线，任务明确失败并要求先执行 `focused` 回填。

### 2026-08-27｜六只重点基金完整历史净值与走势图

状态：实现、真实数据写入和候选 Python → Java → Vue 读链路验证均已完成。
范围：用户指定 `010710.OF`、`160323.SZ`、`013275.OF`、`007832.OF`、`002112.OF`、`005312.OF` 六只重点基金。全市场目录因 15,000 条单次上限仍暂停，不能影响上述精确代码的独立同步，也不能在页面中被表述为已完成。
实现：Python `focused` 命令先完整读取六条精确目录，再逐基金按日期范围读取历史净值；任一目标缺失、混入非目标代码、空响应或达到 10,000 行安全上限均失败关闭。Alembic `20260827_04` 补齐 2015-01 至 2026-07 的月分区。FastAPI 以只读内部接口返回日期范围内的持久化净值；Java 透传为外部 API 并以基金代码和日期范围缓存；Vue 详情页新增近三月、近一年、近三年、成立以来的实际单位净值曲线与可访问表格。
真实写入：2026-08-27 执行 `focused --end-date 2026-08-26`，目录运行 `FOCUSED_CATALOG` 成功读取并更新 6 条；历史运行 `FOCUSED_NAV_HISTORY` 成功读取 11,215 条，新增 11,210 条、跳过 5 条既有业务键。历史范围为：`002112` 2015-11-16 至 2026-08-26（2,635 条）、`005312` 2018-02-07 至 2026-08-26（2,072 条）、`007832` 2019-09-26 至 2026-08-26（1,636 条）、`010710` 2021-01-12 至 2026-08-26（1,326 条）、`013275` 2021-08-19 至 2026-08-26（1,221 条）、`160323` 2016-12-26 至 2026-08-26（2,325 条）。141 个净值分区已存在，默认分区为 0 行。
候选验证：FastAPI `8001` 内部接口对 `002112` 一年范围返回 243 点（2025-08-26 至 2026-08-26）；Java `8081` 外部接口返回同样范围且 `stale=false`。停止仅候选 FastAPI 后，Java 从 Redis 返回同样 243 点、`stale=true` 和缓存时间；恢复候选 FastAPI 后健康。日期范围超过 5,000 天返回 `400/VALIDATION_ERROR`。Vue `5174` 页面实际展示最新单位净值 `4.9047`、累计净值 `5.0527`、2026-08-26 数据来源，近三月为 66 点，成立以来为 2,635 点；清单页仅显示六只重点基金，浏览器控制台无错误。
边界：历史曲线仅显示实际单位净值和来源状态；不推断缺失净值，不输出评分、买卖建议、交易或支付宝操作。Token 不进入源码、同步运行记录、日志或接口响应。

### 2026-08-26｜基金详情净值快照贯通展示

状态：已完成三服务实际联调验证。
实现：FastAPI 以“净值日期倒序、来源代码正序”选出一条最新 `nav_daily`，从同一行返回单位净值、累计净值、日期和来源，避免多来源数据错配；Java 将两个可空十进制字段纳入对外 DTO 与 Redis 陈旧读模型；Vue 在详情页显示单位净值、累计净值、净值日期、同步状态和来源，并将 `TUSHARE_PRO_FUND` 正确映射为 Tushare 数据接口。累计净值为空时展示“暂缺”，不补零或替代为单位净值。
验证：Python 15 项 pytest、Java 4 项 Maven 测试、Vue lint/类型检查/生产构建均通过。2026-08-27 实际通过 Java `8080` 与浏览器 `/funds/002112` 验证单位净值 `4.8936`、累计净值 `5.0416`、日期 `2026-08-25`、来源“Tushare 数据接口”。另以隔离 Java `8081` 实例先写入成功读缓存、再将内部 FastAPI 指向不可达地址，验证返回相同净值快照且 `stale=true`、携带 `cachedAt`；隔离实例已优雅关闭，未影响 8080 主服务。
边界：当前仅展示已落库的单日最新快照，尚未补齐连续历史数据，不绘制净值走势图，也不生成分析、买卖或交易结论。

### 2026-08-26｜Tushare 公募基金同步实现与目录上限阻断

状态：M1-02 部分完成；同步实现、真实小样本净值和幂等已验证，全市场目录仍被外部权限行数上限阻断。
实现：Python 新增 HTTPS Tushare 适配器、目录/净值同步服务、Celery 任务和受控命令；Token 仅以 `SecretStr` 从本机配置读取。Alembic v3 新增份额目录来源字段与 `source_sync_run`，按运行记录保存类型、日期、状态、统计和脱敏错误摘要。`fund_company` 仅用于管理人简称到公司全称的唯一映射；目录按市场/状态分片并以基金代码幂等写入；净值按指定日期批量请求、500 条一批落库，内容哈希相同则跳过。详情有净值时优先显示净值来源。
真实验证：三个基金接口最小只读验权均成功。目录运行时，`fund_basic(market=O,status=L)` 返回正好 15000 条，触发保护后 `CATALOG` 记录为失败，未写入任何部分目录。随后对 2026-08-25 执行两次净值同步：Tushare 返回 10,500 条，首次仅为已有 6 条目录样本写入 5 条，第二次新增/更新均为 0；第 6 条保持无净值。Alembic 当前为 `20260826_03 (head)`，Python Ruff 与 15 项 pytest 通过。
边界：当前真实净值覆盖仅限已有目录样本，不能称为全市场或实时行情；无资讯、特征、评分、回测或交易功能。同步运行和日志不保存 Token、Cookie、支付宝认证信息或原始响应。
恢复条件：须取得能完整返回场外上市目录的 Tushare 权限或供应商确认的分页契约；满足后调整本机目录上限配置、重跑全目录并验证完整性，再启动全市场净值补数。

### 2026-08-26｜六只基金目录样本与本机持仓快照初版

状态：已完成可运行的本机初版，不是全市场实时行情或多人持仓产品。
实现：通过公开资料逐一核验并以一次性 `MANUAL_PUBLISHER_VERIFIED_SAMPLE` 手工导入 6 个基金主实体/份额类别至 `fund_ai`；来源记录默认 `enabled=false`，不触发网站访问或日频同步。Java Flyway 新建 `portfolio_snapshot`、`portfolio_holding_snapshot`，从 Git 忽略的本机确认文件幂等导入 1 份、6 条截图字段，并新增 `GET /api/v1/portfolio/current`。Vue 新增 `/portfolio` 页面，从 Java 读取并汇总，不再硬编码基金行。
数据边界：截图未显示数据日期、份额、成本或交易流水，因此快照强制 `data_as_of_status=UNKNOWN`、`data_as_of_date=NULL`；页面不显示实时净值、完整总资产、成本收益率、买卖建议或交易操作。原始截图未上传或入库。
验证：Python Ruff 与 pytest 11 项通过；Java Maven 3 项测试通过并实际应用 Flyway V3、导入 6 条持仓；前端 lint、类型检查和生产构建通过。实际联调确认 Java 健康为 `UP`，基金目录 6 条、持仓快照 `available=true`/6 条；375、768、812 横屏和 1024 宽度无横向溢出。
恢复条件：Tushare 权限核验通过前，禁止把该样本称为全市场、实时或可回测数据。后续 M4 需先补齐登录后的本人数据隔离，以及用户确认的数据日期、份额和成本录入方式。

### 2026-08-26｜Tushare 本机 Token 配置与基金权限探测

状态：本机安全配置完成；基金数据权限未满足，未登记来源或写入业务数据。
实现：发现 Python 项目的 `.env` 已被 Git 跟踪，因此未向其中写入任何新凭据。新增被 `.gitignore` 忽略的 `.env.local`，`Settings` 按 `.env` 后 `.env.local` 的覆盖顺序加载，并将 `tushare_token` 作为 `SecretStr` 保存；Token 不在源码、测试断言、日志、文档或 Git 状态中出现。
验证：Ruff 通过，pytest 9 项通过；`.env.local` 已被 Git 忽略，运行时确认 Token 已配置。以 HTTPS 对 Tushare `fund_company` 发起一次只读、最小字段权限探测，HTTP 为 200、业务码为 `40203`，返回“没有接口(fund_company)访问权限”，未返回数据行。该结果符合 Tushare 公开权限表中公募基金公司、列表、净值和场内基金日线均要求至少 2000 积分的说明。
恢复条件：用户在 Tushare 账户取得相应权限后，先重新验证 `fund_company`、`fund_basic` 和 `fund_nav` 的最小只读请求；只有三项通过，才可登记 `source_registry` 并实现 M1 同步任务。Token 已在截图中暴露，账户权限完成后应在 Tushare 侧轮换 Token，并仅更新本机 `.env.local`。

### 2026-08-26｜零预算官方披露与数据 API 可行性核查

状态：完成只读核查；未登记来源、未抓取或写入数据。
已核查：证监会基金电子披露网站 `http://eid.csrc.gov.cn/fund/disclose/index.html` 可公开访问，页面标题为“公募基金-资本市场统一信息披露平台”，公开前端脚本展示基金代码校验、净值、公告和 XBRL 功能。该站点未发现面向开发者的稳定公开 API；请求 `robots.txt` 返回 WAF 拦截页面，不能将公开展示页面推定为可批量同步授权。Tushare Pro 提供 `fund_basic`、`fund_company`、`fund_nav`、`fund_daily` 等适配 M1 的 API，但官方文档列明这些接口均需要至少 2000 积分和账户 Token。

结论：官方披露网站可作为人工核验、公告/XBRL 证据和后续许可确认对象，当前不作为自动批量抓取源。若用户在本机自行配置满足权限的 Tushare Token，并确认仅按个人使用协议和接口限频使用，可将 Tushare 作为 M1 目录、份额和日净值的 POC 主数据源；Token 不得写入代码、文档、日志或提交 Git。

恢复条件：用户完成账户/权限准备并将 Token 仅写入 `C:\pythonProject\workSpace06\.env.local` 后，先执行单次只读连通性和字段核验；通过后才登记 `source_registry`、实现受限同步任务和幂等测试。若不使用 Tushare，则需取得官方披露站点或单家基金管理人的明确自动化访问许可，或将 POC 限定为用户提供的手工导入文件。

### 2026-08-25｜M2/M3 本地数据契约、受限读链路与提醒规则完成

状态：部分完成，外部数据能力保持禁用。
实现：FastAPI 已迁移事件、来源引用、特征、评分和回测运行表；数据库保证审核事件才可读取、事件哈希唯一、评分数据不足/不适用时不能写入方向/概率/置信度、回测时间切分顺序正确。Java 通过受服务身份保护的 FastAPI 内部接口读取事件/评分结果，Redis 仅缓存最后成功读页；Vue 基金详情展示来源链接、时间、可信度、关联依据、评分状态和“非交易指令”说明。新增本机单用户提醒规则 API，类型/阈值受数据库和请求校验约束，写审计但不发送外部消息。
验证：Python Ruff、格式检查、8 项 pytest、真实空库内部读取通过；Alembic 已到 `20260825_02 (head)`。Java Maven 3 项测试通过，Flyway 已到 v2；隔离端口 Java→FastAPI 联调确认事件和信号空页正常转发且 `stale=false`。Vue lint、类型检查和生产构建通过。
边界：没有登记或启用任何授权来源，因此未抓取、写入或展示真实资讯/行情，也未运行特征构建、评分、回测或提醒生成。M4 未收到用户选择，未接收个人持仓；M5 仍不实施自动交易。
后续：取得首个来源授权和 M4 录入选择后，按来源治理、数据同步、审核、特征/回测准入和本人数据隔离继续实施。

### 2026-08-24｜本地基础设施镜像漂移已修正并复验

状态：完成。
发现：Compose 文件已锁定 PostgreSQL 16.14 和 Redis 7.4.7，但运行中的旧容器实际仍为 PostgreSQL 16.11、Redis 7.4。
处理：仅对 `fund-radar-postgres-1`、`fund-radar-redis-1` 执行 Compose 强制重建，保留命名数据卷；重建后两个容器均为 `healthy`，实际二进制分别为 PostgreSQL 16.14、Redis 7.4.7，Redis 返回 `PONG`。
复验：Alembic 保持 `20260824_01 (head)`，Flyway v1 校验通过，Celery `health_probe` 返回 `UP`。

### 2026-08-24｜M0 本地基础设施与 Worker 运行态验证完成

状态：完成。
已验证：Docker Desktop 已恢复；PostgreSQL 16.14、Redis 7.4.7 容器健康，`fund_core` 和 `fund_ai` 已由首次初始化脚本创建；Celery `health_probe` 成功执行并回收结果。
约束：Windows 本地 Celery Worker 必须使用 `--pool=solo`；默认进程池会保留任务但不能执行，详见 `C:\pythonProject\workSpace06\docs_zhx\mistakes\celery-windows-worker.md`。
后续：进入 `M1-01`。真实外部数据源继续保持禁用，直到授权范围和接入凭证确认。

### 2026-08-24｜M1 数据迁移与索引验证完成

状态：完成。
已验证：Alembic 已在 `fund_ai` 创建来源、基金主实体、份额和按日期分区的净值表；Flyway 已在 `fund_core` 创建关注、信号、提醒、通知和审计表。事务内测试证明净值 UPSERT 的重复写入不产生第二行，`EXPLAIN` 命中分区索引；测试数据已回滚。
后续：实施 `M1-03`、`M1-04`。来源注册表不含真实供应商且默认禁用，不能将迁移完成误报为真实基金数据已接入。

### 2026-08-24｜M1 本机关注、缓存降级与基础市场页完成

状态：部分完成，真实数据功能继续待授权。
已验证：Java 关注列表、添加和删除均为本机单用户范围，重复添加/删除幂等且写审计；缺少基金代码返回 `400/VALIDATION_ERROR`。列表与详情在 FastAPI 正常时返回 `stale=false`，将候选服务的内部地址指向不可达端口后从 Redis 返回相同读模型并带 `stale=true`、`cachedAt`。前端已展示 Mock/陈旧提示、请求失败和游标分页；`pageSize=1` 时验证 `000001 → cursor=1 → 000002`。
边界：当前基金仍为 M0 Mock；本机单用户并非登录后的多用户隔离；Redis 密钥只从运行环境读取。旧的 IntelliJ 8080 服务未切换到本轮 JAR，页面会提示关注功能需重启后使用。
后续：取得首个数据来源的授权范围、频率、保留期限和访问配置后实施 `M1-02`，再完成真实数据筛选、图表和 M1 验收。

### 2026-08-24｜M1 来源诊断接口完成（无外部同步）

状态：部分完成。
实现：FastAPI 新增受 `X-Service-Token` 和浏览器 Origin 拦截保护的 `GET /internal/v1/sources`；返回来源代码、类型、授权范围、限频、保留期、开关及最后运行摘要，不返回凭证、原始内容或任何外部数据。
验证：无 Token 返回 403；有效服务身份返回脱敏元数据；实际 `fund_ai.source_registry` 只读诊断结果为 0 条，未登记或启用任何供应商。
后续：只有取得具体来源的授权和访问配置后，才能登记来源、实现同步任务、任务日志与异常数据查询。

### 2026-08-24｜建立跨会话实施进度看板

状态：已完成文档治理，未开始工程实施。
已确认：Java 业务接口 + FastAPI AI 服务 + 全市场基金雷达优先；不做支付宝自动交易。
当前恢复点：`M0-01`。
待决：前端栈、PostgreSQL/Redis、单用户或多用户、首个授权数据源。
恢复条件：用户确认 M0-01 的待决项后，从 `M0-02` 创建工程骨架开始。

### 2026-08-24｜M0-01 基线锁定

状态：已完成。
决定：Vue 3.5.41 + TypeScript + Vite 8.2.2 + ECharts 6.1.0；Java 使用项目内 Temurin JDK 17.0.20.1 和 Spring Boot 4.1.1；Python 使用 FastAPI + Pydantic + SQLAlchemy/Alembic + Celery；目标基础设施为 PostgreSQL 16 + Redis 7；一期单用户。
范围：M0 仅使用 Mock 数据，真实基金/资讯来源全部推迟到 M1 授权确认后。
环境事实：系统默认 Java 仍是 JDK 8；Docker Desktop 未启动。因此 JDK 17 固定在 `C:\ideaProject\workSpace12\.tools\jdk17` 供本项目 Maven 使用，基础设施仅提供 Compose 配置。
当前恢复点：`M0-02`。

### 2026-08-24｜M0-01A：确定 Python Web 框架

状态：已完成。
结论：Python 仅负责 AI、采集、回测与 Java 内部接口，业务 API、认证、关注、提醒和审计均由 Java 负责，因此选用 FastAPI 而不是 Django。
实现边界：FastAPI + Pydantic 提供内部接口；SQLAlchemy + Alembic 管理 AI 数据模型；Celery + Redis 运行采集与回测任务；不引入 Django ORM、Admin、会话或模板。
后续：创建对应 FastAPI 工程，并继续验证前端/Java 骨架。

### 2026-08-24｜M0 应用链路与 FastAPI 替换完成

状态：应用层完成；基础设施运行态验证被 Docker Desktop 未启动阻塞。
实现：Vue 3 前端仅访问 Java；Java 以 Service → 内部 Client 调用 FastAPI；FastAPI 采用服务 Token、浏览器 Origin 拦截、Pydantic、SQLAlchemy/Alembic 和 Celery 骨架。M0 列表/详情只返回明确标注为 `M0_MOCK` 的数据。
契约：新增 `docs_zhx/design/fund-radar-api-v1.md`。Java Service 层将 FastAPI snake_case DTO 显式转换为浏览器 camelCase DTO，避免内部契约泄漏。
验证：前端 lint、类型检查、构建及 Vite 启动通过；Java Maven 测试和启动通过；Python Ruff、4 项 pytest 和启动通过；真实联调验证了 Java 列表/详情、Mock 标识、404、FastAPI 403 拦截、CORS 和 TraceID 透传。
阻塞：`docker info` 无法连接 `//./pipe/dockerDesktopLinuxEngine`，因此未启动 PostgreSQL/Redis/Celery Worker，也未创建数据卷。
恢复条件：启动 Docker Desktop 后按 `infra/README.md` 执行 Compose，并验证容器健康和 Celery `health_probe`；完成后将 M0-03 标为完成，从 M1-01 开始迁移。

### 2026-08-24｜本地单环境配置

状态：已完成。
决定：项目仅供本机使用，移除开发、测试、生产环境切换和 `.env.example` 复制步骤。前端、Java、FastAPI 与基础设施均使用各自实际的 `.env`；Java 通过 `spring.config.import` 自动加载根目录 `.env`。
边界：服务 Token、数据库和 Redis 密码仍只存在于被 Git 忽略的 `.env`，不写入源代码或接口响应。
验证：前端、Python、Java 质量检查通过；Java 和 FastAPI 未设置任何临时环境变量即可启动，且 Java→FastAPI 健康检查和基金 Mock 查询均返回 200。

### 2026-08-27｜重点基金列表总数、页大小和页码跳转

状态：已完成并在本机 `8080` 生效；浏览器视觉冒烟待保留登录会话后执行。

实现：FastAPI 在页码模式执行同筛选 `COUNT` 与单页查询，返回 `page`、`page_size`、`total_count`、`total_pages`；Java 透传并转换这些字段，缓存键追加页码，保留旧游标模式；Vue 显示总条数、每页 10/20/50 条、当前/总页数、上一页/下一页和 Enter/按钮跳转。关键字和页大小改变时回到第一页，空结果显示 0 条。

验证：本机持久化目录实测 43 条、每页 20 条共 3 页、尾页 3 条，旧游标仍返回下一游标；Python `pytest tests/test_health.py -q` 13 项通过、Ruff 通过；Vue 目标 lint、类型检查和生产构建通过。修复认证对象权限集合、管理员上下文及旧角色枚举不一致后，Java 使用项目 JDK 17 执行 `mvn test`，13 项测试通过；新包在候选 `8081` 健康后接管 `8080`，`/actuator/health=UP`。

当前恢复点：使用已登录浏览器打开 `/funds`，执行 TC-21 的页大小切换、跳页和小屏视觉冒烟；接口受认证保护，终端未携带浏览器 Cookie 时返回 401 属正常边界，不能据此判断分页失败。

### 2026-08-27｜登录、前后台分区、角色权限与个人数据归属

状态：实现与自动化验证完成；待在受保护运行环境配置首位系统管理员凭据后执行浏览器会话冒烟。

实现：Java 以 BCrypt 账户密码、随机 HttpOnly 会话 Cookie、CSRF Cookie 和 Origin 校验实现登录；登录只校验既有手机号，注册页确认手机号和两次密码后创建默认基金用户并建立会话。密码规则为 6 至 20 位且不允许纯空白。V4/V5 保持历史不改，V6 将运行时登录标识收敛为大陆手机号字段；关注、提醒和持仓均从认证上下文取得 `userId`。三角色经权限矩阵控制：数据运营不能用户管理，系统管理员自动拥有当前与后续全部权限，并可受控查看指定用户确认持仓。管理员可确认迁移历史关注，迁移不会触碰提醒或持仓。

Vue 新增 Pinia 会话、Cookie/CSRF HTTP 封装和权限路由守卫；用户端与后台 `/admin` 分区并使用独立顶栏：用户端仅展示基金市场、我的关注和我的持仓，后台仅展示获授权的工作台、数据同步、用户管理和运行状态。具备后台权限的用户从右上角账户下拉框点击“后台管理”后，以新标签页打开 `/admin`，原前台页保持不变；后台账户菜单不提供“返回前台”。下拉框支持键盘状态声明、Esc 与点击外部关闭。用户管理页提供角色、状态、人工重置密码、历史关注确认迁移和指定用户持仓查看。前端只显示掩码手机号，不保存密码、完整手机号或 Cookie。

验证：Vue lint、类型检查和生产构建通过；Java 使用项目 JDK 17 执行 `mvn test`，17 项测试全部通过，覆盖未知手机号登录不建号、显式注册、重复注册和会话创建；Flyway 已验证 7 个迁移，本机 `fund_core` 处于版本 7。自动化测试不创建真实管理员、不写入真实持仓；浏览器会话冒烟仅在配置受保护的首位管理员环境变量后执行。

### 2026-08-28｜账户姓名与管理员角色展示

状态：源码实现、自动化验证和注册页视觉检查已完成；尚未执行 Java 服务切换。

实现：注册请求新增必填 `displayName`，Java 复用既有显示名校验并保存到 `user_account.display_name`，无需数据库迁移。Vue 新增姓名输入框，并将账户展示集中为“角色中文名：姓名”：内部 `SYSTEM_ADMIN` 对外显示为“管理员”，因此系统预置账户显示“管理员：系统管理员”，普通用户显示“基金用户：姓名”，角色变更后的历史账户以脱敏手机号作为兼容后缀。角色、权限矩阵、最后一名管理员保护、会话撤销和审计边界均保持原状。

验证：Vue `npm run lint`、`npm run build`（含 `vue-tsc`）通过；本地开发页实际确认姓名输入和“管理员”文案渲染。Java 使用 JDK 21 按项目 `--release 17` 执行 `mvn test`，17 项全部通过；随后以 `mvn package -DskipTests` 生成 `target/fund-core-0.1.0-SNAPSHOT.jar`。系统默认 Maven 仍指向 JDK 8，直接执行会因项目 Java 17 语法在编译前失败，故验证命令临时指定 JDK 21，未修改全局配置。

当前恢复点：需要上线本机体验时，先获得服务切换许可，再保留当前 `8080` 服务并以新包在候选端口完成 `/actuator/health` 验证，最后切换；不可直接覆盖或中断现有服务。切换后按 TC-23 对注册、角色变更和重新登录进行浏览器冒烟。

### 2026-08-28｜个人信息姓名维护

状态：源码实现、自动化验证和访问控制检查已完成；尚未执行 Java 服务切换。

实现：账户下拉菜单新增“个人信息”，受登录保护的 `/profile` 仅允许当前会话用户查看掩码手机号、角色和账户展示名，并修改自己的姓名。Java 新增 `PUT /api/v1/auth/me/profile`，从会话上下文取得 `userId`，仅更新既有 `user_account.display_name`；请求继续经过 Origin 与 CSRF 校验。手机号作为登录标识、角色和权限不提供给用户自行修改，避免越权与身份混淆；更新写入审计事件 `USER_PROFILE_UPDATED`。保存成功后，顶栏立即按“角色中文名：新姓名”刷新。

验证：Vue `npm run lint`、`npm run build`（含 `vue-tsc`）通过；本地浏览器确认注册页展示姓名字段，未登录访问 `/profile` 被路由守卫重定向到 `/login?redirect=/profile`。Java 使用 JDK 21 按项目 `--release 17` 执行 `mvn test`，18 项全部通过，覆盖带有效会话和 CSRF 的姓名更新；随后以 `mvn package -DskipTests` 成功生成候选包。

当前恢复点：取得服务切换许可后，使用包含个人信息接口的候选包先在非 `8080` 端口完成健康检查，再保留旧服务直至新服务确认可用；切换后以已登录浏览器完成 TC-24 的个人信息修改冒烟。

### 2026-08-28｜基金市场同步范围迁移与默认每页 10 条

状态：源码、迁移、离线测试和一次真实基金市场同步完成；运行中三服务切换与浏览器冒烟待执行。

实现：不新增也未发现专门存放“重点基金”的表。同步范围改由 `fund_share_class` 的 `source_code=TUSHARE_PRO_FUND AND status=ACTIVE` 动态读取，用户关注关系不参与选取。FastAPI 新增 `source_fund_code` 保存精确 Tushare 代码；存量 43 支只有六位展示代码时，首次同步先按已同步日期的批量净值响应反查，仍缺失时才查询 `.OF/.SZ/.SH` 候选目录并只接受唯一来源响应，禁止猜测后缀。全部解析成功后一次写入。Alembic `20260828_05` 将历史审计 `FOCUSED_*` 改为 `MARKET_*`，保留基金和净值数据。旧的六条人工样本初始化命令与测试已删除。

接口：Python、Java、Vue 的同步路径统一为 `market-nav-incremental`，任务错误码统一为 `MARKET_SYNC_*`；Celery 使用 `fund_ai.tushare.sync_market_nav_incremental`。基金市场的 Vue 状态、Java 对外接口和 FastAPI 内部接口默认 `pageSize=10`，可选项仍为 10、20、50。

验证：Alembic 已从 `20260827_04` 升级至 `20260828_05`，43 支市场基金均已补齐精确来源代码；真实 `market-incremental --as-of-date 2026-08-26` 成功读取并新增 5 条净值，累计 `nav_daily=67,870`，旧 `FOCUSED_*` 审计行数为 0。Python 27 项 pytest、Ruff 和编译检查通过；Vue lint、类型检查和生产构建通过；Java 使用项目 JDK 17 的 Maven 18 项测试通过。待执行：候选服务健康检查、切换和 TC-25 浏览器冒烟。运行中的旧服务未切换前，页面仍可能显示旧文案或调用旧路径。

### 2026-08-28｜个人投资决策辅助定位确认

状态：需求、总体设计与验收口径已确认；不启动新的数据源、模型、接口或用户资金行为变更。

确认内容：项目长期目标是让用户基于可追溯的基金、市场、政策和授权资讯数据，获得“继续定投复核、暂停新增复核、观察、分批降低风险复核、达到止盈条件后复核或数据不足”的可解释分析提示。大模型只在已授权、已校验、已关联的数据之上归纳证据、影响路径和不确定性；它不是自动交易、收益承诺或确定性买卖指令。本次确认不改变任何个人资金、持仓、外部定投或交易平台状态，用户的具体资金规则不写入仓库文档。

恢复点：M2/M3 实施前先完成每个外部来源的授权、刷新、保留/展示范围登记，再定义类别化数据新鲜度与模型准入阈值；随后以可解释基线、滚动回测和长期持有/定投/基准比较验证模型。任一来源、数据完整度或模型准入条件不满足时，仅实现并展示“数据不足/暂不建议行动”，不得开放方向性建议或个人盈亏结论。
