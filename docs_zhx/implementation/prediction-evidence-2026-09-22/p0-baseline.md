# P0 现场与固定对照（2026-09-22）

现场证据为同目录 `p0-site-audit.json`，由两个本机 PostgreSQL 只读事务及真实 Python HTTP 取得。44 只去重关注基金、44 条关注关系；全部有历史净值。新闻及事件表均为 0，当前 Tushare 授权不含新闻公告正文。首批扩展采用已经授权的经理任职和分红公告元数据，外部新闻全文暂未接入。

日历请求 `prediction-p0-audit-20260922` 返回 HTTP 200，覆盖 2026 年；旧二十日接口同请求号返回 `SOURCE_UPDATE_NOT_READY`。原截图无请求号/时间，现存相关本机日志未发现原错误，无法追溯原截图根因。不能把今天的来源水位错误解释成原截图日历故障。新增 `PublicDataFailure` 区分超时、HTTP 503、鉴权失败；新周期日期组件区分当前范围缺失和远期官方日期未公布。

旧一日已登记 3 个线性包，元数据 `fit_end=2024-12-30`；这是输入训练截止，并非已证实的最大标签可用日。旧二十日两个文件模型仍为 `fit_end=2024-03-31`。导入旧包保留旧目标及旧字段，缺少真实 label_end_max 的包禁止被误当成新目标候选。旧接口当前没有返回有效二十日推理，不能声称此刻页面已采用这些模型。

本轮冻结首批 `T5_V1/T20_V1/M6_V1`，新目标为从下一可执行估值日开始的现金分红再投资总回报方向。旧一日单位净值目标、旧二十日锚点、所有已存报告原样保留，独立统计。

首批比较范围为现场 44 只关注基金；产品家族使用 `fund_master_id`，无法确认同源家族时保留独立基金并披露。历史 2024/2025 已经研究使用，统一标 DEVELOPMENT_ONLY；不再称新盲测。候选为各周期总回报动量基线和固定种子的标准化逻辑回归；使用相同基金日期计划分母，失败不剔除。主指标按日期再按产品家族等权，辅报基金题准确率、两类召回、覆盖、类型/时期拆分。选择协议使用 `EXPERIMENT_SELECTION_V1`，禁止更改测试区间后继续沿用原协议身份。

新预测、失败尝试、到期答案分别新增不可变记录；同一期重试复用首份 LIVE，换模重算须带 RECOMPUTED_AFTER_EVENT，历史回放须带 HISTORICAL_REPLAY。旧报告不更新为 V2。首批验收样例：正常日/15:00/假日、自然月末、未来日历待公布、12 个净值点、缺口、分红、候选胜出/同分/未胜、故障回退、十元持仓、清仓买回、跨账号拒绝。替身与真实运行证据分别登记。
