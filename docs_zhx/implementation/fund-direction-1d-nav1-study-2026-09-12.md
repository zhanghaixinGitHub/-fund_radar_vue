# 1日浅层树增加基金自身1日幅度固定方案（2026-09-12）

用户要求先提交推送，再继续下一步训练。近期加权代码已推送Python `efb49aa`、文档Vue `533299b`，Java `70c0816`无新改动。本方案早于本轮真实基金拟合写入；结果另记主实施文档第16.10节。新阶段暂留本地。

## 1. 唯一假设和输入定义

上轮近期加权未超过原树。本轮回到原TREE11的家族/日期权重和树参数，只增加基金自身最近1个交易日的单位净值涨跌幅，候选为TREE_NAV1_12。检验原基金5/20/60日幅度之外的短期幅度是否有增量；原昨日方向简单基线保留，不把基线符号当已存在的连续幅度特征。

新增特征`unit_nav_return_1d` = `float(unit_nav[T]) / float(unit_nav[T前一交易日]) - 1`，单位为小数（0.01表示1%），不乘100、不取整。只读旧冻结历史两条净值，使用既有交易日历找前一交易日，不用自然日前一天，不用目标日U净值，不改单位净值目标为复权收益。保留两条原记录hash、日期与公告日，缺日、非正/非有限净值、重复日期或身份不明时停止，不删题或补值。取T日18:00与两条净值公告日08:00假设的最晚时刻；不得晚于旧FIT截止或旧考题U日08:00。历史公告时刻仍是假设，不冒充当时首次收到。

人口为原25只股票/混合；5704道2024考题及四季度/FINAL原504交易日FIT成员不变。原11维标准化mean/scale直接保留，新增一维只用相同FIT及原家族/日期权重拟合Scaler；不引入近期加权、类别加权、阈值搜索或其他特征。评价仍用原家族/目标日权重。

## 2. 树、预算与归档

原TREE11和LINEAR11直接复用完成包，不重新拟合。新树仍为HistGradientBoostingClassifier：深度2、4叶、100轮、学习率0.05、每叶最少100样本、L2=10、63分箱、seed=0，关闭早停和随机验证；方向阈值严格大于0.5。仅新特征维度由11变12，树形式和其他参数不变。

冻结旧树全部输入及完成记录、方案、新特征输入hash、原/扩展FIT hash、源码、日历、依赖和留存边界，后续不得改配方再覆盖。四季度＋FINAL固定5次主拟合、5次唯一复现；预约与输出独占创建，失败也保留现场，不擦除追加次数。单测可用合成数据验证12维树和数据边界，不能读取真实考题挑选配方。

新模型保存非执行数值JSON，严格检查12维、树拓扑、轮数、配方和数值。每个模型对全部FIT及对应考题与原估计器核对，最大恢复误差≤1e-12；主训练和唯一复现模型/5704条分数严格相等。旧11维代码和冻结指纹不修改，独立代码归档须实际运行verify。

## 3. 完整评价和验收

主要比较新12维树与原11维树的家族/目标日加权准确率；同时报告原始正确数、均衡准确率、上涨/非上涨召回、全部季度、各基金、LINEAR11、GROUPED_7及四种简单基线。配对区间用同目标日所有基金联合5日块重采样，2000次、seed=20260912。不以某只基金或某季度提升替代整体结果，不增加调参。

2021—2024为已多次使用的开发期，2025/2026保护期不解封；无论成绩如何，本轮不是独立验证，不自动注册/正式发布或切换在用模型。全部43只本人关注、29份9月14日真实预测、FIXED/WEEKLY、固定周更及20日研究保留；未到期标记不变。

完成现有126项相关测试与新增边界测试、Ruff/格式、归档恢复、旧研究hash和完整本人预测前后核对；不改Java/Vue业务、数据库结构或后台任务。无新来源、购买、权限、生产部署、交易或对外通知。主文档记录真实训练次数、全覆盖、历史成绩、后台和未验证未来效果；本方案不是完成声明。
