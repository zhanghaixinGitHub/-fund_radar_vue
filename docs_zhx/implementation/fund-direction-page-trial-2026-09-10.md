# 20 交易日方向模型页面试用

2026-09-10 用户要求将交接中的现有模型接入页面使用。训练优化仍暂停，研究结论保持无合格候选、未正式发布。

## 接入决定

- 入口：我的关注 → 基金详情 → 未来 20 个交易日方向，增加实验模型试用。
- 固定使用八季度运行 `0c0e06a9-725e-4b68-b813-de6ff5124b29` 最后时间窗口 `DEV_2024_Q4_FULL_V1` 保存的 D 四特征强约束模型，同时显示 A 原七特征模型作为对照。按最后时间窗口选择，不按成绩挑选；两者训练截止均为 2024-03-31，每基金 703 条训练样本。
- 只读取已有数值模型和当前本地净值进行推理。页面刷新不训练、不同步、不写正式预测表、不变更发布授权，不触发交易或通知。
- 新增独立实验接口：Vue → Java `GET /api/v1/watchlist/{fundCode}/prediction/experiment` → Python `GET /internal/v1/predictions/{fund_code}/experiment`。沿用登录、FUND_READ、WATCHLIST_SELF_READ、本人关注和内部服务 Token 校验。响应禁止共享缓存。
- 实验契约 `DIRECTION_PAGE_TRIAL_V1`：输出方向和未校准分数、模型指纹、训练截止、净值日期、信息截止、20 交易日期间、读取时间和输入指纹。正式接口的 AVAILABLE 和概率门禁保持原样。
- 只支持 001632、006730、008888。采用上一完整交易日为信息截止，输入固定到截止日前一交易日的 61 个交易日净值；完整 20 日终点来自已核验日历。缺数、日历不覆盖、来源停用或同步未就绪时不返回分数，不回退到更旧日期。
- 沿用 NAV V2 的净值下一交易日可用假设，公告字段保留追溯；现金分红仍按公告、实施和生效规则校验。默认不填补缺失，不读取 2025 保留数据或未来价格，不改变原离线研究日期保护。
- 模型文件继续留在本机忽略的研究包，按固定指纹校验；换机器须迁移对应研究包，缺失或损坏时显示不可用，不自动换模型。

## 页面解释

实验分数大于 0.5 判断上涨，否则判断非上涨（含持平）。分数不是收益率，也不是经验证的上涨概率。明确显示训练截止日、历史开发成绩 56.59%（786/1,389）、没有稳定优势及未完成独立验证；不得声称 2026 试用属于预先登记的独立测试或未来观察。

## 验收

- Python：100 项相关测试通过，覆盖 NAV V2 特征与旧研究公式一致、2026 日历、缺数、分红冲突、模型指纹、只读查询、重复输入一致、服务 Token 和浏览器 Origin 边界。原离线 `DirectionInput` 仍拒绝 2026 年，未扩开历史研究范围。
- Java：13 项相关测试通过，包含实验接口的本人关注鉴权、实际内部 HTTP 字段映射、伪发布与错误数字拒收；已构建可运行 JAR。
- Vue：50 项正式与实验协议检查通过；类型检查、ESLint、构建通过。构建仍有既有净值图 ECharts 大文件提示。
- 真实三端：复用隔离 schema 验收程序，真实模型与本地基金数据，23 项新增 HTTP 检查全部通过。三基金实验均有结果，重复试算分数与输入指纹一致；正式接口仍为 MODEL_NOT_RELEASED，概率为空。
- 浏览器：验证 008888 两模型方向一致、006730 方向分歧、390px 手机宽度单列显示，以及暂停测试 Python 后旧分数清除、恢复后重新试算成功。
- 隔离验收已结束，临时 schema 已删除，原真实账号、关注和会话表指纹核对未变。回执在 Python 本机忽略目录 `.local-runs/prediction_e2e_6c9631105f34443897d9fe138c56010e/experiment-http-receipt.json`。
- 本地运行：2026-09-10 停止原 IDE Java/Python 后端，已在原 8080/8000 端口后台启动已验证版本，继续使用原配置；5173 前端仍运行。Java 健康 UP，Python 实验接口返回 EXPERIMENTAL，5173 转接接口匿名访问为 401。后台日志及启动回执位于 Python `.local-runs/direction-page-live-20260910/`。以后若改回 IDE 运行，先停止这些端口的当前后端进程，避免端口冲突。

本次现场信息截止为 2026-09-09，输入净值截至 2026-09-08，20 交易日终点 2026-10-15：

| 基金 | D 组合模型分数 / 方向 | A 原模型分数 / 方向 |
| --- | --- | --- |
| 001632 | 0.399481 / 非上涨 | 0.428267 / 非上涨 |
| 006730 | 0.485774 / 非上涨 | 0.592857 / 上涨 |
| 008888 | 0.606275 / 上涨 | 0.816305 / 上涨 |

这些是本次试用推理结果，不是未来答案、独立测试成绩或收益承诺。未增加训练、自动同步、正式发布、通知或交易。三仓库改动尚未提交推送。
