# 基金雷达：免费已授权数据预测 V1.0 — 实施手册

> 关联需求：[我的关注基金多周期预测模块](../requirements/watchlist-prediction-module.md)
> 关联设计：[免费已授权数据预测 V1](../design/free-data-prediction-v1.md)
> 关联验收：[免费已授权数据预测 V1 测试用例](../testcase/free-data-prediction-v1.md)
> 版本：v1.13 ｜ 更新：2026-09-09 ｜ 总体变更等级：L3
> 当前状态：新版样本保存、研究训练/校准、本人关注状态接口及页面已接通并验收。正式发布仍被数据证据及评估缺口阻断；未启用产品概率或2025最终测试。当前结果、操作和限制见第21节，前文历次状态保留作参考。

## 0. 先用一句人话说清楚

这不是要做一个“能告诉用户该买还是该卖”的 AI，也不是让大语言模型猜基金。

V1.0 要完成的是一台可检查的“历史概率仪表”：面对一只**股票型基金**，在某个数据截止点，只使用当时已经公开的基金净值，回答：

> 在当时已知资料的条件下，未来20个交易日内，按约定计入现金分红并再投资后的总回报为正的概率是多少？

它不预测每天都涨，不预测具体收益率，更不承诺结果。回测没通过时，系统必须老实显示“预测暂不可用”，不能为了页面好看给一个百分比。

## 1. V1.0 已确认的边界

| 项目 | V1.0 决定 | 通俗解释 |
| --- | --- | --- |
| 首批对象 | 仅股票型基金 | 混合、债券、货币、其他基金暂不混在一起训练。 |
| 预测窗口 | 20 个有效净值日 | 大致是一个自然月；不是 20 个自然日，也不是每天都涨。 |
| 首批输入 | 仅基金自身历史净值 | 不假装知道基金持仓，也不使用新闻、公告或用户资产。 |
| 用户入口 | `我的关注 -> 单基金详情` | 公共基金详情只保留事实资料和状态，不展示完整概率。 |
| 模型名称 | `OWN_NAV_DIRECTION_BASELINE` | 表示只看净值历史的第一版，不表示“综合预测”。 |
| 不做 | 买卖建议、收益承诺、仓位建议、自动交易、页面访问时训练 | 预测是基金层面的信息辅助，不能变成个人投顾。 |

未来 5/10/20/40 个交易日的四周期目标仍保留；本次先只验证 20 日，原因是先证明一件事是否可靠，再扩成四件事。

## 2. 先认识六个词，再开始动手

| 词 | 本项目里的意思 | 可以怎么理解 |
| --- | --- | --- |
| 特征（feature） | 模型在预测时看到的输入，例如近 5/20/60 日收益、20 日波动、60 日最大回撤 | 考试前交给学生的已知资料。 |
| 标签（label） | 历史答案：从当时起再过 20 个有效净值日，净值是否更高 | 考试答案；训练时可看，真实预测时不可看。 |
| 训练（training） | 用很多历史“资料 + 答案”拟合一组模型参数 | 从大量旧题里找规律，不是背出未来答案。 |
| 回测（backtest） | 假装回到历史当时，只用当时资料做预测，再与后来真实结果比较 | 不能拿期末答案给模型做模拟考试。 |
| 概率校准（calibration） | 模型说“上涨 70%”的一组情况，长期实际也应接近 70% | 不是只看猜对几次，而是看百分比是否可信。 |
| 发布闸门（release gate） | 候选模型必须通过的数据、回测、校准和稳定性考试 | 训练完成不等于允许给用户展示。 |

## 3. 已有基础与不能误解的事实

以下是 2026-09-02 本机 `fund_ai` 只读核验的起点，后续每个阶段开始前都要重新核验，不把这组数字当永久事实：

| 项目 | 当前事实 | 对 V1.0 的含义 |
| --- | --- | --- |
| 基金份额类别 | 43 只，其中股票型 24 只 | 可只取 24 只股票型做试点。 |
| 历史净值 | 68,037 条，最早 2011-05-25，最新 2026-09-01 | 有足够历史启动样本构建实验。 |
| 股票型单基金净值点 | 每只 648–2,709 条 | 每只均超过初步的 252 训练 + 120 验证 + 120 测试 + 20 标签缓冲要求。 |
| 现有特征快照 | 48 条，均为 `SCORABLE` | 它们只代表近期快照，不是覆盖全部历史日期的训练集。 |
| 预测 / 回测 / 已发布模型 | 均为 0 | 当前没有真实概率，页面不得展示。 |
| 市场指数日线 | 0 条 | 暂不能启用“净值 + 市场状态”模型。 |

现有 `M3_STOCK_MOMENTUM_BASELINE_V1` 也要正确理解：它把最近 20 日收益用固定公式映射为概率。这是**规则基线**，不是已训练的机器学习模型。它的价值是作为比较对象：如果新模型连它都稳定胜不过，就不值得上线。

## 4. 先锁死时间规则：避免“偷看未来”

### 4.1 为什么同一个日期有两种含义

基金净值常常属于 9 月 1 日，但在 9 月 2 日才公告。9 月 1 日当天并不知道这个净值，因此不能把它放进“9 月 1 日的预测输入”。

V1.0 每条输入必须保留两个时间：

| 字段 | 含义 | 例子 |
| --- | --- | --- |
| `business_date` | 数据描述的是哪一天 | 9 月 1 日净值。 |
| `available_at` | 市场最早何时能看到它 | 9 月 2 日公告。 |

特征构建只能使用 `available_at <= cutoff_at` 的记录。页面同时展示“净值截至日”和“本次可用日期”，不能把页面访问日伪装成实时数据日。

### 4.2 V1.0 的可用性规则

| 数据 | 当前可用日期依据 | V1.0 处理 |
| --- | --- | --- |
| 净值 | `ann_date`；当前 68,037 条净值均有该字段 | 可以使用；缺失公告日的净值不进入训练或评分。 |
| 基金经理 | `ann_date` | 先保留为后续候选输入；必须先做“是否带来增益”的独立试验。 |
| 分红 | `ann_date` | 先用于净值异常解释；不是一开始就强行加入模型。 |
| 份额 / 规模 | 当前只有 `trade_date`，没有披露日期 | V1.0 不作为训练特征。不能把本次入库时间当作历史可得时间。 |
| 基金档案变更 | 当前无历史披露日期 | V1.0 不作为会变化的训练特征；只可用于基金类型筛选。 |
| 市场指数 | 尚无已登记指数日线 | V1.0 不使用。 |

### 4.3 标签的固定口径

对基金 `f` 的一条样本：

```text
起点：最后一个当时已公开的有效净值日 t
终点：该基金之后第 20 个有效净值日 t+20
future_return_20d = adjusted_nav(t+20) / adjusted_nav(t) - 1
label_up_20d = future_return_20d > 0
```

`adjusted_nav` 缺失或异常时，必须按版本化规则整段回退到同一口径的累计净值或单位净值；同一条序列不能混用口径。分红导致的机械跳变要记录为质量事件，无法解释的异常点直接拒绝训练。

## 5. 慢一点做：六个小阶段，每阶段都有停顿点

### 阶段 0｜保护现场，先不写模型

**要学什么：** 模型项目的第一步不是调参数，而是固定“问题到底是什么”。

**要做什么：**

1. 审查并固定当前三端的免费数据补齐改动；当前工作区仍有未提交的 P0 改动，不能在其上混入预测改动。
2. 记录本次试点清单：24 只股票型基金、数据源版本、数据读取时间、代码提交号。
3. 把本手册中的“20 日、只看净值、只在我的关注详情”写入需求和设计变更记录。
4. 不触发新的 Tushare 全量同步，不导入持仓、新闻或公告。

**产出：** 一份可复现的起点清单和干净的变更边界。

**停顿点 A：** 用户能复述“这次不是做买卖建议，只验证 20 日上涨概率”后，再进入阶段 1。

### 阶段 1｜用三只基金手算一条样本

**要学什么：** 特征和标签不是抽象名词，它们是一行可核对的数据。

**要做什么：** 选 3 只股票型基金、各选 2 个历史截止点，输出下表；先不训练模型。

| 基金 | 截止可用日 | 最后净值业务日 | 近 20 日收益 | 第 20 个后续净值日 | 未来收益 | 标签 |
| --- | --- | --- | ---: | --- | ---: | --- |
| 示例 | 2025-06-03 | 2025-06-02 | -2.1% | 2025-07-01 | 3.4% | 上涨（1） |

**验收：** 随机抽查一行，能从数据库还原每个净值、公告日、特征和标签；任何公告日晚于截止日的数据都没被使用。

首轮真实样本和逐行核对材料见[阶段 1 学习记录](free-data-prediction-v1-stage-1-sample-review.md)。

**停顿点 B：** 一旦发现日期口径说不清，先修正数据契约，不继续训练。

### 阶段 2｜构建“历史逐日样本集”

**要学什么：** 当前 48 条最新快照只能回答“今天怎样”，训练需要知道“历史上每一天怎样”。

**要做什么：**

1. 新增点时特征构建器，为每只股票型基金的每个合格截止点生成一条样本。
2. 每条样本固定保存：基金代码、`as_of_date`、`available_at`、净值口径、特征版本、特征 JSON、质量标识、特征哈希和来源运行 ID。
3. 每条样本单独记录 20 日标签的口径和有效终点；标签只能存在离线训练/回测数据中，不能在真实评分时读取。
4. 对同一个基金按日期顺序生成，分页/流式处理，不能一次性把全表净值读进内存。
5. 按基金均衡训练样本，避免历史更长的基金凭样本数量支配模型。

**本阶段暂用的特征：** 5/20/60 日收益、20 日波动率、60 日最大回撤、净值相对自身历史位置、连续回撤天数。每个指标都要在特征字典说明计算方式。

**验收：** 同一数据水位重跑生成相同哈希；缺公告日、净值异常、历史不足的样本明确标为 `DATA_INSUFFICIENT`，没有伪造的 0 值。

### 阶段 3｜先跑朴素对照，再训练第一个小模型

**要学什么：** “模型”不一定越复杂越好。先比较简单方法，才能知道复杂模型有没有实际价值。

**对照组：**

1. 永远预测上涨；
2. 使用历史上涨频率；
3. 延续最近 20 日涨跌；
4. 当前固定公式规则基线。

**候选模型：** 正则化逻辑回归。

逻辑回归会把多项特征组合成一个 0–1 之间的上涨概率。正则化的意思是限制参数不要因为偶然历史波动而“记住题目”。它适合首版，因为可解释、CPU 即可训练、模型文件很小，也不需要 GPU 或外部 AI 服务。

**明确不做：** 不用基金名称直接推断、不用大语言模型生成概率、不先上树模型或深度学习、不为提高一次命中率不断调参。

**需要保存的模型产物：** 特征顺序、缺失值处理、标准化参数、系数和截距、校准器、训练截止日、样本量、随机种子、依赖版本、特征/标签版本、模型哈希。它们共同组成“模型产物”，这样以后才能复现同一个百分比是怎么来的。

**停顿点 C：** 先把四种对照的结果做成一张表，再决定是否开始逻辑回归训练；不因为“看起来像 AI”而跳过对照。

### 阶段 4｜严格滚动回测：给候选模型做模拟考试

**要学什么：** 训练分数高不代表未来有效；只有未参与调参的时间段才是考试。

```text
历史训练期 -> 20 日隔离带 -> 验证期 -> 20 日隔离带 -> 独立测试期
```

20 日隔离带也叫 **purge / embargo**：因为一条样本的答案要等 20 日后才知道，分割边界附近的样本会共享未来区间；隔离带用来防止这种隐蔽泄漏。

每次滚动都遵循：

1. 只用训练期拟合标准化、模型和校准器；
2. 用验证期选择固定的一组参数；
3. 锁住参数后才读取独立测试期；
4. 向前移动窗口重复，不只挑一段表现好的行情；
5. 分别按基金、上涨/下跌/高波动阶段汇总结果。

**初步数据门槛：** 每只候选基金至少有 252 个训练、120 个验证、120 个测试和 20 个标签缓冲所需的干净净值点；当前 24 只股票型基金在点数上满足这个最低条件，但仍需经异常和可得日期过滤后复核。

### 阶段 5｜冻结发布闸门，而不是“训练到满意为止”

发布闸门是上线标准，不是训练轮数。它必须在查看候选模型独立测试期结果前写入回测配置并版本化保存。

| 闸门 | 必须回答的问题 | 不通过时怎么办 |
| --- | --- | --- |
| 时间正确性 | 是否有任何未来数据进入特征、标准化或调参？ | 直接判失败，重新构建样本。 |
| 相对增益 | 是否持续优于全部朴素对照，而不是只在一段行情胜出？ | `MODEL_NOT_RELEASED`。 |
| 概率校准 | 60%、70% 等概率组的真实上涨比例是否同方向、接近声明值？ | 重新校准或不发布。 |
| 稳定性 | 是否只由一只基金、一个市场阶段拉高成绩？ | 降级为研究结果，不发布。 |
| 覆盖与新鲜度 | 有多少基金/日期可以答题，数据是否过期？ | 显示 `DATA_INSUFFICIENT` 或 `STALE`。 |
| 可追溯性 | 能否还原模型、样本、来源运行和测试区间？ | 不能发布。 |

本阶段第一次运行只产生“学习报告”，不允许激活模型。等用户和开发共同阅读基线报告后，才冻结具体的相对增益、Brier 分数、覆盖率和样本量阈值；这些数字不能在看到独立测试答案后再改。

### 阶段 6｜通过闸门后才接入 API 和页面

只有阶段 5 通过，才做以下工作：

```text
Python：离线生成并保存已发布结果
  -> Java：校验当前用户确实关注该基金
  -> Vue：在“我的关注 -> 单基金详情”展示一张 20 日预测卡
```

页面只读取本地已发布结果，绝不触发同步、训练、回测、发布或外部服务。首版至少支持以下状态：

| 状态 | 页面应说的人话 |
| --- | --- |
| `AVAILABLE` | 显示上涨概率、数据截至日、模型版本、仅使用净值历史的限制。 |
| `DATA_INSUFFICIENT` | 历史长度、公告日期或净值质量不足，未计算概率。 |
| `MODEL_NOT_RELEASED` | 数据可以用，但模型还没有通过历史考试。 |
| `STALE` | 曾有已发布结果，但来源数据超过时效阈值。 |
| `NOT_APPLICABLE` | 非股票型或不在 V1.0 支持范围。 |

未关注该基金的用户必须由 Java 服务端拒绝；公共详情不能绕过这个限制。Python 不接触 `user_id`、持仓成本或用户资产。

## 6. 需要新增的最小数据契约

现有 `forecast_result` 是单周期结构，不能完整表达 V1.0 的可追溯结果。后续迁移采用加性方式，旧记录不改写。

| 对象 | 最少补充内容 | 目的 |
| --- | --- | --- |
| 历史特征/训练样本 | `available_at`、标签版本、净值口径、数据质量、来源水位 | 证明当时没有偷看未来。 |
| 模型产物 | 模型版本、特征顺序、参数/校准器、训练截止、依赖和哈希 | 同一模型可重复运行。 |
| `backtest_run` | 隔离带、滚动窗口、对照结果、Brier/校准、分组稳定性、发布结论 | 审核“为什么能/不能上线”。 |
| `forecast_result` | `horizon_trading_days=20`、`model_kind`、输入覆盖、数据新鲜度、不可用原因 | 让页面如实说明结果边界。 |

实际表名、字段类型、索引和原生 PostgreSQL DDL 在开始编码前单独评审；不得用一个大 JSON 偷藏没有版本和索引的关键业务字段。

## 7. 七维治理：用工程语言保护这个小模型

| 维度 | V1.0 的做法 |
| --- | --- |
| 业务边界 | 只给基金层面的历史条件概率；不做买卖、收益、个人仓位结论。 |
| 数据 | 每条输入保存业务日期和可得日期；缺失不补 0；异常净值不静默吞掉。 |
| 性能 | 样本按基金和日期分批构建；训练/回测异步离线；读取接口只查已发布结果。 |
| 稳定性 | 同一模型训练/回测使用互斥锁、幂等运行 ID 和失败原因；失败不覆盖上一次已发布结果。 |
| 安全与权限 | 浏览器只调用 Java；Java 校验本人关注关系；Token 和模型运行参数不下发浏览器。 |
| 可观测性 | 每次运行记录数据截点、样本数、拒绝数、模型版本、回测指标和耗时；日志不记录 Token 或个人数据。 |
| 可扩展 | 股票型 V1.0 和未来混合型/市场增强版分版本、分发布；不能直接复用同一模型。 |

## 8. 每一步结束后，用户应该看懂什么

| 到哪一步 | 你应该能问的问题 | 项目应给出的证据 |
| --- | --- | --- |
| 阶段 1 | “这一行为什么算上涨？” | 净值、公告日、20 日后净值和手算标签。 |
| 阶段 2 | “模型当时看到了什么？” | 可复现特征 JSON、来源水位、缺失原因。 |
| 阶段 3 | “复杂模型真的比简单规则强吗？” | 同期对照表，不是只报一个命中率。 |
| 阶段 4 | “有没有拿未来答案作弊？” | 时间切分、20 日隔离带、独立测试期。 |
| 阶段 5 | “为什么可以给用户看？” | 冻结的闸门、校准图/表、分组稳定性和发布审核记录。 |
| 阶段 6 | “为什么这个用户能看、别人不能看？” | Java 关注关系校验、页面网络记录和降级状态。 |

## 9. 本轮不做的事情和下一次再讨论的事情

1. 不购买或抓取基金持仓、新闻、公告；没有授权就不通过“猜测”补齐。
2. 不把指数目录当作市场特征。只有白名单指数日线真实入库并证明增益后，才另立 `OWN_NAV_PLUS_MARKET_V1` 候选。
3. 不把本轮单 20 日试点伪装成四周期产品完成。
4. 不自动激活模型；发布应经过预先定义的闸门和人工审核。
5. 后续要扩展混合型、5/10/40 日、市场状态、经理/分红特征时，每一项都重新做独立回测，不继承“股票型 20 日已合格”的结论。

## 10. 下一次实际编码只做什么

下一次只做**阶段 0 和阶段 1**：固定当前三端 P0 改动、抽取 3 只基金的 6 条手算样本、输出日期和标签核对表。完成后暂停，一起看懂样本和时间边界，再决定是否进入“历史逐日样本集”。

这就是“边学习边完成”的节奏：每次只跨过一个能验证、能解释、能回退的小台阶。

## 11. 阶段2当前学习入口：HTTP样本预览（2026-09-07）

阶段0/1已完成首轮核对。现在阶段2已实现单基金历史样本纯函数及`POST /internal/v1/features/historical-nav-samples/preview`，允许通过Postman提交小批量净值、指定一个业务日查看结果。阶段2的分页读库、持久化、来源水位验证尚未完成，仍未进入模型训练。

Python仓库中的`docs_zhx/implementation/historical-nav-http-preview.md`提供逐步操作，`docs_zhx/examples/historical-nav-preview.postman_collection.json`可直接导入，`historical-nav-preview.request.json`提供可编辑的完整请求。示例为模拟数据，Token不包含在集合中。

本轮优先学习HTTP输入、特征和标签分离、样本状态；净值口径已改为逐个历史时点确定，未来标签缺失或补齐均不改变过去特征。核对完示例和TC-FDP-09后，再逐步接入真实数据读取。

2026-09-07补充：已接入单基金、单净值日的数据库只读预览，优先使用GET同一路径，传`fundCode`和`asOfDate`即可，Body留空。普通验收无需导入文件或构造净值；参见TC-FDP-10。这里只读取单条样本所需最多81条记录，全量分页生成、样本持久化和训练仍未实施。

### 当前小步：小范围批量制作练习题（2026-09-07）

现已增加`GET /internal/v1/features/historical-nav-samples/dry-run`，单基金、含首尾最多31个自然日；
`pageSize`默认10、允许1–30，是内部每批样本起点数，不是只返回一页。服务查询已有净值，逐日调用原构建器，返回全部样本及统计。
与前面的单日GET相比，本步只增加“按日期分页、补齐各起点窗口、汇总”，没有改变指标公式，也没有样本落库或模型训练。

先用`008888`、`2025-08-01`至`2025-08-31`调用。当前数据读到21份样本，每页10份处理3页，每页30份处理1页，
两次`items`应完全相同；其中`2025-08-07`的对象应与原单日GET完整JSON一致。独立请求间对比要求净值和来源运行记录未变化。
操作说明在Python仓库`docs_zhx/implementation/historical-nav-http-preview.md`第6节，验收见TC-FDP-11。

**停顿点：** 本轮看懂“样本起点日期范围”和“计算需要的前后净值范围”并不相同，再讨论后续。
不要因为看到21份样本或SCORABLE，就认为模型已经训练或预测有效；全历史离线任务、持久化及训练均未实施。

### 当前下一小步：出题前检查净值起点是否已经过时（2026-09-07）

先理解一句话：到这条净值公告的时候，如果已经有更新日期的净值公布，就不能再拿旧净值当新的预测起点。
例如008888的2025-03-31净值被记为4月22日公告，而4月已有更新净值公布；保留原始记录，但这份题目返回`STALE_NAV_AT_CUTOFF`，不生成输入和答案。
不猜改公告日、不删除数据、不偷换起点，也不把20条净值改成公告日之后20个自然日。

按下面顺序读Python代码：

1. `historical_nav_samples.py`的`_find_stale_anchor_dates`：只用净值所属日和公告日判断，反向走一遍序列，记住更新记录最早的有效公告日。
2. 同文件`_build_one_sample`：起点公告日先检查，再检查过时集合；不合适就返回明确原因，后面的指标公式不执行。
3. `historical_nav.py`的`_has_newer_announced_nav`：数据库用`EXISTS`回答“是否已有更新公告”，不加载全历史；结果经`stale_anchor_dates`交给同一个构建器。

`stale_anchor_dates`是内部检查事实，不是你需要填写的参数；`sample_rule_version`是接口新增的筛选规则版本，不参与特征，不是训练模型版本。
手工POST只能检查你提交的记录；GET能检查81条计算窗口以外的更新公告。正常样本的原有内容与哈希应保持不变。

手工调用与详细解释见Python仓库`docs_zhx/implementation/historical-nav-http-preview.md`第7节，测试见TC-FDP-12。
**本步完成后暂停：先看懂“出题日”和“旧起点”，不继续保存样本或训练。首次公告日期与历史修订追溯仍待后续核实。**

### 当前小步：先准备保存格式，不执行真实建表（2026-09-07）

用户确认采用“批次封面、题目、答案”三部分后，现只准备三张表的Python实体、Alembic迁移、等价原生PostgreSQL DDL和测试。
**当前没有执行真实数据库迁移，没有保存接口，原GET仍然只读，不会保存结果。** 看见实体里的“模型”一词是ORM数据模型，不是已经训练出的预测模型。

| 阅读位置（Python仓库） | 这一页先看懂什么 |
| --- | --- |
| `app/models/historical_nav_sample.py` | Batch是封面、Record是题目、Label是答案。先读字段中文注释，再读主键、外键、唯一约束。 |
| `docs_zhx/implementation/historical-nav-http-preview.md`第8节 | 逐字段通俗解释；重试与重算；数据库约束和未来保存服务各负责什么。 |
| `alembic/versions/20260907_13_add_historical_nav_sample_storage.py` | Alembic迁移是有版本的建表步骤，不是预测算法，也不会因导入实体自动执行。 |
| `tests/test_historical_nav_storage_schema.py` | 离线核对三表定义、33个字段注释、约束、独立答案、Decimal精度及原生SQL一致性，不连接项目数据库。 |

封面记录基金、起点范围、来源水位、规则版本与统计；题目保存当时已知条件及拒收原因；答案是后来实际结果，缺失时不写行，不用0冒充未知。
`request_key`是保存请求凭证：将来重试沿用，明确重算换新凭证并保留新旧批次。`batch_id`则是已经保存的那份练习册编号。
`LEARNING_ONLY`表示只冻结学习计算结果，不证明来源首次可得日期已修复，也不自动取得训练/发布资格。

先完成TC-FDP-13的离线验收。**本步停下：一起看懂字段与关联，再确认是否实际建表；随后另一步才实现整批保存和回滚。**
不要直接开始模型训练，也不要把准备好的原生SQL与Alembic迁移各执行一次。

## 12. 当前交付：样本保存闭环（2026-09-07）

按最新要求直接推进功能，不再以答练习题、逐段读懂代码作为继续实施的前置条件。原有模型范围、数据准入和发布门槛不因此放宽。
本轮完成了从数据库净值到持久化样本的闭环：计算、整批保存、失败回滚、重复请求复用、按批次读回。

- 本机`fund_ai`已执行Alembic `20260907_13`，新增批次、样本、答案三表；原净值和特征快照不改。
- 新增保存`POST /internal/v1/features/historical-nav-samples/batches`和查询`GET /internal/v1/features/historical-nav-samples/batches/{batch_id}`。单日预览、批量dry-run仍只读。
- 保存只传基金、日期段、页大小与UUID凭证；一只股票型、最多31个自然日。无需导入已有数据库数据或手工填写答案。
- 重试沿用凭证，重新计算才换新凭证；旧批次不会被服务更新。三表全部成功才提交，失败不留半批结果。
- 已保留真实`008888 / 2025-08-07`的1条批次、1条样本、1条答案；175项相关自动测试通过。

**调用入口：** Python仓库`docs_zhx/implementation/historical-nav-http-preview.md`第9节有可复制的请求和字段/错误解释。
在IDE或原启动终端重启Python服务后，可直接GET批次`75b8258a-7cfd-4bfc-b2e8-2b5f05d9f8b3`。当前8000旧进程尚未加载新接口，404不代表数据库没保存。
数据库已有真实样本，空表回退脚本现在应拒绝执行，不得绕过保护删数据。

**下一功能包：** 在已存样本之上准备可追溯的候选训练数据，明确多批次去重、按时间划分训练/验证/测试及标签缓冲，再做朴素基线评估和候选模型。
本轮未批量保存全历史，未训练模型，也未开放关注详情预测。现有LEARNING_ONLY样本不自动取得严格训练/发布资格；首次可得日期、历史修订和交易日历限制继续保留。

## 13. 当前交付：候选数据准备与基线验证

已按用户确认范围补存三只试点基金2022–2025年的144个月度批次，得到2922条样本、2874条答案；没有修改原始净值。
从这些批次生成固定7列X和独立y，按公告可得日划分、按标签公告时间清除跨界数据。每基金训练459条、验证220条、保留测试219条，均达到本轮固定数量门槛。

新接口为`POST /internal/v1/features/historical-nav-samples/baseline-evaluation`，只读所选批次，不补存样本、不训练或发布。
参数、字段、异常与续跑命令见Python仓库`docs_zhx/implementation/historical-nav-http-preview.md`第10节。
本机完整请求与报告在`.local-runs/nav-baseline-2022-2025-2a87b245/`，复制`evaluation.request.json`到HTTP Body即可验收，无需填写144个批次编号。
原8000进程尚未加载新路由，应在IDE或原启动终端重启Python服务；真实数据库连接的进程内HTTP已验证200、请求顺序无关和403鉴权。

本次2024验证段660条样本：永远上涨49.39%、训练上涨频率50.61%、20日趋势48.48%、固定公式48.48%。完整Brier、平衡准确率与逐基金结果见报告及TC-FDP-15。
这说明当前固定公式没有显示出超过简单历史频率对照的成绩；不能把规则分数改名为预测概率后发布。
2025测试段只准备、不展示成绩、上涨比例或样本答案；尚不是数据库权限层面的封存。本轮也不是完整滚动回测。

下一阶段可进入**学习用途的逻辑回归候选模型离线训练**，先固定特征顺序、训练处理和模型方案，再使用同一验证段比较；正式训练准入、首次可得/修订核验、滚动回测和发布闸门仍未完成。
本轮未训练候选模型，不因基线报告生成而解除`LEARNING_ONLY / MODEL_NOT_RELEASED`，不使用测试成绩反向调整方案。

## 14. 当前交付：第一个逻辑回归候选模型（2026-09-08）

**一句人话：** 现在不只是“整理题目、检查简单猜法”，而是已经让程序从历史样本中学出了一组权重，并让它对另一段历史答题。这是自己用机器学习库训练的小模型，不是调用大语言模型，也不需要GPU。

### 完成了什么

- 三只基金001632、006730、008888共用一个模型；2022–2023年1377条样本用于学习，2024年660条用于验证，2025年657条继续保留、不出成绩。
- 标准化与模型都只使用训练行；按基金均衡权重，不混入基金编号、未来答案或验证期统计。固定L2逻辑回归方案，不搜索参数；尚未增加概率校准。
- 新增内部`POST /internal/v1/features/historical-nav-samples/candidate-training`。沿用基线Body并增加`expectedDatasetHash`；指纹变化则拒绝训练。HTTP返回模型和报告，不写库、不在服务器保存文件。
- 本机CLI保存request.json、report.json、model.json和complete.json；不覆盖旧实验。模型回读后的计算结果与训练库输出交叉核验，重复请求和批次换序也保持一致。
- 没有新表、原始净值/样本修改、外部AI服务、Java/Vue预测入口或模型发布。只读事务退出后才开始拟合。

### 成绩怎么看

本次模型正确351/660，方向准确率53.18%；历史频率基线334/660、50.61%。方向多答对17条，但模型Brier为0.26914973，比频率基线0.25820037差（越小越好），所以不能据此说预测概率已可靠。
逐基金准确率：001632为48.18%、006730为59.09%、008888为52.27%；不能只看总体。模型收敛用了21次迭代，这只是算法算了多少轮，不是上线标准。

**怎么验收：** [Python接口手册第11节](C:/pythonProject/workSpace06/docs_zhx/implementation/historical-nav-http-preview.md)有完整字段解释与调用方式。
本机实验目录：[nav-candidate-6e25d583-5ab3-443c-bd65-8aa7ae2ce5d1](C:/pythonProject/workSpace06/.local-runs/nav-candidate-6e25d583-5ab3-443c-bd65-8aa7ae2ce5d1)。其中request.json可直接粘贴到HTTP Body，model.json可直接阅读权重。目录已忽略，不随Git提交。

### 下一步从哪里继续

进入**训练期内部的时间隔离校准与滚动验证设计/实现**：先固定时间窗口和比较协议，再检查不同历史阶段的稳定性；校准只能使用训练段内部另留出的时间块，不利用2024验证答案拟合，更不能偷看2025测试成绩。
校准是检验并调整“报出的分数有多可信”，不是保证提高命中率。正式历史数据准入、概率可靠性、滚动回测和发布闸门依然需要后续证据；本轮维持`LEARNING_ONLY / training_eligible=false / MODEL_NOT_RELEASED`。

## 15. 当前交付：校准与滚动研究验证

**一句人话：** 现在能让模型在更早的历史上学习、用另一段历史调整分数，再到更晚的历史考试；已经真实跑了三轮。结论不是“校准好了”，而是“这套方法没有稳定变好，暂不能上线”。

### 完成的功能

- 事先固定2023第三季度、第四季度的两轮TRAIN内部滚动考试，再做2024固定验证。2024不参与拟合，2025未算成绩；没有改变既有三基金或扩大历史范围。
- 每轮分别用FIT训练基础模型、CALIBRATION训练一维分数映射，EXAM只考试；每段都按实际可得日期剔除跨界答案。基础训练每基金252条最低门槛不降低。
- 校准前后采用同一基础模型比较，返回准确率、平衡准确率、Brier、固定五档“报出的分数对比实际上涨比例”和ECE；报告保留空档/样本少/反向映射等风险，不自动选赢家。
- 新增内部`POST /internal/v1/features/historical-nav-samples/calibration-evaluation`，Body直接沿用上一阶段候选训练请求。HTTP只读数据并计算，CLI另存完整JSON报告与组合模型，不覆盖旧实验。

### 三次考试的结果

| 时段 | 校准前后Brier（越小越好） | 结论 |
| --- | --- | --- |
| 2023第三季度 | 0.2566 → 0.2580 | 略变差，分箱偏差也变差。 |
| 2023第四季度 | 0.3034 → 0.2146 | 此轮变好，但校准映射出现负斜率风险。 |
| 2024年 | 0.2751 → 0.3367 | 明显变差，ECE也从0.1320增至0.2629。 |

2024本轮校准前后准确率为51.82%→51.21%。这不是把上一阶段53.18%的原模型覆盖掉：本轮为校准预留了历史，基础模型只用1005条训练行，另312条专门校准；上一轮1377条训练的模型、报告和完整重跑结果保持不变。
只有一轮分数偏差改善，不能挑它代表整体。负斜率意味着基础模型报高分时，校准器反而倾向报低分，这是需要进一步调查的信号，不是已经查明某项数据错误。

**验收入口：** [Python接口手册第12节](C:/pythonProject/workSpace06/docs_zhx/implementation/historical-nav-http-preview.md)包含字段说明、三轮结果及可直接复制的[请求文件](C:/pythonProject/workSpace06/.local-runs/nav-calibration-f0f9f709-b82b-4af1-8326-f09b11abac50/request.json)。完整报告和models.json保存在同一本机实验目录，不随Git提交。

### 下一步从哪里继续

先做**失败归因和历史数据准入核验**：区分是时间段变化、样本代表性、指标有效性还是数据口径问题，再决定是否补充历史/基金或调整训练方案。上述原因目前是待验证方向，不是已经证实的根因。
不因某轮成绩不好就不断换参数、反复挑验证窗口；不提前查看2025测试。当前只是两轮内部滚动研究加2024固定验收，不冒充覆盖多种行情的完整发布回测；历史修订/首次可得、严格交易日历和发布闸门仍未完成。

## 16. 当前交付：失败复盘和历史数据准入核验

详细、通俗的证据和复现方式见[为什么校准后没有变好](free-data-prediction-v1-diagnostics.md)。

- 新增只读诊断脚本，复用保存的三组模型，不重新训练。拆出逐基金、阶段、2024季度成绩，检查七项输入的均值/范围变化，同时核验当前原始净值与旧样本的一致性。
- 2024窗口的校准段上涨比例29.49%，2024实际49.39%；校准后只判10/660条上涨，三个基金、四个季度的Brier都恶化。存在明显的分数偏向和映射关系变化，但尚不能锁定唯一根因。
- 2037条已纳入训练/验证样本的七项输入、方向与可得日期可由当前净值重现；不能因此证明历史首次版本正确。
- 每基金存在五个周末净值日期，240条训练/验证样本的未来窗口含周末净值；当前20条记录不等于严格20交易日。累计/复权口径在1个同端点窗口给出相反方向，历史版本与分红口径也未取得正式准入。
- 没有新HTTP、模型调参、数据同步、样本重存、表结构或Java/Vue变更。旧数据和实验文件未改，2025不评分，MODEL_NOT_RELEASED保持。

**下一步：先核准交易日计数和净值口径，再做新版样本。** 明确日历来源、期末非交易日记录与缺数处理，以及起算点和净值含义；新规则必须单独版本化、保留旧批次。当前不扩大基金池、不自动接入外部日历、不用2024反复调参。历史首次版本仍需证据，不能只靠补建一个字段就追认旧历史可用。

## 17. 当前交付：把交易日和截止日数清楚

人话说：**先给出题程序一把独立的日期尺子。** 以前按数据库里后面20条净值记录数；遇到周末也有净值，日期就可能数错。现在先按交易所日历列出必须对应的日期，再检查数据库有没有这些日期，不能缺一天就用后一天顶上。

### 已实现的效果

- 新增“交易日窗口预览”，直接传基金和信息截止日；不需要导入净值、不填写Body。
- 区分“截至哪天知道的信息”与“最近已公告净值属于哪天”；只用截止时已公告的日期选起点。
- 列出准确的历史61个交易日、未来20个交易日，以及缺数、公告异常和不参与计数的周末/节假日记录。
- 日历按官方沪深休市事实固定保存为独立版本，覆盖2021–2025；不会自动猜范围外的日期。它不代表基金申赎或境外市场日历。

### 你可以这样调用

```text
GET http://127.0.0.1:8000/internal/v1/features/historical-nav-samples/trading-window-preview?fundCode=008888&cutoffDate=2025-08-08
```

Headers仍填已有`X-Service-Token`，Body留空。**新参数叫`cutoffDate`，不是旧`asOfDate`。** 验收时8000未监听；需在Python仓启动加载新代码的服务后才能调用，启动方法和逐字段解释见[HTTP验收手册第14节](C:/pythonProject/workSpace06/docs_zhx/implementation/historical-nav-http-preview.md#14-交易日窗口预览先确定日期不计算涨跌)。本轮用临时18013端口验证后关闭，没有重启你的其他进程。

以下是008888真实日期元数据结果，不是预测涨跌结果：

| 你传的cutoffDate | 已公告起点净值日 | 从起点按旧20条记录数 | 从起点按20个交易日数 | 新规则：从截止日之后数20交易日 |
| --- | --- | --- | --- | --- |
| 2025-08-08 | 2025-08-07 | 2025-09-04 | 2025-09-04 | 2025-09-05 |
| 2024-03-07 | 2024-03-06 | 2024-04-02 | 2024-04-03 | 2024-04-08 |

第二行分别说明两件事：旧记录计数包含了3月31日周末净值；把起算点改为信息截止日以后，又跨过清明休市。因此终点不同有明确原因，不是算法随机变化。

`WINDOW_DATES_COMPLETE`只表示这些日期和公告信息齐了。`history_dates`是以后计算输入所需的日期；`future_dates`是以后核对答案所需的日期。现在既没取净值数值，也没算收益，所以`feature_generated`、`label_generated`和`training_eligible`仍为false。

### 实际验收结果

- 三只基金在2021–2024各有974条原始净值：969个交易日均存在，另外5条为周末记录。已按官方日期核对，没有删除原始记录。
- 六组真实窗口（3只基金×2个截止日）日期完整；7次真实服务读取共35条SQL，全为SET/SELECT，净值查询只取两个日期字段。
- 新增50项日期/HTTP/仓储测试；相关离线测试共382项，真实PostgreSQL隔离回归19项，共401项通过。实际网络HTTP成功200，鉴权403、错误参数422、范围不足409均验收；重复结果一致。
- 原三表仍为145批次、2923样本、2875答案，原始净值和旧候选/校准/诊断产物内容指纹未变。2025只做日期检查，没有做最终模型考试。
- 详细可复查结果保存在Python本机忽略目录`.local-runs/nav-trading-window-0147bbd8-f375-40a3-aabc-fc1bea4a3955/report.json`，不随Git提交。

### 下一步是什么

**把“用哪个净值算涨跌”核准，再制作独立版本的新样本。** 日期尺子已经能用，但累计净值与复权净值不一定表达同一种收益，不能只因为字段齐全就随意挑一种。下一步要明确分红/复权计算含义和缺失策略，然后让新版构建器使用本步日期；保留旧批次用于对照，不覆盖旧实验。历史首次可得版本依然是单独的准入缺口，不能靠日期齐全就宣布已解决。本轮没有重训或发布预测，也没有提交和推送。

## 18. 当前交付：净值口径审计，找到不能直接换字段的原因

### 先说结论

**三个“净值”不能当成同一种东西随意替换。** 原定先核准复权净值再做新版样本，但实际复算发现来源复权与现金分红算式有区别，因此没有把新版标签仓促接进训练。此次已交付可重复运行的审计接口，不是又训练了一次模型。

本轮开始前已提交并推送此前代码：Python `454c644`、文档 `c88c022`，均已核对远端main一致。无关的优化候选文档保留在工作区；本节描述的是推送后继续实现的工作，未再次提交/推送。

### 为什么需要区分

006730在2023-06-21分红，数据库记录：前一天单位净值1.4188，当天1.2235，每份现金分红0.1676。

| 算法 | 结果 | 含义 |
| --- | --- | --- |
| 1.2235 / 1.4188 - 1 | -13.7652% | 只看单位净值，漏掉了收到的现金。 |
| (1.2235 + 0.1676) / 1.4188 - 1 | -1.9524% | 明确把这次现金算入当天持有回报。 |
| 来源复权净值当日 / 前日 - 1 | -2.2139% | 来源复权序列的变化，并不等于上一行。 |

差约0.2615个百分点，不是把小数多保留几位就能消除。三次实施分红日都发现了此类差异。[供应商文档](https://tushare.pro/document/2?doc_id=119)只给出了复权字段名称，没有该字段完整算法；不能把“复权”三个字直接解释成我们想要的现金再投收益。

### 已实现什么

新增GET，Headers仍是已有`X-Service-Token`，Body留空：

```text
GET http://127.0.0.1:8000/internal/v1/features/historical-nav-samples/nav-basis-audit?fundCode=006730&startDate=2023-01-01&endDate=2023-12-31
```

它会按准确交易日读取现有净值和分红，返回每个分红日的原始数值、三种来源净值的同端点变化、现金算式的条件性连乘结果、缺值/冲突原因和快照指纹。最多一年，只开放2022–2024，不取2025数值，不查询已存训练样本；不会自动补数据、切换口径或修改原记录。

`DIFFERENCES_FOUND`表示发现差异；`NO_LARGE_DAILY_DIFFERENCE_FOUND`仅表示现金/复权日差异没有超过1基点，不能理解为所有口径相同；`AUDIT_INCOMPLETE`表示存在缺数或事件问题。三种状态都保留`NOT_APPROVED`，不会因为HTTP200就取得训练资格。字段说明见[HTTP手册第15节](C:/pythonProject/workSpace06/docs_zhx/implementation/historical-nav-http-preview.md)。

### 实际验收到哪里

- 2021–2024三只基金各974条源记录，三种净值中的复权列均有效；累计分红字段却全部为空，不能把空值解释成“没有分红”。
- 三基金×2022/2023/2024共9组年度审计完成。006730的三次日差异为1.348241、17.982339、26.152031基点；其余年份/基金未发现超过1基点的现金/复权日差异，不表示累计净值也相等。
- 新增64项测试，相关离线446项、真实PostgreSQL隔离回归19项，共465项通过。10次真实服务读取共60条SET/SELECT，没有读取样本表；真实网络200/403/422/409、重复响应、TraceID已验证。
- 三表仍145批次、2923样本、2875答案，原净值及旧候选/校准/诊断产物指纹不变。没有新特征、新标签、模型拟合或2025考试。
- 验收用临时18014端口，完成后关闭；8000未监听，需要启动加载新代码的Python服务后调用。本地报告：`.local-runs/nav-basis-audit-f6931bd0-5860-4320-abf5-a79882f02c84/report.json`，不随Git提交。

### 下一步建议

先明确新版研究目标为**“把现金分红计入，并假设当天按净值再投资后的收益”**，再制作独立回报序列和新版本样本。这个定义能写出明确公式，不依赖猜供应商算法；但仍要处理分红事件是否完整、拆分/折算，以及历史首次可得证据。

目前没有把这个建议当成已确认的新标签契约，也没有修改旧样本。不能用2024哪种算法成绩更好来选择收益含义，更不能在资料缺失时悄悄退回旧累计净值标签。本步审计已完成，正式净值口径准入尚未通过，二者应分开理解。

## 19. 当前交付：用新规则真正制作一道“练习题”

上一节结束时只有建议；本轮你要求继续，已经按建议实现**现金分红计入、假设当天按单位净值再投资**的新研究样本。不是再次只做检查：现在可以直接调接口看到“过去的信息 + 后来的答案”。新旧规则独立编号，旧样本和模型没有被覆盖。

### 怎么调用

```text
GET http://127.0.0.1:8000/internal/v1/features/historical-nav-samples/cash-reinvestment-preview?fundCode=006730&cutoffDate=2023-06-20
```

Headers沿用`X-Service-Token`，不带Origin，Body留空。不需要导入文件或手工提交净值。只允许001632、006730、008888；截止日限2022–2024，答案跨入2025的请求也拒绝。

本次检查时8000未监听，需在Python仓库启动服务后调用：

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

实际网络验收用独立临时端口，已关闭；没有重启你的其他服务。若自己已有进程运行旧代码，需要让该进程重新加载新版。

### 这条真实例子看什么

| 返回内容 | 本次结果 | 人话含义 |
| --- | --- | --- |
| `status` | `RESEARCH_SAMPLE_READY` | 按新研究规则，这道题和答案都能算出来；不是模型发布成功。 |
| `cutoff_date` | 2023-06-20 | 假设现在站在这天结束。 |
| `anchor_nav_date` | 2023-06-19 | 6月20日截止时，最近能看到的是6月19日净值。 |
| `feature_payload` | 3月21日至6月19日，61个交易日 | 给模型看的历史输入，包含7项指标和可复算过程。 |
| `label_base_date` / `label_end_date` | 6月20日 / 7月20日 | 计算之后20个交易日收益的分母日和终点日。 |
| `offline_label.future_return_20d` | `-0.023690666143` | 后来按本研究公式算出约－2.3691%，不是预测。 |
| `offline_label.label_up_20d` | `0` | 答案不是上涨；1代表上涨，0包括持平和下跌。 |
| `offline_label.label_available_at` | 2023-07-21 | 到这一天，答案所需的已使用信息才全部公告。 |

**为什么6月20日净值还能作为答案的分母？** 因为“输入”和“答案”用途不同。输入不能偷看当时没公布的信息；答案本来就是过后核对的。这里6月20日净值不放进输入，只用于后来计算6月21日至7月20日的20段收益。如果从已经知道的6月19日一直算到7月20日，就多算了一段。

6月21日每份分红0.1676元，前一天单位净值1.4188，当天1.2235：

```text
这一天收益 = (1.2235 + 0.1676) / 1.4188 - 1 ≈ -1.9524%
```

接下来将每天的`1 + 日收益`逐日相乘，再减1，得到这20日约－2.3691%的答案；并非只看单位净值降了多少。分红只有到了实际生效日才进入收益链：6月20日这道题的历史输入没有6月21日分红；改为截止6月22日，6月21日已公告净值和分红才进入历史。后一请求的答案从6月21日开始，基准日的分红不重复计一次。

### 为什么仍写“仅研究使用”

本轮解决了**怎么算、日期怎么数、未来答案怎么和输入分开**，不代表所有数据疑点都消失。

- 来源没有某条分红记录，并不能证明真的没有分红。当前预览在没有当时可见事件时按现金0计算，属于明确假设。
- 当前数据库会更新历史数据，还不能完整恢复当年首次公告的每个版本；拆分、折算也未覆盖。当前“实施”状态不是历史状态档案。
- 所以`training_eligible=false`表示尚不具备正式训练准入资格；`model_fitted=false`表示本接口本来就不训练。两者不是同一个意思。

旧候选模型以前已经训练过，但本轮没有用新样本重训，更没有发布可供用户使用的预测概率。新样本也不能直接塞进原来的累计净值训练接口。

### 验收结果和下一步

新增80项自动化用例，相关离线526项、真实PostgreSQL隔离回归19项，共545项通过，Ruff及依赖检查通过。10组真实样本都能生成，重复网络请求与直接调用完整JSON一致；200/403/422/409、TraceID及被拒请求不读库均核对。

最终一轮实库验收12次服务读取共72条SET/SELECT，无业务写入、无样本表读取。旧三表仍145批次、2923样本、2875答案；原始净值和旧实验文件内容指纹前后完全一致。完整结果在Python忽略目录`.local-runs/nav-cash-sample-d87bc4e4-0e2b-457c-b629-0834dda861f8/report.json`，不提交真实数据包。

**下一步：让新版规则一次制作一小段日期的练习题，也就是“新版批量dry-run”。** 要求单日与批量结果一致、换页大小不变，并明确多少条输入可用、多少条答案可用、为什么拒收。先不保存、不混入旧训练器；后续再接新版本快照和研究评估。正式准入缺口继续单独列明，不靠扩大数量假装解决。本轮没有提交或推送。

字段解释与代码入口见[Python HTTP手册第16节](C:/pythonProject/workSpace06/docs_zhx/implementation/historical-nav-http-preview.md)，验收清单见TC-FDP-21。

## 20. 当前交付：一次制作一段日期的新版练习题

**以前一次看一道，现在一次看一小段日期。** 本次实现新版批量dry-run：从数据库读取现有净值和分红，按同一套现金再投规则逐日出题，返回全部样本及可用/不可用数量。dry-run就是“真的算一遍，但不保存结果”，不是模拟出假数据。

### 可以直接这样调用

```text
GET http://127.0.0.1:8000/internal/v1/features/historical-nav-samples/cash-reinvestment-dry-run?fundCode=006730&startDate=2023-06-01&endDate=2023-06-30&pageSize=7
```

Headers沿用`X-Service-Token`，不带Origin，Body留空。`startDate/endDate`都包含当天，限制本次要制作哪些截止日的题目，最多31个自然日；不限制计算所需的前后历史资料。基金和年份范围仍是三试点、2022–2024。

每个交易日出一道题，周末/节假日列入`skipped_cutoff_dates`。比如2023年6月共有20个交易日，所以返回20道题，不是30道；交易日没有净值也不会少出一道，而是保留那道题并说明哪里缺数据。

`pageSize=7`表示内部每次处理7道，20道分3页处理，但响应仍一次返回全部20道，不需要再发请求去翻下一页。每页合并查询所需净值，各页用同一份数据库快照；不是把所有单日HTTP重复调一遍。

### 实际返回与最简单的验收方法

本次006730、2023年6月结果：

| 字段 | 值 | 含义 |
| --- | --- | --- |
| `sample_count` | 20 | 总共制作20道题，包含不可用题目。 |
| `ready_count` | 20 | 本例20道题的输入、答案都算得出，不是预测对了20次。 |
| `input_available_count/label_available_count` | 20 / 20 | 历史输入可用数、后来答案可用数。 |
| `input_unavailable_count/label_unavailable_count` | 0 / 0 | 输入不可用数、输入可用但答案不可用数。 |
| `page_count` | 3 | 每页7道，内部处理了3页。 |
| `items` | 20个对象 | 每个对象就是上一步单日接口的一条完整结果。 |
| `batch_hash` | 一段固定指纹 | 对整批内容做的“内容校验码”，不计分页方式；不是数据库批次编号。 |

你可以这样验收：

1. 原请求发一次，再把`pageSize`分别改为1和30。
2. `page_count`应分别变成20和1，但`items`、可用数量和`batch_hash`都应一致；只有两个分页字段不同。
3. 在`items`里找到`cutoff_date=2023-06-20`，再单独调用第19节的006730/2023-06-20接口：**整条对象应完全一致**，不只是收益大致相同。

相同结果的前提是两次调用之间源数据、水位、日历和代码规则未变化。已有净值被同步更新时，内容指纹可能合理地改变。

### 有问题的题目会怎么显示

试传006730、`2023-12-01`至`2023-12-08`：本次真实结果有6道题、6道输入可用、1道答案可用、5道答案不可用；`label_issue_counts`中`FUTURE_CALENDAR_NOT_KNOWN_AT_CUTOFF=5`。意思是这些题的未来窗口跨年，但当时下一年交易日安排尚未公布；**不是模型预测错了，也不是预测下跌**。

问题统计数的是“受影响题数”。一道题有两天缺同一种资料，该原因只计1题；同一道题有两类问题，会分别计入两种原因，所以原因数相加不一定等于题数。

只有周末/休市日的范围正常返回`NO_TRADING_CUTOFFS`、0题0页。年份或范围参数错误422；任何一道题的未来窗口进入2025，整次409；中途数据库或计算失败503，不把前半段结果当成功返回。

### 验证结果、边界和下一步

- 新增57项测试，相关离线583项、真实PostgreSQL隔离回归19项，共602项通过，Ruff和依赖检查通过。
- 3只基金各以1/7/30三种页大小完成2023年6月试跑，样本和整批指纹一致；与60次单日真实调用逐条相同。旧单日006730/2023-06-20完整示例也与上轮保存的验收报告一致。
- 真实网络200/403/422/409、TraceID、空范围、跨年拒收及分页一致已验证；旧样本145批次/2923行/2875答案、原始净值和旧实验文件内容指纹未变。没有业务写入或2025评分。
- 本次8000未监听；网络验收使用临时57807且已停止，没有重启你的其他进程。需启动加载新代码的Python服务，启动命令仍见第19节。
- 验收报告在Python忽略目录`.local-runs/nav-cash-batch-23ea0dca-afdf-4e45-81df-260d667ef13b/report.json`，不是已保存的业务批次。本轮未提交或推送。

**下一步：给新版练习题增加保存和按批次读回。** 现在重新调用会重新计算；保存后才能固定一份明确版本的研究资料，再考虑接新版研究评估。新旧口径不能混用，分红完整性、历史首次版本等正式准入问题仍需保留；批量成功不代表可以发布预测。

全部参数、字段和检查入口见[Python HTTP手册第17节](C:/pythonProject/workSpace06/docs_zhx/implementation/historical-nav-http-preview.md)，验收见TC-FDP-22。

## 21. 当前交付：新版研究已经连到“我的关注详情”

**页面链路已经打通并验收，但模型还不能发布上涨概率。** 现在不是“只有几道练习题”，而是具备保存资料、准备训练输入、实际训练和校准、保留研究报告、检查发布条件、按本人关注权限显示状态的一条完整链路。

### 21.1 这次实际做了什么

1. 在本机`fund_ai`执行加性迁移`20260908_14`，新建4张独立表：`cash_sample_batch`（批次封面）、`cash_sample`（历史输入）、`cash_sample_label`（后来答案）、`cash_research_run`（模型和研究报告）。旧样本、旧模型、原始净值不覆盖。
2. 按既定001632、006730、008888，保存2022-01-01至2024-12-03的现金口径研究样本。末日是为了让完整20交易日答案仍留在2024；没有偷偷使用2025测试答案来提高成绩。
3. 七项历史指标单独组成X（输入），后来涨没涨单独成为y（答案）。按cutoff划分时间段，答案到分段结束时还未知的题目剔除，不随机打散。
4. 沿用先前固定的逻辑回归算法及三轮时间窗口，实际拟合基础模型、独立校准，再考试。数据不足的窗口保留缺口，不缩短条件凑一个成功。
5. Java新增本人关注读取接口，Vue新增独立预测状态卡。你点击刷新只会查询现成研究状态，不会再次训练、同步或保存样本。

这里“完整链路”指研究到页面的状态交付。**正式发布、2025独立测试、生产版实时特征/预测生成仍未启用，也没有绕过门禁的接口。** 当前研究日历只覆盖2021–2025，后续真实运行还需要核验新的交易日历和数据时效规则。

### 21.2 真实结果，不是人工演示数据

| 项目 | 结果 | 通俗解释 |
| --- | --- | --- |
| 新版批次 | 108 | 3只基金，按月保存，能够按编号读回 |
| 总题数 | 2,118 | 缺资料的题目也保留，不当作不存在 |
| 输入、答案均可计算 | 1,750 | 具备研究条件，不等于预测对了1,750次 |
| 输入不可用 / 答案不可用 | 269 / 99 | 分别保留缺口，没有填0或随意补数 |
| 正式研究记录 | 1 | `f70feb1a-129d-4482-b66d-f4e2e3a5425c`，用途仍是研究 |
| 前两轮DEV窗口 | 数量不足 | 逐基金FIT/CAL/EXAM缺口可在报告中查看 |
| 2024验证 | 已完成 | 778条拟合基础模型，272条校准，625条考试 |
| 发布状态 | 未发布 | 不产生产品可用概率，也不代表判断基金会跌 |

2024考试方向正确率为52.64%，但它**不比简单按训练期涨跌频率猜方向更好**。模型的Brier误差为0.35242478，简单频率对照为0.25321469（越小越好），因此不能说复杂模型就更可靠。ECE约0.2835，也是较明显的分箱分数与真实比例偏差，不能把校准后的分数包装成可信概率。

这些是已经观察的2024研究验证成绩，**不是2025最终独立测试成绩，也不是未来收益保证**。不因这次成绩重新调整时间窗、参数或放宽发布条件。

研究资料指纹：`7f482c3f6cb43ac00c7dc635dcaa3397c5795e25e127e5bc8b338c70fb8f1c41`。报告指纹：`db527ccca8015a4f41af2ee68608dae27ec5ab86c2627ef158d39f2f6b067795`。

### 21.3 你现在如何验收页面

启动加载本轮代码的Python、Java、Vue服务后：

1. 用你自己的账户登录，进入“我的关注”，打开已关注的008888详情。
2. 在“关注资料速览”下方看到“未来20个交易日方向”卡片，当前应为“模型未发布”，没有上涨概率或涨跌方向。
3. 展开“为什么现在没有预测数字？”，查看历史版本、分红完整性、非现金调整、最终测试及滚动窗口缺口。
4. “本地最新净值日”和“最近研究记录时间”是两种日期，不是同一件事，也都不表示生成了今日预测。本次现场分别是2026-09-04和2026-09-08 18:45:55（北京时间）。
5. 刷新状态不会重新训练。普通基金详情`/funds/008888`不显示这张卡；未关注基金不能通过直接输入URL绕过权限。
6. 债券型等基金显示“暂不适用”；未进入试点的股票型显示“研究资料不足”；服务故障显示“暂时无法读取”，其他已经加载的资料仍可阅读。

本轮使用临时测试服务18000/18080/15173、随机Java测试schema和合成账户完成实测。**测试账户不等于你的账户，也没有给你的真实账户添加关注。** 临时环境验收后清理；正常使用仍沿用你的8000/8080/5173配置，不需要复制测试账户。

已有服务若尚未加载新代码，需要正常重启。示例启动方式（各在自己的终端运行）：

- Python目录`C:\pythonProject\workSpace06`：`.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000`。
- Java目录`C:\ideaProject\workSpace12`：使用项目`.tools/jdk17/jdk-17.0.20.1+1`的JDK17运行`mvn spring-boot:run`。不是系统Java8；服务Token继续从既有本地配置读取。
- Vue目录`C:\WebStormProject\workSpace05`：`npm run dev`。

### 21.4 现在新增的接口

| 接口 | 用途 | 是否写库 |
| --- | --- | --- |
| POST `/internal/v1/features/cash-reinvestment/batches` | fundCode/startDate/endDate/pageSize/requestKey，制作并保存独立批次 | 新key写入；同key同参数读回，冲突409 |
| GET `/internal/v1/features/cash-reinvestment/batches/{batchId}` | 原样读回已保存的资料 | 否 |
| POST `/internal/v1/features/cash-reinvestment/preparation` | Body明确指定batchIds，检查七列输入和分段数量 | 否 |
| POST `/internal/v1/features/cash-reinvestment/research-runs` | batchIds、expectedDatasetHash、requestKey，固定研究并保存 | 是；重试不重新训练 |
| GET `/internal/v1/features/cash-reinvestment/research-runs/{runId}` | 读取完整研究报告和模型JSON | 否 |
| GET `/internal/v1/predictions/{fundCode}` | Python给Java的最小状态；不返回模型系数、样本、考试答案 | 否 |
| GET `/api/v1/watchlist/{fundCode}/prediction` | Java给已登录且本人关注该基金的页面 | 否；不允许共享缓存 |

Python内部接口继续要求`X-Service-Token`，拒绝浏览器Origin；前端只调用Java，不能拿到这个Token。样本保存最多31自然日，研究最多128个明确批次；不接受外部X/y、降低门槛、打开测试、强制发布等请求参数。

最简单的只读报告验收：

```text
GET http://127.0.0.1:8000/internal/v1/features/cash-reinvestment/research-runs/f70feb1a-129d-4482-b66d-f4e2e3a5425c
Header: X-Service-Token: 你当前配置的内部服务令牌
```

返回的`report.preparation`是资料检查；`report.windows`是三轮考试；`model_fitted=true`只是“确实训练过”，而`release_gate=BLOCKED`和`publication_status=MODEL_NOT_RELEASED`才说明“仍不允许发布”。两者并不矛盾。

### 21.5 接下来真正缺的是什么

不是再加一个页面按钮就能发布，而是还缺两类东西：

- **资料证据**：历史首次公开版本、分红记录完整性、拆分/折算等调整是否齐全；当前快照和“查不到分红”不能替代证明。新版日历、线上数据时效也要核验。
- **可靠性证据**：补足固定滚动窗口的有效材料，在不偷看最终测试答案的条件下改进研究方案；完整独立测试及跨窗口、逐基金对照通过后，才能实现/启用正式发布与在线预测生成。

当前不会自动采集新来源、购买数据、开放2025调参或改动你的个人资产。页面验收完成与“模型已经能可靠预测”是两件不同的事。

代码阅读顺序：Python `cash_reinvestment_storage → cash_reinvestment_research → watchlist_prediction`；Java `WatchlistPredictionController → AiPredictionClient`；Vue `WatchlistFundDetailPage → WatchlistPredictionCard`。表字段、HTTP字段和关键逻辑均已补中文说明。

## 22. 完成度复核与生成前检查（2026-09-09）

**第21节的“页面已接通”是事实，但不能把它写成“完整预测功能已经完成”。** 目前页面读取的是研究状态，并没有一份可用的线上预测结果。完整目标继续保留，未标记全部完成。

本轮补充 `POST /internal/v1/predictions/generation-check`：明确指定基金、研究编号和报告指纹，实际核验报告，再逐窗口、逐基金对照已有成绩。检查失败时，`GENERATION_BLOCKED`、`inference_executed=false`、`forecast_created=false`；不会先算一个分数再隐藏，更不会写一条伪造预测。页面原因也会明确提示“已有研究成绩尚未证明模型能稳定优于简单对照规则”。

这是“生成前检查”，**不是已实现的数值生成器**。它不读取新净值数值、不重训、不解封2025、不修改原报告，不提供强制通过参数。方向准确率差和Brier差仅说明已有研究对照情况，不是本轮看完结果后新增一套可通过的发布阈值。

### 完整目标核对

| 要求 | 当前证据与结论 |
| --- | --- |
| 新版批次保存与查询 | 已实现；108个真实批次、完整性/幂等/事务测试及实库证据见第21节。 |
| 训练准备与准入 | 已实现；输入和答案隔离、固定时间划分、明确证据缺口，未把数量够等同正式准入。 |
| 训练、校准与回测 | 固定研究代码已实现，真实2024完成；前两窗数量不足，完整滚动验证尚未通过。 |
| 严格发布门槛 | 当前失败关闭及生成前拒绝检查已实现；没有可正式发布的模型，也没有允许客户端清除缺口的接口。 |
| 后端数值预测生成及存储 | **未完成**。历史输入计算器与2026独立日历已补充，实库验证已完成（第23、24节）；还缺正式证据驱动的发布协议、模型版本绑定、生成结果持久化及失效处理。预检或输入计算不能替代完整生成器。 |
| 本人关注页面与权限 | 未发布/不适用/不足/故障状态链路已实现，上一轮完成真实三端浏览器验收。 |
| 已发布概率端到端验收 | **未完成**。没有通过闸门的真实模型，不能用测试造出的通过标记冒充现场结果。 |

生成前检查新增20项测试，本轮相关离线总计659项通过、Ruff与差异检查通过。上一轮的27项隔离PG、18项Java及浏览器证据保留其2026-09-08时点，不冒称本轮再次通过。本轮新增入口的真实HTTP403/422/数据库离线503已验证；正常实库200、新增PG用例及页面新原因复验尚未完成。

本节当时的环境阻塞（后续已恢复，见第24节）：Docker启动因临时通信文件不可访问而退出，54329没有监听。已将只含两个临时通信文件的Docker/run目录备份保留，未动数据库或数据卷；随后Docker又在Secrets Engine通信文件处失败，该凭据服务目录未改动。当时启动的进程已退出，未重置Docker或调整权限。处理范围、备份路径和原始证据见TC-FDP-24。

可安全继续的工程与需要补充的真实证据分别追踪。历史首次版本、分红/非现金调整完整性不能凭当前数据库反推；2025仍保留为最终独立测试。未通过前，页面保持“模型未发布”是正确行为，但完整目标不能因此自动划为完成。

## 23. 预测时只准备已经知道的输入（2026-09-09）

通俗地说：以前是在制作“练习题”，同时准备历史资料和后来发生的答案；实际预测当天没有未来答案。因此，本节把“整理历史资料”独立出来，供后续生成流程调用。**本节还没有让模型作答，不生成上涨概率，也没有新增一个让你手动调用的HTTP接口。**

- 历史选窗与现金再投指标改为训练、预测共用同一段代码；仍是最新已公告净值往前61个精确交易日，计算60段变化和七项指标。计算公式、小数规则及旧输入/标签版本不变。
- 只读适配器限定三试点；数据库只读取截止日以前的净值。尚未公告的价格在SQL层就置空，不先取回来再隐藏；分红仅取截至当日已知的事件。两条查询均有上限，采用同一只读一致性事务，不保存输入或预测。
- `cutoffDate`按自然日结束解释，真实适配器拒绝尚未结束的今天。它不生成标签、不需要未来20日净值，甚至不依赖未来日历才能准备历史输入。
- 新增独立日历`CN_A_SHARE_2026_V1`，由[上交所年度公告](https://www.sse.com.cn/disclosure/announcement/general/c/c_20251222_10802507.shtml)与[深交所年度公告](https://investor.szse.cn/disclosure/notice/general/t20251222_618087.html)核对后固化。按已核验休市规则得到242个开市日，调休周末不算交易日；它不是实时临时停市服务或基金申赎日历。
- 旧2021–2025日历内容及指纹不变；2026日历单独绑定指纹。新日历可用于准备当前输入，不会自动赋予旧研究模型发布资格，后续仍须核验版本兼容性。
- 2026年初不足61日历史及1日起点滞后时拒绝；不借2025数值补齐。2025数值读取保护保留，未核验的2027也不猜日期。

主要代码是Python的`cash_prediction_features`（schema/repository/service）、`trading_calendar`及已有样本构建器中提取的两个共用历史函数。所有新增字段、输入边界和关键公式路径都有中文说明。

验证层次见TC-FDP-25。该阶段的新旧完整计算结果已做80组独立对照，逐字段一致；当时输入与2026日历只使用人工资料验证，实库和页面仍因环境故障待验。后续真实验证已补齐，见第24节；不能将后来的证据冒称当时已经取得。

## 24. Docker恢复后的真实数据与页面复验（2026-09-09）

用户告知Docker已启动后，实际确认PostgreSQL/Redis容器健康、54329可用，之前的环境阻塞已解除。本轮没有再操作Docker凭据目录或运行目录备份。

### 24.1 现在确实跑通了什么

- 29项真实PostgreSQL测试通过：现金链路10项、旧保存/准备/训练19项，全部在随机隔离schema中运行并清理。包含之前待验的生成前检查及当前历史输入SQL。
- 使用真实的108批、2118个样本、1750个答案和1份冻结研究报告，三试点的生成前检查都正常返回200；每次含16组真实成绩对照和6项阻止发布的原因，未执行推理或写预测。
- 三只基金以2026-09-04或09-05为历史截止日，都能从真实数据库构造61个交易日的输入。以09-08为截止日，三者均返回DATA_INSUFFICIENT：最新已知净值仍为09-04，落后2个交易日，超过允许的1日。没有把截止日偷偷退回、补旧值或改成可用。
- 真实Java→Python的24项HTTP检查通过，另补2项非股票型/非试点检查；未登录401、未关注或跨用户403、非法代码400、报告指纹冲突409、额外force参数422都按契约拒绝。本人关注正常读取，概率和方向保持null，缓存隔离及TraceID正常。
- 真实浏览器再次验证：008888显示未发布及6条原因；001021显示不适用；007045显示研究资料不足；直接访问未关注006730被拒绝且无卡片；公开基金详情无预测卡。停掉本次测试Python后，卡片独立降级而基础资料保留；恢复服务后刷新卡片恢复。键盘展开/收起、390×844无横向溢出也通过。

本轮Java预测链路7项测试及重新打包通过，Vue类型、lint、构建通过；ECharts大包警告仍是既有构建提示，不是预测接口失败。相关Python离线724项重新通过。额外健康测试的既有断言不一致仍见TC-FDP-25，本轮未改该链路，不能因此宣称仓库全部测试无失败。

### 24.2 原数据和账户有没有被改动

没有改原始净值、研究报告、真实账户和关注关系。三试点2022-01-01至2026-09-08范围内共3414条NAV，前后指纹同为`552557f9847e2182f960f856318a3b2b`；现金表计数不变，报告指纹不变。Java真实账户、关注、权限映射及会话表的前后指纹一致。

验收使用的`prediction_e2e_bf37c42ca0684ba48ac83a8275e0c53e`测试schema及合成账户已清理，可用脚本重新创建；18000/18080/15173三个临时服务和测试浏览器已关闭，Docker数据库继续运行。脱敏回执保留在Python忽略目录`.local-runs/prediction_e2e_bf37c42ca0684ba48ac83a8275e0c53e/`，不提交日志或凭据。

### 24.3 “页面验收通过”不等于“已经有可靠预测”

现在可确认的是**研究状态到本人关注详情的链路已经通过真实验收**，以及历史输入计算能真实运行并拒绝过时数据。未发布概率路径不能冒充已发布概率路径；第22节正式发布、结果生成/存储/失效处理的未完成项继续保留。

剩余核心限制是历史首次版本、分红/非现金调整完整性等证据缺口，以及前两窗样本不足、已完成窗口未证明稳定增益。仅恢复Docker、补最近净值或再训练同一份资料，均不会自动补上这些证据。2025最终测试仍未解封，不能为了让页面出现数字而改发布状态或填一个研究分数。

本轮没有提交或推送。完整用例及可重复执行方式见TC-FDP-26。
