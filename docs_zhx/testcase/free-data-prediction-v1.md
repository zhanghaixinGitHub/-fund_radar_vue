# 基金雷达：免费已授权数据预测 V1.0 — 测试与学习用例

> 关联需求：[我的关注基金多周期预测模块](../requirements/watchlist-prediction-module.md)
> 关联设计：[免费已授权数据预测 V1](../design/free-data-prediction-v1.md)
> 关联实施：[免费已授权数据预测 V1.0 实施手册](../implementation/free-data-prediction-v1.md)
> 版本：v1.13 ｜ 更新：2026-09-08 ｜ 状态：新版单日与批量只读预览均已验收，单日/批量和分页不变通过；未接新版保存/训练，未发布。前文历次验证状态保留，当前进展见TC-FDP-22。

这些用例不是要求一次性全部执行。每完成一个实施阶段，只执行对应的一小组；任何关键用例失败，都回到上一个阶段处理，不继续发布。

## TC-FDP-01｜手算一条 20 日标签

前置条件：选择一只股票型基金和一个历史截止点；该截止点及其后至少有 20 条有效净值，起点/终点净值都有公告日。

操作步骤：

1. 记录起点净值的业务日期、公告日和统一净值口径。
2. 找到该基金之后第 20 条有效净值，记录其业务日期与净值。
3. 按 `终点净值 / 起点净值 - 1` 手算未来收益。
4. 收益大于 0 标为 `1`，否则标为 `0`。

数据库验证：

```sql
SELECT fund_code, nav_date, ann_date, unit_nav, accumulated_nav, adjusted_nav
FROM nav_daily
WHERE fund_code = :fund_code
  AND nav_date BETWEEN :start_nav_date AND :end_nav_date
ORDER BY nav_date;
```

| 验收点 | 预期结果 |
| --- | --- |
| 窗口 | 第 20 个终点按同一基金的有效净值顺序计算，不按自然日猜测。 |
| 口径 | 起点与终点使用同一净值口径；不可混合单位、累计和复权净值。 |
| 标签 | 与手工计算出的上涨/非上涨结论一致。 |

测试结果：

- [ ] 通过
- [ ] 未通过
- 未通过原因：

---

## TC-FDP-02｜公告日晚于截止日时不得进入特征

前置条件：准备一条净值业务日期早于公告日期的数据。

操作步骤：

1. 将特征截止时间设为公告日前一天。
2. 构建该时点特征快照。
3. 将截止时间推进到公告日，再次构建。

| 验收点 | 预期结果 |
| --- | --- |
| 公告日前 | 该条净值不在特征输入中。 |
| 公告日及之后 | 该条净值才可成为输入。 |
| 可追溯性 | 快照能记录业务日期、`available_at` 和来源运行。 |
| 降级 | 缺少公告日时，该点不用于 V1.0 训练/评分。 |

测试结果：

- [ ] 通过
- [ ] 未通过
- 未通过原因：

---

## TC-FDP-03｜份额和档案没有可得日期时拒绝参与 V1.0

前置条件：存在 `fund_share_snapshot` 或 `fund_profile` 记录，但没有对应披露/公开日期。

操作步骤：

1. 构建一个历史训练样本。
2. 查看该样本的输入覆盖和质量说明。

| 验收点 | 预期结果 |
| --- | --- |
| 份额/档案字段 | 不作为历史训练特征。 |
| 输入覆盖 | 明确标为“未纳入：缺少可得日期”，而不是补 0。 |
| 模型名称 | 仍为仅净值历史的基线模型。 |

测试结果：

- [ ] 通过
- [ ] 未通过
- 未通过原因：

---

## TC-FDP-04｜规则基线与逻辑回归都必须参加同一场回测

前置条件：已生成多个历史日期的股票型训练样本，并固定训练、验证、独立测试区间。

操作步骤：

1. 在训练期分别运行永远上涨、历史上涨频率、趋势延续、固定公式和逻辑回归。
2. 只使用验证期选择候选模型配置。
3. 锁定配置后读取独立测试期结果。

| 验收点 | 预期结果 |
| --- | --- |
| 公平比较 | 所有方法使用同一批样本、相同截止日和相同标签。 |
| 结果 | 同时输出方向准确率、Brier 分数、覆盖率和按基金/市场阶段分组结果。 |
| 结论 | 逻辑回归没有稳定增益时，不进入发布候选。 |

测试结果：

- [ ] 通过
- [ ] 未通过
- 未通过原因：

---

## TC-FDP-05｜20 日隔离带阻断标签重叠

前置条件：已配置训练、验证和测试边界，预测窗口固定为 20 个有效净值日。

操作步骤：

1. 检查各分割边界前后 20 个有效净值日的样本分配。
2. 人为尝试将训练边界最后 20 日样本加入验证或测试。
3. 运行回测。

| 验收点 | 预期结果 |
| --- | --- |
| 隔离 | 共享未来标签区间的边界样本被剔除或隔离。 |
| 调参边界 | 独立测试期从未参与特征选择、参数选择或校准。 |
| 审计 | `backtest_run` 记录训练、验证、测试和隔离带边界。 |

测试结果：

- [ ] 通过
- [ ] 未通过
- 未通过原因：

---

## TC-FDP-06｜未通过发布闸门时绝不返回概率

前置条件：准备一组样本不足、校准失败或未优于朴素对照的候选回测。

操作步骤：

1. 尝试将该候选模型发布为 `ACTIVE`。
2. 用已关注用户读取该基金的预测摘要。
3. 查看页面和接口返回。

| 验收点 | 预期结果 |
| --- | --- |
| 发布动作 | 被拒绝，或只保留研究/候选记录。 |
| 返回状态 | `MODEL_NOT_RELEASED`、`DATA_INSUFFICIENT` 或具体失败原因。 |
| 概率字段 | 必须为空；不得由旧规则或大模型填充。 |
| 页面文案 | 不出现“上涨 xx%”“建议买入/卖出”。 |

测试结果：

- [ ] 通过
- [ ] 未通过
- 未通过原因：

---

## TC-FDP-07｜通过发布闸门后的本人关注读取

前置条件：存在一份完成、合格、人工审核激活的股票型 20 日模型和对应评分；用户 A 关注基金 F，用户 B 不关注 F。

操作步骤：

1. 用户 A 打开 `/watchlist/{fundCode}`。
2. 用户 B 请求相同预测读取接口。
3. 打开公共基金详情并观察网络请求。

| 验收点 | 预期结果 |
| --- | --- |
| 用户 A | 能看到 20 日概率、净值截至日、模型版本、仅净值历史限制和风险提示。 |
| 用户 B | Java 返回 `403/WATCHLIST_REQUIRED`，不因基金公开而放行。 |
| 公共详情 | 不读取或展示完整预测概率。 |
| 读取边界 | 页面读取不触发同步、训练、回测、发布或外部大模型调用。 |

测试结果：

- [ ] 通过
- [ ] 未通过
- 未通过原因：

---

## TC-FDP-08｜来源陈旧或异常时安全降级

前置条件：存在一条已发布历史结果；分别模拟净值更新停滞、异常净值和 Python 服务不可用。

操作步骤：

1. 读取预测摘要。
2. 检查数据截至日、缓存时间、状态和概率字段。

| 验收点 | 预期结果 |
| --- | --- |
| 数据陈旧 | 返回 `STALE`，显著说明数据截至日；不把旧日期写成今天。 |
| 没有安全结果 | 返回受控不可用状态，不临时重算。 |
| 异常净值 | 对应样本/基金拒绝评分并保留质量原因。 |
| 旧结果保护 | 失败任务不覆盖已发布的历史结果。 |

测试结果：

- [ ] 通过
- [ ] 未通过
- 未通过原因：

---

## TC-FDP-09｜HTTP样本预览与未来数据不变性（2026-09-07）

前置：启动Python服务，配置服务Token；导入Python仓库`docs_zhx/examples/historical-nav-preview.postman_collection.json`。使用模拟数据，不读写真实数据库。

| 操作 | 预期与通过标准 |
| --- | --- |
| 原样发送集合请求 | HTTP 200；返回1条样本，过去20日收益`0.14285714`，未来20日收益`0.125`，标签1。 |
| 移除最外层`as_of_date` | 返回81条，60条历史不足、1条可用、20条标签未成熟；计数与items一致。 |
| 将目标日公告设为空 | HTTP 200，`DATA_INSUFFICIENT`和`MISSING_NAV_ANNOUNCEMENT_DATE`，无伪造输入或答案。 |
| 只保留前61条 | `LABEL_NOT_MATURED`；特征和哈希仍与完整输入中的同一目标日相同。 |
| 最后一条累计净值设为空 | 过去特征、净值口径、哈希不变；标签为空且说明缺值原因。 |
| 缺Token、错Token、带Origin | 均为403，不执行计算。 |
| 0/负数/非有限净值、重复/倒序日期、超过512条或额外个人字段 | 422，不计算不落库。 |

已验证范围：38个相关单测/接口测试通过；真实本机HTTP验证了正常返回200、未来缺值及标签补齐不影响过去特征、无Token返回403、零净值返回422。这不代表训练、真实数据批量构建或发布闸门已经通过。

## TC-FDP-10｜直接读库的GET验收（2026-09-07）

带服务Token调用`GET /internal/v1/features/historical-nav-samples/preview?fundCode=008888&asOfDate=2025-08-07`，Body为空。真实读库预期：200，过去20日收益`0.06821425`，未来20日收益约`0.1400961881`，终点2025-09-04，标签1。数据若被来源修订应重新核对，不能硬编码示例结果。

已验证：50个相关测试通过，含有界读取/来源隔离/延迟公告/目标日缺失；本机真实HTTP验证200、缺认证403、非法日期422、无净值日404。查询不写库，不调用Tushare。

## TC-FDP-11｜小范围批量样本、分页不变性与只读边界（2026-09-07）

重载新代码后带服务Token调用`GET /internal/v1/features/historical-nav-samples/dry-run`，
参数`fundCode=008888&startDate=2025-08-01&endDate=2025-08-31&pageSize=10`，Body为空。

| 操作 | 预期与通过标准 |
| --- | --- |
| 上述真实数据示例 | 200，DRY_RUN，当前数据21份样本、3个非空页，质量计数与items相符。 |
| pageSize改为1或30 | 当前数据分别21页/1页；全部items、数量和质量统计不变，不比较page_size/page_count。 |
| 逐日调用原GET | 每一份完整样本与同日GET一致，包括特征、标签、口径、状态、原因、哈希。 |
| 构造跨页迟到公告、缺公告、累计净值缺失或0值 | 批量与单日一致；不提前使用迟到数据，不跳过坏值挪动第20日，不丢弃异常样本。 |
| 起点历史不足、末尾标签未成熟 | 对应样本及原因保留，三种状态计数之和等于总数。 |
| 范围为2025-08-09至2025-08-10 | 当前数据没有净值，200、0条、0页，不偷偷找其他日期。 |
| 反向范围、超过31个自然日、pageSize为0/31/小数、额外字段 | 422，参数校验先于读库。 |
| 基金不存在、品类/来源不适用 | 分别404/409，不绕过基金来源检查。 |
| 缺Token、错Token、带Origin | 403，不读库。 |
| 第二页异常或超出页间预算 | 整次503、无部分items，退出事务、释放连接。 |
| 读取与规模 | 实际只读一致性事务；每个非空页最多2条净值SQL，窗口上限81×pageSize，输出最多31份。 |

已验证：83项相关自动测试通过；临时SQLite库覆盖分页1/7/10/30及异常数据，不修改项目数据库。
真实PostgreSQL读取008888上述范围，每页1/10/30的21份样本完全一致，21个起点逐一与单日服务比较通过；
24个真实事务核验为read-only、repeatable read。新代码通过未替换数据库依赖的进程内HTTP测试客户端验证200、403、422、
空范围0条及8月7日单日JSON一致；这不表示现有8000/8001监听进程已加载新代码，手工验收前应重载。
对比要求相同净值/公告/来源同步记录/计算版本；不据此宣称历史修订可回放、模型训练完成或预测有效。

## TC-FDP-12｜过时净值起点拒收与正常样本回归（2026-09-07）

前置：重载Python新代码，沿用服务Token。真实例子依赖当前数据库版本；异常边界使用内存测试库或自备POST，禁止为了测试修改项目数据库。

| 操作 | 预期与通过标准 |
| --- | --- |
| 单日GET：008888、2025-03-31 | 当前记录公告为2025-04-22，且已有更新净值公布；HTTP 200，原日期不变，`DATA_INSUFFICIENT / STALE_NAV_AT_CUTOFF`，指标与标签为null。 |
| 批量GET：008888、2025-03-01至2025-03-31，pageSize分别1/10/30 | items逐项一致，3月31日仍保留并与单日完整JSON一致，新原因计入`unavailable_reasons`。 |
| 正常单日：008888、2025-08-07 | 原有特征、标签及哈希不变；新增独立`sample_rule_version`，不是模型版本。 |
| 后续只有第一条已公告，第20条未公告 | 起点已过时，不能继续标成SCORABLE；后续不足20条时也优先报过时。 |
| 前20条均较晚公告，只有第21条在截止日已公告 | GET和完整POST序列均拒收；GET保持81条计算窗口，不受页边界或请求日期段限制。 |
| 更新公告与截止日相同／晚一天 | 前者算已知而拒收；后者不因该记录拒收。 |
| 更新记录来自另一基金／来源，或公告缺失／早于其净值日 | 不能作为当前基金已有更新公告的证据；仍按原日期、标签质量规则处理。 |
| 公告相隔较长，但没有更新净值公告 | 不因自然日间隔被拒收，不改变原来的第20条终点。 |
| 起点公告缺失或无效，同时有内部过时标记 | 优先报告起点公告错误，保留可解释原因。 |
| 核对边界 | 不写库、不改公告日、不换请求日期、不训练；旧原因为终点已知的样本也使用新过时原因。 |

本规则不验收“历史首次可得版本已恢复”，也不等于模型已合格。

已验证（2026-09-07）：102项相关测试通过，Ruff通过。三只基金七个固定月份的429份正常样本，
除新增规则版本字段外，原有全部内容的摘要与修改前一致；另21条季度末旧起点全部拒收，核对的原始净值、公告及更新时间未变。
真实008888的2025年3月返回21份（20份可用、1份过时），分页1/10/30内容和计数一致，3月31日与单日JSON一致。
新代码的54个真实事务确认只读、可重复读；PostgreSQL该月起点查询实际使用分区索引（本次执行4.24ms，不作为普遍性能承诺）。
进程内HTTP客户端未替换数据库依赖，验证正常/过时/批量200、无认证403及TraceID透传。
现有8000监听进程实测仍返回旧规则，须由启动它的IDE或终端重载；本轮未重启该进程。测试依赖有既有TestClient弃用警告，未扩展升级依赖。

## TC-FDP-13｜学习样本存储结构的离线验收（2026-09-07）

本步不调用新的HTTP接口、不连接项目数据库、不执行迁移。Python仓库运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_historical_nav_storage_schema.py -q
```

| 核对内容 | 本步通过标准 |
| --- | --- |
| 迁移链 | 新版本20260907_13接在20260905_12后，只有一个head，旧迁移不改。 |
| 三表对应 | 批次、题目、答案的ORM与迁移列类型/空值/默认值/注释、主外键、唯一键、检查约束和索引一致。 |
| 中文注释 | 三表和全部33个字段都有中文说明，迁移编译为相应COMMENT语句。 |
| 范围、质量与重试基础 | 声明STOCK、LEARNING_ONLY、含首尾31自然日上限、计数非负与总和；请求凭证唯一、不自动换凭证；不存分页过程字段。 |
| 题目与答案 | 题目表不含未来答案列，同批同日唯一；答案以题目编号主键/外键关联，一题至多一份；不默认生成0答案，不设置级联删除。 |
| 异常保留与数值 | 拒收行允许公告缺失或错误；状态和原因有规则；未来收益为不固定精度的Decimal/NUMERIC，声明有限数、收益符号与1/0一致和固定20区间。 |
| 原生SQL | 升级/回退副本与Alembic离线生成逐字一致；升级只建三表、索引、注释，无DML、无改旧表。 |
| 安全回退声明 | 加锁在检查前、检查在删表前，三表任一非空则拒绝；逆序删除且不CASCADE。这里只核对SQL，不冒充真实删表验收。 |
| 现有能力回归 | 原样本计算、读取、单日HTTP、批量HTTP、特征读取测试仍通过；不改变现有GET或启动中的服务。 |

后续隔离PostgreSQL验收清单（**本步未执行**）：空表升级和中文注释查验；合法样本/空批次插入；重复请求凭证、同批同日、孤儿答案、错误状态/收益/日期/数量的数据库拒绝；空表回退、有数据回退拒绝与锁冲突。
后续保存服务验收（**尚未实现**）：同请求重试复用且异参数冲突；范围、基金、来源、版本、哈希及明细统计核对；仅完整题目配完整答案；第二/三张表失败整批回滚；重算保留旧批次；真实数据库保存后查询验证。
不能把本步离线测试通过写成“样本已保存”“真实迁移已通过”或“可以开始训练”。

本次已验证：115项相关自动测试通过（13项新存储结构检查、102项原有功能回归），改动Python文件Ruff通过。
本轮未执行真实数据库迁移或保存；已有TestClient依赖弃用警告仍在，未扩展升级依赖。

## TC-FDP-14｜整批保存、重试与读回（2026-09-07）

前置：本机`fund_ai`已应用`20260907_13`；重启Python服务以加载新路由。Headers沿用服务Token、不带Origin。
接口示例和只读数据库验收SQL见Python仓库`docs_zhx/implementation/historical-nav-http-preview.md`第9节。

| 验收项 | 预期与通过标准 |
| --- | --- |
| 新UUID凭证首次POST保存 | 201、STORED、返回批次编号；真实三表数量和响应统计对应。 |
| 相同凭证重发，页大小改1/30 | 200、原编号、完整JSON相同，不再调用构建器，不重复插入。 |
| GET批次编号 | 200且与保存JSON一致；只读事务、按批次有界查询、不查原始净值。 |
| 同凭证改基金/日期/当前规则 | 409 REQUEST_KEY_CONFLICT，旧数据不变；明确重算新凭证则另存批次，旧批次仍可查。 |
| 两个相同请求并发 | 一次新建、一次复用，数据库只有一批；同凭证异范围并发则一方成功、一方409。 |
| 第二/三张表插入失败、提交前失败 | 503，三表没有半批记录；故障解除后原凭证可以正常新建。提交前异常模拟不等同于真实网络丢失提交确认。 |
| 历史不足、答案未成熟、空日期段 | 拒收/未成熟样本保留而不写答案；空范围只保存0条统计的批次。 |
| 不合法范围、UUID、pageSize、任意items/user字段 | 422，参数校验先于数据库访问；布尔/小数页大小不当整数接收。 |
| 缺/错Token或带Origin | 403，不访问数据库。 |
| 载荷混入未来答案、哈希或日期/版本/来源不符、标签无效、统计不符 | 新结果拒绝保存；已存内容发现不一致时503 STORED_BATCH_INCONSISTENT，不重算覆盖。 |
| 精度与日期 | 答案Decimal/NUMERIC保持原值；created_at输出+08:00，业务日期语义不变。 |
| 与旧接口兼容 | 同一净值/来源水位/规则下items与原预览一致；原GET/dry-run不写表、不补数据。 |

自动化验证分两层，不把模拟数据当真实净值验证：

1. 161项离线回归通过：包含46项新保存契约/HTTP测试，以及115项既有计算、读取、HTTP和三表定义检查。
2. 14项真实PostgreSQL事务测试通过：每例独立随机测试schema，采用真实迁移/INSERT/事务；样本构建使用人工数据，不写public业务表，结束清理自建测试区。默认跳过，需显式启用：

```powershell
$env:RUN_NAV_STORAGE_PG_TESTS = '1'
.\.venv\Scripts\python.exe -m pytest tests/test_historical_nav_storage_postgres.py -q
```

合计175项相关自动测试通过，改动Python文件Ruff通过。已有TestClient依赖弃用警告保留，未升级依赖。
另在隔离事务验证空表升级/回退、重复凭证/样本日期、孤儿答案、窗口越界、错误方向、NaN与非空拒绝回退；未执行专门的迁移锁竞争压测。
真实public库迁移后核验三表及33/33字段中文注释；原168张表、2310个字段定义/默认值/注释未变。

**真实数据验收：** 使用未替换数据库依赖的进程内HTTP客户端保存`008888 / 2025-08-07`。
批次`75b8258a-7cfd-4bfc-b2e8-2b5f05d9f8b3`，请求凭证`70bd8a7d-e1d4-4fbb-9e9e-d5eeadab62aa`。
首次201、换页大小重试200、GET200且完整JSON相同；同凭证改日期409、无Token403；按编号查真实库为1批次/1样本/1答案。
样本与当时单日预览完整JSON一致，源起点净值/公告未变；该批次保留供验收，没有全历史写入。
原8000监听进程仍返回404，需重载服务；上述证据证明新代码连接真实数据库可工作，不冒充旧监听进程已更新。
保存只是冻结历史计算结果，不验收模型训练、概率有效性或首次可得版本已还原。

## TC-FDP-15｜候选矩阵、时间隔离与基线验证

前置：Python已加载新代码，已有`20260907_13`三表；沿用服务Token、不带Origin。
新接口`POST /internal/v1/features/historical-nav-samples/baseline-evaluation`，完整参数说明见Python接口手册第10节。

| 用例 | 预期与通过标准 |
| --- | --- |
| 同来源/同版本的足量批次 | 200、BASELINE_EVALUATED；固定7列X，y独立；4种方法只返回验证成绩。 |
| 批次顺序变化、预览数量0/5、内部读取页大小32/7 | 数据指纹、纳入数量和成绩一致；只允许预览字段变化。 |
| 重叠相同样本 | 按基金/业务日合并，重复数可追踪；不修改原批次。 |
| 同基金同日不同标签、载荷或来源水位 | 409 CONFLICTING_SAMPLES，不能按最新或质量状态挑选。 |
| 规则/来源不同、水位缺失、已存哈希不一致 | 分别明确冲突/不一致错误；不静默过滤整批或补造来源。 |
| 按公告日划段 | 净值日等于训练截止、但公告在次日的样本不能进训练；多基金使用同一边界。 |
| 标签终点在段内但公告晚于段末 | 必须剔除；答案可得日恰好等于段末可保留，跨界样本不共享到下一段。 |
| 改验证段答案 | 训练内容指纹和训练上涨频率不变，只能影响验证成绩。 |
| 改测试段答案 | 训练指纹、训练频率、验证结果和对外样本预览不变；完整数据指纹变化；不得输出测试成绩/上涨比例/答案。 |
| 单位净值回退、质量不足、答案未成熟、范围外样本 | 有互斥剔除原因，去重数量=纳入数量+剔除数量。 |
| 任何一只基金不满足252/120/120、空批次 | 200 INSUFFICIENT_DATA及逐段缺口，baselines为空；不把多个基金样本拼成单基金门槛。 |
| 单一训练类别、单一验证类别 | 训练提示TRAIN_SINGLE_CLASS；验证平衡准确率null，不除零或伪造满分。 |
| 手算指标 | y=[1,0,1,0]、分数=[1,0.5,0,0.2]，正确3/4、平衡准确率0.75、Brier=0.3225；0.5判非上涨。 |
| 旧固定公式 | 0.5+2r、限幅0.05–0.95、四位HALF_UP保持；旧评分/回测测试继续通过，不恢复旧发布入口。 |
| 非法批次/日期/预览/额外字段，含降低门槛或includeTest | 422且不读库；无/错Token或Origin为403且不读库。 |
| 缺一个批次、超时/数据库错误 | 404或503，整次无部分报告，错误不暴露连接或Token。 |
| 35批真实隔离库读取 | SET只读事务+1次封面+2页明细，总4条SQL；不读nav_daily，不INSERT/UPDATE/DELETE，表行数不变。 |
| 试点脚本 | 固定3基金/2022–2025/144个月；不加execute或预检不足绝不写；限制本机fund_ai；同run-key续跑复用已保存批次。 |

已执行：225项离线相关测试（含57项新评估/脚本测试）与17项真实PostgreSQL存储/读取测试，共242项通过；Ruff与差异检查通过。
PostgreSQL测试显式启用`RUN_NAV_STORAGE_PG_TESTS=1`，只写随机隔离测试schema并清理，不把人工数据当真实基金成绩。既有TestClient弃用警告保留，不升级依赖。

### 用户确认范围的真实验收

- 三基金001632、006730、008888，样本业务日2022–2025；运行凭证`2a87b245-dae5-45a5-a547-d34409ad0c2b`。
- 新增144条月度批次、2922条样本、2874条独立答案，原有单日验收批次保留。仅新增样本表数据，不修改原始净值。
- 原始范围2922行的全部列内容指纹执行前后相同：`538cdbcb454670dfeb13374ac3e9566b2ab121f87371376ef7625bd9e8b4a6f0`。
- 每基金训练459、验证220、保留测试219；合计纳入2694条。剔除48条过时起点、180条跨界答案（每个分界段60），没有把剔除项从样本表删除。
- 真实数据库依赖的进程内HTTP返回200、调换批次顺序完整JSON相同、无认证403。原8000进程实测仍404，需重载，不冒充该监听进程已更新。
- 报告指纹`67e1bd1ab68226a1e8ef29cfd5f369cd3879eb96d57a2e8ce20276ca243e1e10`；测试段未输出成绩或类别比例。

| 方法 | 2024验证准确率（660条） | Brier，越低越好 |
| --- | --- | --- |
| 永远上涨 | 49.39% | 0.50606061 |
| 训练上涨频率 | 50.61% | 0.25820037 |
| 延续20日趋势 | 48.48% | 0.51515152 |
| 原固定公式分数 | 48.48% | 0.27739452 |

这些只是本次固定验证段的学习成绩，不能当未来概率、泛化保证或发布依据。当前固定公式在两个指标上均未超过历史频率对照，不能凭此上线。
完整请求、报告及原始数据指纹清单位于Python本机`.local-runs/nav-baseline-2022-2025-2a87b245/`，已忽略，不含Token、不提交个人环境运行产物。

## TC-FDP-16｜固定逻辑回归候选、只用训练段拟合与产物复现

前置：Python安装本轮固定数值依赖并加载新路由；沿用Token、不带Origin。接口、完整本机请求和字段说明见Python接口手册第11节。

| 验收项 | 预期 |
| --- | --- |
| 相同批次与已确认指纹 | 200 CANDIDATE_EVALUATED；七列、L2、C=1、lbfgs、最多1000轮，不搜索参数，未校准。 |
| dataset_hash不匹配 | 409 DATASET_HASH_MISMATCH，拟合函数不执行。 |
| 任一基金数量不足 | 200 INSUFFICIENT_DATA，model/candidate为null，保留原缺口和无模型状态。 |
| 修改验证或测试答案 | 模型均值、尺度、系数、训练指纹与模型哈希均不变；测试答案变化也不影响候选验证成绩。完整数据哈希允许变化。 |
| 修改验证输入 | 标准化和模型不变，仅验证预测可以改变；测试对象设为禁止读取仍可完成训练。 |
| 修改训练输入 | 学到的均值/尺度及模型内容可以变化。 |
| 调换批次顺序或previewSize=0 | 模型、数据指纹及成绩不变，只改变预览字段。 |
| 不等量基金训练 | 每基金总权重相同，标准化也采用该权重；常量列scale=1。不是类别重采样。 |
| 单类训练、NaN/Infinity/缺失值 | 明确拒绝，不补造样本或伪造0；未收敛或超出阶段预算不交付模型。 |
| 同进程训练忙碌 | 429 TRAINING_BUSY，不读库；失败后锁释放。限流非跨进程，阶段预算非硬中断。 |
| JSON回读 | 完整七列顺序/有限数值/正尺度/协议及内容哈希校验，纯数值公式与库预测差值不超过1e-12。 |
| 篡改系数、列顺序、尺度、协议或增加未知字段 | 拒绝回读，不执行产物中的任何代码。哈希不冒充安全签名。 |
| CLI成功 | 新目录中request/report/model/complete四文件，完成清单文件指纹全部一致，保存后模型与内存相同。 |
| CLI重复目录、磁盘失败或非本机库 | 不覆盖，不改原实验；失败无有效完成清单，不冒充保存成功。 |
| 未授权/Origin/调参/外部矩阵/路径/includeTest/坏请求 | 403或422且不训练；数据库或缺依赖错误503，响应不暴露内部细节。 |
| 真实隔离库端到端 | 人工700行通过真实存储契约保存；训练入口仅SET只读+封面SELECT+明细SELECT，拟合时连接已释放，表行数不变。 |

自动化：265项离线相关测试通过（含40项新训练/HTTP/产物/CLI用例），18项显式PostgreSQL隔离测试通过（含1项新增完整训练链路），合计283项。Ruff、pip check通过；既有Starlette/TestClient弃用提示仍在，未升级原Web依赖。
PostgreSQL使用已验证的随机测试schema隔离及清理机制，不在public业务表保存人工样本。启用命令：

```powershell
$env:RUN_NAV_STORAGE_PG_TESTS = '1'
.\.venv\Scripts\python.exe -m pytest tests/test_historical_nav_training_postgres.py tests/test_historical_nav_evaluation_postgres.py tests/test_historical_nav_storage_postgres.py -q
```

### 真实三基金验收（不是人工测试成绩）

- 沿用144个已确认批次；数据指纹`67e1bd1ab68226a1e8ef29cfd5f369cd3879eb96d57a2e8ce20276ca243e1e10`，训练1377/验证660/保留测试657。只训练固定方案，未根据结果调参。
- 候选正确351/660，准确率0.53181818、平衡准确率0.52798391、Brier 0.26914973；逐基金正确106/220、130/220、115/220。相对历史频率准确率增加约2.58个百分点，Brier反而变差0.01094936，不宣称全面优于基线或可以发布。
- 实验编号`6e25d583-5ab3-443c-bd65-8aa7ae2ce5d1`；模型哈希`05c5873aecb134651222011ce8a282e69c1812de62f4a9bf3022c96e26ebebac`，实际21次求解迭代。JSON模型回读、完成清单文件指纹核验通过。
- 新代码连接真实数据库的进程内HTTP200，重复请求/批次倒序与已存报告完整JSON一致，无Token403，错误数据指纹409。三次读库仅21条只读SQL，不查询nav_daily，不写模型或样本表。
- 独立临时`127.0.0.1:18011`网络HTTP200且与保存报告一致，无Token和Origin均403；验证后已关闭临时服务，不改用户已有服务。验收时8000未连通，不能把临时端口验证当作用户8000已部署。
- 真实运行前后三表保持145/2923/2875；三基金2022–2025的2922条原始净值全列内容MD5保持`ccba9f019c6d7f1c6f281d052d4aa34c`，这是本次前后变化检查，与旧SHA256用途不同，不混用。
- 测试段未输出分数/类别比例/答案，LEARNING_ONLY、training_eligible=false、MODEL_NOT_RELEASED保持。校准、滚动回测、历史修订及严格发布验收仍未完成。

## TC-FDP-17｜训练期独立校准与固定三窗研究验证（2026-09-08）

前置：沿用144个已保存月度批次，不重新保存样本；加载新Python代码、使用原服务Token、不带Origin。
接口为`POST /internal/v1/features/historical-nav-samples/calibration-evaluation`，Body沿用候选训练请求。
时间窗口、字段解释和完整本机文件见Python接口手册第12节。这里验收的是功能和实验可复现，不是模型效果已经达标。

| 验收项 | 预期与实际通过边界 |
| --- | --- |
| 固定协议 | 只接受2022–2025全局边界和三个预先定义窗口；不接受自定义窗口、方法、门槛、includeTest或路径。 |
| FIT / CALIBRATION / EXAM隔离 | 基础模型只用FIT，校准器只用其后的CALIBRATION，成绩只用再后面的EXAM；各段答案都必须在段末前可得。 |
| 逐基金数量与跨界答案 | 全局252/120/120不变；本窗FIT最低252、CALIBRATION最低60、季度EXAM最低40、2024最低120。报告分别列缺口和新增跨界剔除，不重复计算已全局剔除项。 |
| 修改2024/2025答案 | 三组组合模型均不变；2025答案变化也不影响任何一窗成绩。完整数据指纹允许随源数据变化。 |
| 修改考试输入/禁止访问测试对象 | 不改变对应模型拟合；把TEST设置为读取即报错，训练和校准仍完成，不生成2025预测或答案预览。 |
| 修改CALIBRATION答案 | 可改变校准参数，不改变对应基础模型。修改某DEV考试答案不改变该窗模型，后续窗口可依法使用已经成熟的历史。 |
| 成对比较 | before/after必须使用同一冻结基础模型；校准后不再用FIT+CAL重训基础模型。不能与上一轮全TRAIN模型混比。 |
| 权重与数值回读 | 基金总权重相同；校准仅拟合一维映射，固定L2/C=1；组合JSON回读与库预测差值不超过1e-12。 |
| 五档可靠性 | 手算边界、平均分数、实际上涨比例、偏差和ECE相符；空档null、少于30条提示不足；不造独立样本置信区间。 |
| 非正校准斜率 | 保留研究参数并显示NON_POSITIVE_CALIBRATION_SLOPE，不根据考试成绩事后裁剪或换方法。 |
| 缺口、单类、未收敛 | 保留失败窗口及原因，部分成功为PARTIAL_EVALUATION，全无可用窗口为NO_VALID_WINDOWS或INSUFFICIENT_DATA；不伪造成绩或模型。 |
| 指纹、忙碌和时间预算 | 错指纹409且不拟合；候选训练和校准共享同进程计算槽，忙时429；超出阶段预算不返回部分成功报告。阶段预算不是硬中断。 |
| HTTP安全 | 缺Token或带Origin返回403；坏协议422；数据库/运行错误返回受控错误，不暴露敏感连接信息。HTTP不写数据库或本机文件。 |
| CLI完整性 | 新目录保存request/report/models，回读后最后写complete清单；文件指纹、组合模型关联及内容指纹全部一致。重复目录拒绝覆盖，磁盘失败不生成有效完成清单。 |
| 旧能力回归 | 原候选模型和接口完整JSON不变，既有基线、样本预览、批量及保存测试继续通过。 |
| 真实隔离PostgreSQL | 人工1570行经真实保存契约入随机测试schema；校准读取只执行SET只读事务+封面SELECT+两页明细SELECT，共4条SQL，拟合时连接已释放，行数不变。 |

自动化已执行：303项离线相关测试通过（本轮新增38项校准测试），19项显式PostgreSQL隔离测试通过（本轮新增1项校准端到端），合计322项。Ruff、pip check、差异检查通过；既有TestClient弃用警告仍在。
PostgreSQL只在受控随机测试schema内保存人工样本并清理，不写public业务样本表。两类测试分开执行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_historical_nav_calibration.py tests/test_historical_nav_training.py tests/test_historical_nav_evaluation.py tests/test_historical_nav_storage.py tests/test_historical_nav_storage_schema.py tests/test_historical_nav_samples.py tests/test_historical_nav_repository.py tests/test_historical_nav_batch.py tests/test_historical_nav_http.py tests/test_stock_feature_snapshot.py tests/test_feature_read.py tests/test_baseline_analysis.py -q --tb=short
$env:RUN_NAV_STORAGE_PG_TESTS = '1'
.\.venv\Scripts\python.exe -m pytest tests/test_historical_nav_training_postgres.py tests/test_historical_nav_evaluation_postgres.py tests/test_historical_nav_storage_postgres.py -q --tb=short
```

### 真实三基金研究结果与只读核验

- 沿用001632、006730、008888及原数据指纹`67e1bd1ab68226a1e8ef29cfd5f369cd3879eb96d57a2e8ce20276ca243e1e10`。三窗每基金FIT/CALIBRATION/EXAM数量分别254/61/44、315/64/40、335/104/220。
- 实验编号`f0f9f709-b82b-4af1-8326-f09b11abac50`；独立保存到本机忽略目录`.local-runs/nav-calibration-<编号>/`，不覆盖上一轮实验。三模型与完成清单的文件指纹回读验证通过。
- 三轮均完成，但只有1轮Brier改善。2023第三季度Brier为0.25659806→0.25797493；第四季度0.30338054→0.21464207；2024为0.27513393→0.33669545。不能把完成状态解释为通过发布门槛。
- 2024成对准确率51.82%→51.21%，第四季度和2024校准斜率均为负。没有据此调整算法、窗口或参数；目前只能判定本方案未稳定改善，不能由此断言具体根因。
- 新代码连接真实数据库的进程内HTTP返回200；重排批次与已保存报告完整JSON一致；错误指纹409、无Token403。原候选接口仍与上一轮保存报告完整JSON一致。四次实际读库共28条SET/SELECT，不查询nav_daily，不执行数据写入。
- 独立临时`127.0.0.1:18012`网络HTTP返回200且与保存报告完整JSON一致，无Token/Origin为403、错指纹409。验收后关闭了自建临时服务，确认端口释放；未重启或修改用户已有进程。验收时8000未连通，调用用户端口前仍需启动/重载Python服务。
- 真实运行前后三表数量保持145/2923/2875；试点范围2922条原始净值全列内容MD5保持`ccba9f019c6d7f1c6f281d052d4aa34c`；上一轮request/model/report/complete四文件SHA256全部不变。
- 2025仍仅由既有准备层读取、核验、纳入完整数据指纹，不进入模型/校准拟合或评分；这不是数据库权限层封存。LEARNING_ONLY、training_eligible=false、MODEL_NOT_RELEASED保持，没有新增产品展示或自动发布。

下一步是失败归因和历史数据准入核验，而不是继续按2024成绩反复调参，也不是提前使用2025测试。当前仍缺完整多行情验证、严格历史数据准入及正式发布验收。

## TC-FDP-18｜只读失败诊断、旧模型复现及当前源快照核验（2026-09-08）

本轮无新HTTP、训练参数或表结构。维护脚本`python -m scripts.historical_nav_diagnostics`读取完整旧校准实验，输出新诊断目录。业务发现与限制见[诊断报告](../implementation/free-data-prediction-v1-diagnostics.md)，字段说明见Python手册第13节。

| 验收项 | 预期与已验证边界 |
| --- | --- |
| 不重新训练 | 把LogisticRegression.fit替换为调用即报错，诊断仍完成；三个旧组合模型哈希和原考试成绩保持完全一致。 |
| 不读取TEST计算 | 将PreparedDataset.test设为迭代即报错，模型诊断和当前NAV重放都完成；不输出2025预测或方向统计。既有准备层仍做2025完整性和指纹校验。 |
| 旧产物与数据关联 | 数据指纹、固定三窗协议、FIT/CAL内容指纹、版本和旧EXAM成绩不匹配则拒绝；不以重训绕过差异。 |
| 分组与手算 | 总体/基金/FIT/CAL/EXAM有角色说明；四季度数量加回原2024总数；常量分数、单类和空组行为可手算，单类平均得分差null。 |
| 负斜率与输入指标 | 不裁剪负斜率；保留七列描述统计，不筛选特征。均值/尺度复用冻结基础模型，贡献不冒充因果重要性。 |
| 当前NAV重放 | 人工同源快照与已存输入、方向和可得日期一致；修改当前净值可报告差异，缺失也报告且不覆盖旧样本，例子最多5条。 |
| 复权敏感性 | 改变人工adjusted_nav可使方向差异被计数，已存标签和累计口径输入不变；不可比较返回null，不填0。 |
| 原始读取边界 | 仅既有试点基金、同一来源、2021-01-01至2024-12-31，4501条哨兵发现超量；重复、乱序、混来源、测试年原始行或未知基金拒绝。 |
| 查询和连接 | 仓储测试确认SET只读+四个SELECT、来源/日期条件和LIMIT，退出连接；无授权基金在连接前拒绝。真实现场SQL另外核验，不以替身冒充现场。 |
| 旧实验文件 | 完成清单仅允许request/report/models三个固定文件名、每文件2MiB上限；文件SHA256、模型哈希、请求/报告关联和完成状态均校验，未知路径或部分实验拒绝。 |
| 本机新报告保存 | 原校准四文件前后不变；新report写完回读后最后写complete。重复目录拒绝覆盖，磁盘失败无有效完成清单；非本机fund_ai和非法UUID拒绝。 |

自动化：本轮29项新增诊断用例通过；与303项既有离线相关回归合计332项，另19项真实PostgreSQL隔离存储/读取/训练回归通过，共351项。Ruff、pip check和差异检查通过。既有TestClient弃用提示保留，无依赖升级。

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_historical_nav_diagnostics.py tests/test_historical_nav_calibration.py tests/test_historical_nav_training.py tests/test_historical_nav_evaluation.py tests/test_historical_nav_storage.py tests/test_historical_nav_storage_schema.py tests/test_historical_nav_samples.py tests/test_historical_nav_repository.py tests/test_historical_nav_batch.py tests/test_historical_nav_http.py tests/test_stock_feature_snapshot.py tests/test_feature_read.py tests/test_baseline_analysis.py -q --tb=short
$env:RUN_NAV_STORAGE_PG_TESTS = '1'
.\.venv\Scripts\python.exe -m pytest tests/test_historical_nav_training_postgres.py tests/test_historical_nav_evaluation_postgres.py tests/test_historical_nav_storage_postgres.py -q --tb=short
```

### 真实现场证据

- 复盘旧校准实验`f0f9f709-b82b-4af1-8326-f09b11abac50`，新诊断编号`1054d538-301e-44a1-858c-0deca79a32f6`，完整数据指纹仍为`67e1bd1ab68226a1e8ef29cfd5f369cd3879eb96d57a2e8ce20276ca243e1e10`。
- 实际运行时禁止fit仍完成，12条数据库语句全部SET/SELECT，其中两个REPEATABLE READ/READ ONLY事务；完成后连接释放，无外部采集请求。没有新增HTTP，因此不把这次CLI验收写成用户8000已更新。
- 当前原始净值2922行，仅2021–2024，不读2025原始净值；2037条已纳入训练/验证行的输入、方向、可得日期重放一致。当前原始诊断快照SHA256`d31e7b0be21e102eeb3d4096a5f6d24eb61c8b8e771cad38e1802aaa5f637283`前后相同。
- 真实样本三表仍145/2923/2875；原三基金2022–2025净值全列MD5仍`ccba9f019c6d7f1c6f281d052d4aa34c`。候选实验四文件和校准实验四文件SHA256均未变。
- 诊断report.json的SHA256为`f669844e68791b08cb3fdbad2093a5e7214ef3fb3d3113236adee54295ce19a4`；完整清单回读核验，产物在Git忽略目录。
- 2024三个基金、四个季度Brier均恶化；校准后只判10/660条上涨。当前源数据每基金五个周末净值日，240条已纳入样本的未来窗口含周末记录；累计与复权口径同端点比较2037条，仅006730的一条方向不同，但收益幅度也存在差异。
- 上述结果没有授予训练/发布资格：来源登记不代表重新验证法律或账号授权，当前重放一致不证明历史首次可得正确，20条源净值未通过严格交易日历核验。无样本重存、模型调参或发布。

## TC-FDP-19｜独立交易日窗口、可得截止与只读HTTP（2026-09-08）

关联设计v1.11、实施第17节、Python HTTP手册第14节。新GET仅`fundCode/cutoffDate`，仅三基金日期预览；不修改既有样本/训练接口，不生成特征或标签。

| 检查 | 预期 |
| --- | --- |
| 官方日历 | 五年年度公告各2个官方来源，交易日243/242/242/242/243；规范化JSON与固定SHA256一致，周末和节假日排除。 |
| 春节/调休 | 2024-02-08之后首个交易日为02-19；02-09及调休周末02-18、04-07不参与计数。 |
| 日历边界 | 2020、2026、2021年初历史不足、2025年末未来不足均拒绝；读库前检查，不用普通工作日兜底。 |
| 日历文件 | 损坏、缺年份、超过64KiB或同版本内容改变拒绝；不请求第三方API。 |
| 起点 | 只取`nav_date <= ann_date <= cutoff`且属于交易日的最新日期；正常可同日或滞后1交易日，滞后2日明确拒收。 |
| 起点不可用 | 无已知起点或过旧时历史为空并给出原因；未来日期仍只由日历决定，不将未查询旧日期伪报成源数据缺失。 |
| 精确历史 | 固定截至起点61交易日；删除一条或改晚/改错/去掉公告，会报告对应日期，不能向前补一条掩盖。 |
| 精确未来 | 严格晚于cutoff的20交易日；缺某日时终点及历史均不变，返回未来不完整，最晚可得日null。 |
| 非交易日净值 | 增加周末记录不改变历史和未来日期，只进入忽略列表及影响旧记录计数的对照值；不删除源记录。 |
| 不看未来选历史 | 修改未来公告只能改变未来诊断，不能影响已知起点/历史；无2025数值、标签或评分。 |
| 年度公告可得性 | 2023-12-08的窗口跨到2024，年度安排尚未公告，返回`future_schedule_known_at_cutoff=false`，不伪称当时已知。 |
| 仓储约束 | 单基金、同来源、范围不足192自然日、最多192行、LIMIT193；只取两个日期列；重复/乱序/越界/超量拒绝。 |
| 鉴权及参数 | 无Token、错Token和Origin403；非试点、错日期、额外参数422；拒绝后不查询数据库。 |
| 异常映射 | 缺基金404，来源/适用性/日历范围不足409，SQL/文件/超时内部异常503且脱敏。 |
| 状态与边界 | 日期齐全也仅`WINDOW_DATES_COMPLETE`，所有生成/写入/训练资格标记false，仍`MODEL_NOT_RELEASED`。 |

### 自动化与现场证据

- 新增50项测试。382项相关离线回归、19项真实PostgreSQL隔离回归通过，共401项；Ruff/pip check通过，既有TestClient弃用警告保留。隔离测试仅使用自己创建的测试schema，未写业务样本表。
- 三基金×2021–2024按年只读核对：每只969个官方交易日都有源记录，额外五日为2022-12-31、2023-09-30、2023-12-31、2024-03-31、2024-06-30。没有按源数据反向构造日历或删除源记录。
- 六组真实预览（3基金×2024-03-07/2025-08-08）均日期完整；再通过真实数据库的TestClient核验008888完整响应一致。7次请求35条SQL全为SET/SELECT，7个READ ONLY/REPEATABLE READ事务，7条净值SELECT均只含`nav_date, ann_date`。
- 008888截止2025-08-08：起点08-07/公告08-08/滞后1；历史61日，未来20日08-11至09-05；从净值日起算两个对照终点均09-04。2024-03-07的三个对照分别04-02/04-03/04-08，体现周末记录与起算语义两种独立差异。
- 临时18013实际网络成功200、无Token/Origin403、额外参数422、越日历范围409，TraceID传递及重复完整JSON一致。临时服务已关闭；验收时用户8000未监听，未声称该端口已加载新代码。
- 三表保持145/2923/2875；三基金2022–2025净值2922行全列MD5仍`ccba9f019c6d7f1c6f281d052d4aa34c`，仅用于前后不变核对，不把2025数值作为窗口/模型输入。旧候选、校准、诊断JSON指纹全部不变。
- 日历内容指纹`a7258f7368a071b9fd1df2b2ac039a60bb2be0b4e5c9bde51e836c76ca3c75fa`；本地验收包`.local-runs/nav-trading-window-0147bbd8-f375-40a3-aabc-fc1bea4a3955/report.json`记录日期核验、响应、SQL摘要及前后保护快照。

复跑本步单测：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_trading_nav_window.py -q --tb=short
```

全量相关回归在TC-FDP-18的离线命令中增加`tests/test_trading_nav_window.py`，隔离PG命令不变。本轮无DDL/DML、重训、参数搜索、2025最终评分或发布。日期元数据齐备不是完整净值样本准入，分红/复权和历史首次版本仍须另行核验。

## TC-FDP-20｜净值口径对照与禁止自动准入（2026-09-08）

关联设计v1.12、实施第18节、Python HTTP手册第15节。新GET `nav-basis-audit`，三试点+2022–2024+最多366自然日，只读当前净值与分红快照，不修改旧规则或生成新版样本。

| 用例 | 预期 |
| --- | --- |
| 手算现金/来源复权 | 已实施分红日按两种明确公式分别计算，差异如实保留；不以接近程度宣称供应商公式已确认。 |
| 现金再投连乘 | 多次分红按相邻日毛收益连乘，不把多年现金简单加至终点；改变外层Decimal精度不改变结果。 |
| 三列异常 | 各列缺失/0/负/NaN/Infinity分别报告，现金候选null，不切换为其他列。 |
| 缺交易日 | 保留应有日数，记录确切缺失日，不跳过后接下一行；端点比值可描述但整体不完整。 |
| 周末/节假日 | 源额外记录不影响日收益链，仅进入忽略列表；不删除源数据。 |
| 分红异常 | 生效日期冲突时两天均不假设现金0；未知有效日、非交易日、非实施、晚/缺公告、坏现金拒收候选；同日多条不擅自求和。 |
| 无事件 | 无记录不是没有分红的证明；无事件却出现>1基点现金/复权日差异，记录无法解释调整并阻断候选。 |
| 无大日差异 | 不代表累计净值比值相等，更不批准正式训练/发布；`NOT_APPROVED`固定。 |
| 确定性 | 同请求/快照/日历完整响应一致，事件顺序不影响合法计算；内容变化引起审计哈希变化。 |
| 输入边界 | 非试点、2025、反向/过大日期、额外口径/测试开关422且不读库；纯函数乱序/重复/超量/越界拒绝。 |
| 仓储有界 | 净值386/事件100上限，LIMIT387/101超量哨兵；同基金/来源过滤，窄字段读取，无样本/训练表读取。 |
| 无交易日 | HTTP409，开数据库前拒绝。 |
| 鉴权/错误 | 无Token/错Token/Origin403；缺基金404、来源409；SQL/文件/超时503脱敏，TraceID传递。 |
| 无副作用 | 写库、生成特征/标签、拟合、2025评分均false，旧样本/模型/净值不变。 |

### 实际证据

- 新增64项用例；446项相关离线、19项真实PostgreSQL隔离回归，共465项通过。Ruff及pip check通过，保留既有TestClient弃用警告。
- 2021–2024三基金各974条源记录复权列均有效，累计分红全部为空。006730现有三条实施分红日，单位与复权日收益差异集中于这些日期；不据此证明分红表无漏项。
- 九组真实年度审计（3基金×2022/2023/2024）；006730现金/复权日差为1.348241/17.982339/26.152031基点，其他组合未见超过固定1基点阈值的大日差。没有读取2025净值参与审计。
- 006730/2023：242个交易日，前置日2022-12-30，末日2023-12-29；读取246行包含3条非交易日，所需243点累计分红字段均为空。每日检查242对，1个分红对照，`DIFFERENCES_FOUND`且`NOT_APPROVED`。
- 同端点四算式分别约-17.7952%/-4.1216%/-6.7837%/-6.5344%，顺序为单位/累计/来源复权/条件性现金再投。不是模型成绩、标签替换或个人实际收益。
- 九次服务加一次TestClient实库HTTP共60条SQL，10个READ ONLY/REPEATABLE READ事务，10条净值+10条分红查询，全为SET/SELECT，无样本表读取。
- 临时18014网络HTTP成功200、鉴权403、2025/额外口径422、无交易日409，重复响应和TraceID一致。8000未监听，临时服务验收后关闭，不声称用户端口已更新。
- 三表仍145/2923/2875；原三基金2022–2025净值2922行全列MD5仍`ccba9f019c6d7f1c6f281d052d4aa34c`，仅用于确认未改动；旧候选/校准/诊断/日期验收产物未改。
- 本地验收报告`.local-runs/nav-basis-audit-f6931bd0-5860-4320-abf5-a79882f02c84/report.json`记录九组结果、原始核验摘要、公式差异和前后保护快照，不提交真实数据包。

复跑单测：`python -m pytest tests/test_nav_basis_audit.py -q --tb=short`。完整离线命令在TC-FDP-19基础上加该文件，隔离PG命令不变。本步确认差异而非准入口径；未重存历史样本、创建新标签、训练或发布。

## TC-FDP-21｜现金再投样本、输入/答案时间隔离与只读HTTP（2026-09-08）

关联设计v1.13、实施第19节、Python HTTP手册第16节。新增`cash-reinvestment-preview`；只读三试点2022–2024，不修改旧构建器和存储/训练入口。

| 用例 | 预期 |
| --- | --- |
| 现金算式 | `(unit_t+cash_t)/unit_prev-1`；多次分红逐日连乘，不能只把现金累加到终点；序列基准日分红不计入。 |
| 精度与方向 | 内部Decimal40位，输出12位，指标从显示指数计算；外部精度/舍入设置不改变结果；显示收益0对应方向0。 |
| 准确20段 | 截止当日尚未公告净值不入特征，但可作后来答案分母；61历史点、21答案点；终点只由cutoff后20交易日决定。 |
| 已知输入 | 公告不晚于cutoff，净值业务日不晚于公告；锚点最多滞后1交易日，无起点明确拒收。 |
| 缺值或坏值 | 历史缺交易日、公告缺失/非法/过晚，单位净值缺失/0/负/NaN/Infinity均拒收，不换累计/复权列、不补老行。 |
| 未来变动隔离 | 未来/尚未公布基准日的价格、坏值、缺行、公告变更不改变feature_payload/hash；答案可变或null，终点不顺延。 |
| 事件隔离 | 未来现金、状态、公告变化不进输入；未来才获知的历史分红不追溯回填；已知历史分红改变输入不改变后续无关标签。 |
| 实施公告 | 历史使用方案公告与已提供实施公告的较晚日期，截止当天可用、晚于截止不可用；标签最晚可得日包括实施公告。 |
| 事件异常 | 日期冲突、非交易日、非实施、缺/超时公告、坏现金、同日多个事件拒收相关整段，不求和。已公告但生效日未知时历史也拒收。 |
| 周末/节假日 | 截止非交易日基准用前一交易日；额外非交易日源记录不改变特征/答案hash，不删除源数据。 |
| 日历公告边界 | 未来年度安排当时未知则保留输入、标签不可用；日历范围不足不猜日期。 |
| 平坦窗口 | 相对位置分母0则输入不可用，不伪造位置值。 |
| 数据包结构 | 重复/乱序/越界/超量净值，重复事件标识或超过100事件拒收；不悄悄裁剪。 |
| 只读仓储 | 单基金同来源，净值仅三列、分红七列含实施公告；LIMIT193/101哨兵；SET REPEATABLE READ, READ ONLY在来源/数据读取前执行。 |
| 测试期保护 | 2025截止422；2024截止但完整标签进入2025则409，均不读数值；不提供includeTest、口径、任意horizon等参数。 |
| HTTP | 200响应与直接调用一致；无Token/错Token/Origin403，非法参数422，基金404/来源等409，内部异常503脱敏；TraceID保持。 |
| 状态边界 | 算得出只表示RESEARCH_SAMPLE_READY；所有正式训练资格、历史版本/事件完整性、写库、拟合、评分标记false，MODEL_NOT_RELEASED。 |

### 实际验证结果

- 新增80项自动化用例；相关离线526项、真实PostgreSQL隔离19项，共545项通过。Ruff及pip check通过，沿用的TestClient弃用警告保留。PG回归仅操作测试schema，不写业务样本表。
- 3基金×2022-06-23/2023-06-20/2024-03-07，加006730/2023-06-22，共10组真实样本均`RESEARCH_SAMPLE_READY`。这是当前快照/假设下可计算，不是正式训练准入。
- 006730截止2023-06-20：历史3月21日至6月19日61点，输入最晚公告6月20日；答案基准6月20日、未来首日6月21日、终点7月20日，最晚公告7月21日。6月21日现金0.1676，日收益`-0.019523541021`，20日收益`-0.023690666143`，方向0。分红只在答案序列出现。
- 改截止2023-06-22：已知锚点6月21日，分红进入历史；答案基准仍为6月21日但现金不重复计入，末日7月21日，收益`-0.003759705762`。
- 最终一轮10次服务+2次网络成功请求共72条SET/SELECT，12个只读事务、12条净值和12条分红查询；不读样本表，不查累计或来源复权列。失败请求没有产生SQL。真实200/403/422/409、TraceID、重复完整JSON及直接/HTTP一致均通过。
- 临时54479网络服务已关闭，8000未监听，未重启用户进程。
- 旧三表仍145批次/2923样本/2875标签；三基金2022–2025原始净值2922行、全列MD5仍`ccba9f019c6d7f1c6f281d052d4aa34c`。该2025检查仅用于前后未改动指纹，不作为研究输入/答案。旧候选/校准/诊断/日期/口径审计文件SHA256前后一致。
- 验收包`.local-runs/nav-cash-sample-d87bc4e4-0e2b-457c-b629-0834dda861f8/report.json`已回读并确认被Git忽略，SHA256 `ed7148d9742ccdb49354e20cce16c18998a461d54786cc8105a5c934d63cb8c3`。包内保留10组摘要、一条完整响应、网络/SQL摘要及前后保护快照。

复跑本步：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_cash_reinvestment_samples.py -q --tb=short
```

完整离线回归在TC-FDP-20基础上增加该文件，隔离PG命令不变。本步没有新版批量生成、保存、重训、发布或2025评分；下一步批量dry-run还需单日/批量和分页不变验收。

## TC-FDP-22｜新版批量dry-run的单日等价、分页不变和只读失败边界

关联设计v1.14、实施第20节、Python HTTP手册第17节。新增GET `cash-reinvestment-dry-run`，不保存、新旧版本不混用。

| 用例 | 预期 |
| --- | --- |
| 日历选题 | 2023年6月20道交易日题，10个跳过日；6月22/23端午休市不出题。缺数据的交易日仍返回问题样本。 |
| 单日等价 | 每条完整items对象与相同基金/cutoffDate的新单日GET一致，包括日期、问题、指标、序列和双哈希。 |
| 分页不变 | 页大小1/2/7/10/30不改变样本；只变page_size/page_count，其余字段及batch_hash相同。 |
| 资料边界 | 每页合并读取后按单题精确窗口裁剪，邻题事件和周末源行不能混入另一题诊断；分红无日期、冲突日期按单日处理。 |
| 异常数据等价 | 缺净值、缺公告、零净值、未来缺数、分红日期冲突/无日期/未知公告/晚实施公告、非交易日行，单日/批量及多页大小结果一致。 |
| 不看未来改历史 | 沿用单日现金测试的未来输入隔离；未来资料有问题不改该题历史payload/hash，不能把无答案填0。 |
| 计数 | 三状态互斥且和为sample_count；输入可用/答案可用与items实际payload对应；问题统计同题同代码只计一次。 |
| 空范围 | 仅休市日200、NO_TRADING_CUTOFFS、0题0页；先核验来源，但不查询净值/分红。 |
| 跨年安排 | 2023-12-01至12-08保留6道输入，其中5道未来安排当时未知，标签不可用，不改截止或顺延终点。 |
| 测试期保护 | 2024-11-20至12-20任何一题答案跨2025即整次409，页大小1和30都在开库前失败；2025截止422。 |
| 事务与查询 | 一个READ ONLY/REPEATABLE READ事务，一次来源检查，一次全批分红，一页一条净值SELECT；不逐题重开快照。 |
| 固定容量 | 全批事件100条上限与页大小无关；超量/重复标识拒绝；净值超量/重复/乱序/越界拒绝，不靠裁剪隐藏。 |
| 中途失败 | 第二页SQL失败、题间超时关闭事务，HTTP503脱敏，不返回部分items。 |
| HTTP | 非试点/过长反向日期/非法页大小/额外参数422，Token/Origin403，来源409、缺基金404、内部错误503；TraceID保持。 |
| 版本和保护 | 单日规则版本不变，新增批量编排版本；正式训练资格、写库、拟合、2025评分均false，未发布。 |

### 实际证据

- 新增57项用例；相关离线583项、真实PostgreSQL隔离回归19项，共602项通过。Ruff及pip check通过；保留既有TestClient弃用警告。PG隔离回归仅操作测试schema，不写业务批次。
- 三试点2023年6月各20条输入与答案可计算，pageSize1/7/30共9组批量一致；与60次真实单日调用逐条完整JSON一致。006730/2023-06-20与上轮完整单日验收示例一致。
- 006730上述三种页大小页数20/3/1，batch_hash均`454558c6cec14e04b1b93a24ba3abc27f17570799714f181eca8672cb3f4cfb9`；只排除page_size/page_count后整份响应完全相同。
- 006730、2023-12-01至12-08为6题、6输入、1答案、5标签不可用，原因全部`FUTURE_CALENDAR_NOT_KNOWN_AT_CUTOFF`；不是模型命中率。休市空范围也已实际网络验证。
- 最终验收75个只读事务，共532条SET/SELECT，其中158条净值、74条分红查询；净值值域只有unit_nav，分红含实施公告。无旧样本表读取、无DDL/DML；鉴权/参数/测试期拒绝没有SQL。
- 临时57807真实网络200/403/422/409、三种页大小、单日对照及TraceID通过，服务已关闭；8000未监听，未重启其他进程。
- 原三表145/2923/2875；三基金2022–2025原始净值2922行、全列MD5`ccba9f019c6d7f1c6f281d052d4aa34c`前后相同，2025仅用于未改动指纹，不作输入/答案。旧实验及现金单日验收包SHA256均未变。
- 本地报告`.local-runs/nav-cash-batch-23ea0dca-afdf-4e45-81df-260d667ef13b/report.json`已回读校验，Git忽略；SHA256 `f34b3d73b63d7caaea8d979a1d94f7d4def46c690aaf600a42011cdb884e260b`。报告含9组摘要、单日对照记录、一条完整例子、跨年/网络/SQL摘要和前后保护快照，不是已入库批次。

复跑本步：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_cash_reinvestment_batch.py tests/test_cash_reinvestment_samples.py -q --tb=short
```

完整离线命令在TC-FDP-21基础上新增`tests/test_cash_reinvestment_batch.py`，隔离PG命令不变。本轮不保存、新训练器未接入，历史首次版本/分红完整性及拆分折算仍未核准。
