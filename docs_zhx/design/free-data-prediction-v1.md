# 基金雷达：免费已授权数据预测 V1 设计

> 关联需求：[当前 2000 积分可用数据补齐](../requirements/tushare-2000-data-completion.md)、[自选与预测模块](../requirements/watchlist-prediction-module.md)
> 版本：v1.17 ｜ 日期：2026-09-09 ｜ 总体变更等级：L3 ｜ 状态：Docker恢复后，实库输入及关注状态页面已复验通过；正式发布与在线数值生成尚未完成。契约不变，完整目标复核及最新证据见实施第22–24节。

## 1. 结论和目标

在不购买基金股票持仓、新闻、公告等付费数据的前提下，先做一个以“基金自身历史净值 + 当时已披露的公共基金信息 + 市场状态”为输入的日频方向概率模型。它回答的是：

> 截至某个明确数据日，未来第 20 个交易日，该基金净值上涨的历史条件概率是多少？

它不是买卖建议，不承诺预测一定正确，也不声称知道基金现在持有哪些股票。模型首先要通过严格的历史滚动回测；回测不达标就只展示数据状态，不发布概率。

## 2. 产品范围

### 2.1 第一阶段支持对象

| 分类 | V1 处理方式 | 原因 |
| --- | --- | --- |
| 股票型基金 | 首批支持，目标为未来 20 个交易日方向 | 历史净值较充足，市场状态的解释价值相对更高 |
| 混合型基金 | 与股票型分开建模，达到样本与回测门槛后再启用 | 风险暴露差异较大，不能直接共用股票型模型 |
| 债券型、货币型、其他 | V1 不发布方向概率 | 收益机制与数据特征不同，混在一起会制造错误精度 |
| ETF/LOF | 仅在基础档案和日线完整时作为独立子类评估 | 场内价格与场外净值不能混作同一个标签 |

当前数据快照中有 24 只股票型、15 只混合型、2 只债券型和 2 只其他基金。V1 先以股票型做可用基线；这是减少异质性，不是对其他基金下结论。

### 2.2 不纳入 V1 的内容

1. 基金的股票持仓、行业真实暴露和重仓股涨跌：当前没有 `fund_portfolio` 授权，禁止猜测；
2. 新闻、公告、研报和社交情绪：当前不购买相应权限，也不以未经授权的抓取替代；
3. 用户个人买入金额、成本、盈亏和风险偏好：公共预测与个人投顾分离；
4. 分钟级、实时价格和盘中交易提示：V1 只用有明确截至日的日频数据；
5. “必涨”“稳赚”“建议买入/卖出”等交易结论。

## 3. 数据输入与时间边界

本设计依赖 [数据补齐需求](../requirements/tushare-2000-data-completion.md) 的 P0 数据。每一条特征都必须同时记录 `as_of_date`（预测截点）和 `available_at`（当时实际可获得日期）。回测时，`available_at > as_of_date` 的记录一律不可用。

| 特征组 | 主要数据 | 举例 | 使用原则 |
| --- | --- | --- | --- |
| 基金自身走势 | 单位净值、累计净值、复权口径 | 5/10/20/60 日收益、波动率、最大回撤、均线距离、历史分位 | 只计算截至预测日已经确认的净值；分红导致的机械跳变要处理 |
| 基金自身风险状态 | 净值历史 | 下行波动、连续回撤天数、收益偏度、相对自身历史位置 | 不使用未来区间的最高/最低值 |
| 基金经理 | 历史任职记录 | 截点任职天数、近 90 日是否换经理、多经理标识 | 任职年限以当时截点计算，不能把今日经理带回过去 |
| 基金规模和份额 | 历史份额快照 | 份额同比/环比、规模分层、最近披露距今天数 | 低频且可能缺失；缺失本身作为特征并保留质量标识 |
| 分红事件 | 历史分红/除权日期 | 最近分红距今天数、除权窗口标识 | 只用于避免把规则性净值变化误读为市场波动 |
| 基金档案 | 基金类型、成立日、费率等 | 基金年龄、类型、场内/场外标识 | 类型用于分模型，不能用来替代持仓风格 |
| 市场状态 | 已授权指数目录、分类、日线、权重 | 宽基指数 5/20 日收益、市场波动、成长/价值或行业指数状态 | 是“外部天气”，不是拿另一只基金来比输赢；指数无法识别时可降级 |

### 关于“可用基准”的更正与定义

基金自身净值历史足以构造一个“自身方向基线模型”，因此没有市场指数时，仍可以回测并输出标明范围的自身走势概率。此前“没有基准就不能做预测”的说法过强，本设计不再采用。

这里的市场参考指数有两个可选作用：

1. 作为模型输入，区分“整个市场普遍上涨”与“基金相对自身节奏的变化”；
2. 作为解释文字，让用户知道当期市场环境，而不是把某只别的基金强行作为比较对象。

它不是发布 V1 的硬性前置条件。若指数数据缺失，系统可运行 `OWN_NAV_DIRECTION_BASELINE`，同时明确标记“未启用市场状态特征”；若指数数据完整且滚动回测证明有稳定增益，才升级为 `OWN_NAV_PLUS_MARKET_V1`。

## 4. 标签、模型和输出

### 4.1 预测标签

对基金 `f` 在预测截点 `t`，计算未来第 20 个有效交易日的累计净值变化：

```text
future_return_20d = adjusted_nav(f, t + 20) / adjusted_nav(f, t) - 1
label_up_20d = 1,  当 future_return_20d > 0
label_up_20d = 0,  当 future_return_20d <= 0
```

若截点或未来日期的净值不可用、异常且不能由分红解释，则该样本不进入训练和评估。标签口径、净值口径、交易日规则和异常过滤版本必须随模型版本保存。

### 4.2 建模顺序

先从可解释、抗小样本过拟合的模型开始，再以独立时间段验证是否值得增加复杂度：

1. 无模型对照：延续最近 20 日方向、历史上涨频率、始终上涨/始终下跌；
2. 自身走势模型：正则化逻辑回归或等价的概率分类基线；
3. 自身走势 + 市场状态模型：仅当指数特征提高独立样本表现且未破坏校准时启用；
4. 只有在样本量、独立回测和可解释性都达标时，才评估树模型等非线性方法；不能为了追求单次命中率而更换模型。

训练产物必须记录训练数据截点、样本数量、基金范围、特征版本、标签版本、模型版本和随机种子。禁止用大语言模型直接根据基金名称生成预测概率。

### 4.3 对外输出契约

| 字段/状态 | 含义 |
| --- | --- |
| `as_of_date` | 这次判断实际用到的数据截至日，不能写成当前自然日冒充实时 |
| `horizon_trade_days` | 预测窗口，V1 固定为 20 个交易日 |
| `direction_probability_up` | 未来 20 个交易日累计净值上涨的经校准概率；仅在回测达标时输出 |
| `model_kind` | `OWN_NAV_DIRECTION_BASELINE` 或 `OWN_NAV_PLUS_MARKET_V1` |
| `data_completeness` | 各特征组可用性、最新数据日和缺失原因 |
| `explanation` | 仅列数据驱动的正/负向因素及其数据日，不输出交易指令 |
| `DATA_INSUFFICIENT` | 历史长度、关键净值、披露时效或质量不足，不输出概率 |
| `MODEL_NOT_RELEASED` | 回测未通过或尚未完成，不输出概率 |
| `STALE_DATA` | 来源数据超过时效阈值，保留最近结果但显著标明不能据此决策 |

前端统一使用“免费数据方向模型 V1”的名称，并在结果旁显示：截至日期、预测窗口、模型类型、数据完整度、未包含基金持仓/新闻/公告。概率只能表示历史条件下的估计，不能显示成收益率承诺。

## 5. 严格的滚动回测

### 5.1 回测到底在做什么

回测不是把所有历史数据拿来训练后再看自己答得对不对。正确做法是模拟当时的真实状态：

```text
历史截点 t
  -> 只取 t 当天及以前、当时已披露的数据
  -> 训练或更新模型
  -> 预测 t 后第 20 个交易日涨/跌
  -> 等真实净值出现后，与预测比较
  -> 向前滚动到下一个截点，重复
```

这样才能发现“看到了未来”“只适合某一段牛市”“只靠偶然命中”等问题。每次回测必须按时间顺序切分训练、验证和最终独立测试区间；最终测试区间不得用于选择特征、阈值或模型。

### 5.2 覆盖范围与指标

回测要同时覆盖上涨、下跌、高波动和横盘等不同市场阶段，并按基金类型、基金、市场状态分别汇总，而不是只报一个总体命中率。

| 指标 | 要回答的问题 | 防止的误导 |
| --- | --- | --- |
| 方向准确率 | 涨跌方向判断是否比朴素对照更好 | 只报一段好行情的命中率 |
| Brier 分数/概率校准 | 说 70% 上涨时，长期是否真的接近 70% | 概率数字看似精确却不可信 |
| 覆盖率 | 多少基金日期满足数据条件并能作答 | 只挑容易的基金预测 |
| 分组稳定性 | 各基金、市场阶段和滚动窗口是否持续有效 | 个别基金拉高总体成绩 |
| 回撤/波动区间表现 | 高风险阶段是否恶化到不可用 | 平常可用、极端行情误导用户 |
| 相对对照增益 | 是否优于“延续近期方向”等简单规则 | 复杂模型没有真实价值 |

### 5.3 发布闸门

发布不是预先承诺一个漂亮命中率，而是需要在预先冻结的独立测试期满足以下所有条件：

1. 没有未来数据泄漏，抽查任意样本都能还原当时的数据版本和可得日期；
2. 有效样本覆盖多个不同市场阶段，且股票型基金的可评估样本量、基金数和测试窗口已在回测运行记录中固定；
3. 模型在独立测试期相对全部朴素对照存在可重复的增益，而不是只在某一个基金或一个窗口胜出；
4. 概率校准可接受：高概率组不能长期比低概率组更差，校准恶化时必须降级或重新校准；
5. 数据完备度、数据时效和异常净值处理均达到本次运行的质量门槛；
6. 结果页有明确的风险提示、数据边界和“未覆盖持仓/新闻/公告”说明。

若任何一项不满足，系统只能保留回测报告和 `MODEL_NOT_RELEASED` 状态，不能为了产品展示而输出概率。

## 6. 技术架构和运行流程

```text
Tushare 已授权日频/低频数据
          ↓（后台增量、质量校验、数据日期）
fund_ai 数据表与同步运行记录
          ↓（点时特征构建）
feature_snapshot
          ↓（滚动训练与严格回测）
backtest_run ──未达标──> MODEL_NOT_RELEASED
          ↓ 达标
forecast_result（带模型/特征/数据版本）
          ↓（只读 API）
基金详情页 / 关注列表
```

前端负责状态呈现和解释，不计算模型、不持有 Token；Java 服务负责业务接口、权限和任务编排；Python 服务负责可重复的特征计算、训练和回测。所有任务使用已有的来源注册、同步运行、特征快照、预测结果和回测运行等对象扩展，新增表和字段通过版本化迁移落地。

## 7. 版本与实施包

| 包 | 内容 | 结果 |
| --- | --- | --- |
| A. 数据底座 | 落地上一份文档的 P0 回补、增量、水位、异常清单和指数参考数据 | 可追溯的免费数据基线 |
| B. 特征层 | 构建点时净值、经理、份额、分红、档案和可选市场状态特征 | 每个截点可复现的 `feature_snapshot` |
| C. 回测层 | 实现朴素对照、时间滚动、指标、校准和结果留档 | 可审查的 `backtest_run` |
| D. 发布层 | 仅将通过发布闸门的模型写入 `forecast_result` | 明确数据日与模型边界的概率结果 |
| E. 页面层 | 在详情页和自选页展示状态、概率、解释、数据完整度和限制 | 用户不会把结果误解为实时持仓或投资建议 |

各包都可独立验证。A 未完成时不能把缺失数据伪装成特征；C 未通过时不能执行 D；详情页不可绕过 D 直接计算或调用外部数据源。

## 8. 七维治理、风险与验收

| 维度 | 设计要求 |
| --- | --- |
| 业务 | 定位为信息辅助和历史条件概率，不输出买卖指令；模型类别和适用基金范围要公开 |
| 数据 | 严守点时可得性，分红/异常净值处理可追溯，低频披露缺失不补零 |
| 接口与性能 | 训练和回测离线批处理，查询接口只读已发布结果；按基金、日期分页处理，避免全表加载 |
| 权限与安全 | 不使用未授权持仓、新闻、公告；Token 只存在服务端配置；公共模型与用户资产数据隔离 |
| 兼容 | 未上线前保留现有详情页行为；新增模型状态让旧客户端安全降级 |
| 运维 | 每次预测、特征和回测都保存版本、数据截至日、运行状态和失败原因；过期或失败必须可见 |
| 测试 | 覆盖未来泄漏、缺失低频披露、分红跳变、异常净值、指数缺失、回测失败、模型降级和前端风险文案 |

### 最终验收口径

这套方案的成功标准不是“每次猜中”，而是：用户能看到数据确实覆盖到了哪里、模型在看不到未来的条件下经过了多少历史验证、概率是否经校准、以及当前结果何时应当明确拒答。只有这四件事同时成立，免费预测功能才值得上线。

## 9. v1.1｜已确认的 V1.0 最小实施范围（2026-09-02）

为避免把“最终多周期产品目标”和“第一轮可验证试点”混为一谈，现确认：

1. V1.0 仅覆盖 `STOCK` 股票型基金，预测窗口固定为 20 个有效净值日；混合、债券、货币、其他类型和 ETF/LOF 均不输出概率。
2. V1.0 首先只使用基金自身历史净值，模型类型固定为 `OWN_NAV_DIRECTION_BASELINE`。市场指数目录不等于市场特征；在登记白名单、回补指数日线并证明独立增益前，不启用 `OWN_NAV_PLUS_MARKET_V1`。
3. 完整概率仅在“我的关注 -> 单基金详情”中向已关注用户展示；公共基金详情保留基础事实和受限状态摘要，不能绕过关注关系读取概率。
4. 这一试点不取消后续 5/10/20/40 个交易日四周期目标。V1.0 通过发布闸门后，才将相同的点时数据、独立回测和独立发布规则逐周期扩展。
5. 首次实际开发按“手算样本 -> 历史逐日特征 -> 朴素对照 -> 小型概率模型 -> 滚动回测 -> 发布读取”顺序推进；每个阶段均可暂停复核，回测未通过不允许发布。

详细的学习式实施步骤、数据可得日期规则和测试用例见[实施手册](../implementation/free-data-prediction-v1.md)。

## 10. v1.2｜阶段2的HTTP纯计算预览（2026-09-07）

新增 `POST /internal/v1/features/historical-nav-samples/preview`，挂在Python现有features路由，沿用`X-Service-Token`认证和拒绝`Origin`策略。维护人员通过Postman调用，没有Java/Vue用户入口。输入单基金1–512条净值，日期严格递增、无重复，数值有限且为正；输出特征、离线标签、状态和计数，无数据库/外部I/O。

本预览中的`as_of_date`是样本的**净值业务日**；可得截止日为该起点`ann_date`，输出为`available_at`。请求最外层可选`as_of_date`仅筛选结果，不能用本文早期“available_at > as_of_date 一律不可用”的说法直接比较这两个不同语义的字段。公告只有日期，按公告日结束后可用解释；盘中可得时刻和历史修订版本仍未完成。

每条样本只用当时可见的最多61条净值决定输入口径及特征，再附加后20条净值的标签。标签缺值或未成熟仍保留已有特征，不能切换过去口径。特征版本为`M3_STOCK_HISTORICAL_NAV_FEATURE_V2`，未来标签不参与特征哈希。标签窗口出现无效记录时拒收，不跳过后延终点；标签可得日取所需未来记录最晚公告日；起点公告时终点答案已知的样本拒收。

HTTP 200可以包含数据不足状态；格式/顺序/数量错误为422；认证失败/浏览器来源为403；未配置服务Token为503。无持久化、无DDL/DML。同步预览限制512条，日志记录TraceID、基金、计数和耗时；异常保留堆栈，不打印请求体或Token。

说明和JSON/Postman集合在Python仓库`docs_zhx/implementation/historical-nav-http-preview.md`及`docs_zhx/examples/`。此入口不代表分页读库、持久化、真实来源水位校验或模型训练已完成。

### v1.3｜GET直接读库预览（2026-09-07）

同路径新增GET，参数`fundCode`与`asOfDate`，无请求体，直接返回单条HistoricalNavSample。复用内部认证，数据库确定启用股票型基金及已启用、成功同步的来源；净值按该来源隔离。读取起点、此前最多60条截至起点公告日已可见的记录，以及此后原始序列的20条记录；未来不按公告日过滤，以免偷换终点。

数据库使用独立2连接池，建连、等待连接和单查询各5秒超时；查询在REPEATABLE READ、READ ONLY事务完成，不写库、不补数。缺基金/精确日期净值为404，来源未就绪/不适用品类为409，数据库异常为503，不向调用者输出连接细节。返回当前来源水位，不宣称已有历史修订版本。无需新增表、DDL或DML。

### v1.4｜单基金小范围分页只读dry-run（2026-09-07）

新增`GET /internal/v1/features/historical-nav-samples/dry-run`：`fundCode`、`startDate`、`endDate`必填，
`pageSize`可选、默认10、范围1–30。日期含首尾、最多31个自然日；不接受多余字段，非法参数422。
继承内部Token/拒绝Origin认证；数据库核验启用股票型基金与就绪来源，无Java/Vue产品入口。

响应固定`mode=DRY_RUN`，返回基金/日期范围、`page_size`、非空`page_count`、`sample_count`、三种质量计数、
`unavailable_reasons`及日期递增的全部`items`。每项与单日GET相同；空范围200且0条，不向邻近日回退。
页大小改变时，仅分页元数据允许变化；同一数据/来源运行记录/计算版本下，样本内容及统计必须一致。

一请求一个REPEATABLE READ、READ ONLY事务，基金/来源仅检查一次。按`nav_date > 上页最后日期`做游标分页，
每页最多两条净值SQL：取起点；将各起点的历史LIMIT 60及未来LIMIT 20子查询通过UNION ALL合并读取。
各查询固定同一基金和来源；历史按各自公告截止筛选，未来不筛公告/数值。辅助数据可越过请求范围和页边界；
每份最多81条输入，调用原构建器只取本起点输出，累计输出最多31份。不一次加载全基金历史，也不逐日网络查库。

复用2连接预览池及5秒单SQL超时，增加15秒页间预算（不是HTTP硬截止）。故障或超时整次503，不返回部分成功；
基金不存在404，品类/来源不适用409，计算数值异常422。只记录一次完成摘要和带TraceID的异常堆栈，不打印Token或净值明细。
无样本持久化、DDL/DML、任务调度、外部采集、模型训练；历史修订回放和来源整日漏数识别仍未完成。

### v1.5｜样本起点新鲜度检查（2026-09-07）

L2局部规则变更：从“终点已知才拒收”收紧为“起点公告时已有任何更新净值已公告即拒收”。
更晚记录必须满足同基金、同来源、`newer.nav_date > anchor.nav_date`且`newer.nav_date <= newer.ann_date <= anchor.ann_date`。
同日公告按日终已知解释，缺失/早于净值日的公告不作已知证据；不依赖净值涨跌方向或人为天数阈值。

- 数据读取：起点SELECT附带相关`EXISTS`（是否存在）布尔值，搜索到起点公告日为止，不限后20条或请求日期段，不新增逐日网络查询。批量仍每个非空页最多2条净值SQL，每份最多81条净值资料。
- 公共计算：`HistoricalNavSampleInput.stale_anchor_dates`是仓储额外确认的过时起点集合，仅内部传递，不开放为HTTP参数。构建器另对所给序列从后向前检查一次，再合并该集合；因此POST也使用同一规则，但无法证明调用者未提供的记录。
- 拒收顺序：先检查起点自身公告日期，再检查是否过时，之后才计算历史指标和标签。过时项保留业务日期/公告日，`nav_value_basis=UNDETERMINED`、`metrics=null`、`offline_label=null`，总体及特征质量均为`DATA_INSUFFICIENT`，原因为`STALE_NAV_AT_CUTOFF`。
- 兼容与追溯：响应新增`sample_rule_version=HISTORICAL_NAV_SAMPLE_RULE_V2`；无此字段的旧响应按旧规则V1理解。它不进入`feature_payload`，指标公式版本仍为`M3_STOCK_HISTORICAL_NAV_FEATURE_V2`，标签仍为`FUTURE_UP_20D_V1`，正常样本原有字段及哈希保持一致。旧起点即使终点已公告也优先使用新的过时原因。
- 安全与运行：现有Token、Origin拒绝、来源隔离、只读一致性事务、超时/连接上限及日志TraceID不变。批量在`unavailable_reasons`汇总新原因，不在循环内增加INFO日志；失败仍不返回部分成功。
- 验收：覆盖部分后续净值已知、仅第21条先公告、同日边界、错误来源/基金、长间隔但无更新公告、日期错误优先级、原始记录不变与分页/单日一致。

这道检查只防止选用旧起点；不证明来源的`ann_date`就是首次可得日期，也没有恢复历史修订版本。保存训练集、训练与发布继续暂停在后续阶段。

### v1.6｜学习批次、题目与答案存储结构（2026-09-07，L2）

本次只交付Python实体、Alembic `20260907_13`迁移、等价PostgreSQL建表/空表回退SQL及离线契约测试。
**未在真实数据库执行迁移；无保存服务、HTTP写入口或样本数据。** 不复用会按基金/日期/版本覆盖旧内容的`feature_snapshot`，新增结构独立保留学习批次。

| 七维筛查 | 决策与边界 |
| --- | --- |
| 业务 | 一只股票型基金、起点日期含首尾最多31个自然日；20个净值区间。固定LEARNING_ONLY，存储就绪不代表可以训练或发布。 |
| 数据 | 三表分离；无答案不写标签行；保留拒收样本及缺失/错误公告事实；规则版本按批次登记。原始净值和既有特征表不改。 |
| 规模 | 封面按基金/创建时间/批次编号索引；题目以批次/日期唯一键检索；答案以题目编号主键检索。后续读取必须有界分页，不加载全基金历史。 |
| 稳定性 | 请求凭证唯一；以后同凭证同请求重试复用，异参数拒绝，明确重算换凭证。整批事务和不覆盖旧批次由后续服务实现，本步不做任务状态机。 |
| 安全 | 不增加API或个人字段，不变更现有Token与关注权限。数据库所有者为Python Alembic管理的fund_ai，不触碰Java Flyway管理的fund_core。 |
| 可观测 | 封面保留来源水位、三个规则版本、保存时间、质量数量与原因统计；未来保存日志只记录TraceID/批次/基金/计数/耗时和异常堆栈，不打印密钥或全量题目。本步没有运行日志链路。 |
| 兼容与扩展 | 加性迁移接在20260905_12之后，不修改旧迁移。只声明学习数据，后续训练准入、首次可得校验、全历史任务单独推进。 |

#### 三表字段契约

- `historical_nav_sample_batch`：`batch_id UUID`主键，`request_key UUID`唯一且由未来调用方显式提供；`fund_code varchar(32)`关联基金档案；`fund_type varchar(32)`固定STOCK；`source_code varchar(64)`为来源快照；`source_sync_run_id UUID`可空并关联来源运行；`start_date/end_date date`；`feature_version/sample_rule_version/label_version varchar(128)`非空；`purpose varchar(32)`固定LEARNING_ONLY；四个质量计数为integer；`unavailable_reasons jsonb`为原因到数量的对象；`created_at timestamptz`为保存时间。
- `historical_nav_sample`：`sample_id UUID`主键，`batch_id UUID`关联批次，`as_of_date date`与批次组成唯一键；`available_at date`可空；`nav_value_basis/eligibility_status varchar(32)`；`unavailable_reason text`可空；`feature_payload jsonb`；`feature_hash varchar(64)`。同批共享基金、来源和规则，未来服务核对载荷中的重复元数据与封面一致。
- `historical_nav_sample_label`：`sample_id UUID`同时是主键和题目外键；`horizon_trading_days smallint`固定20；`label_end_date/label_available_at date`；`future_return_20d numeric`不固定小数位，保持现有Decimal值；`label_up_20d smallint`为1/0。所有列非空，缺答案通过没有行表达。

数据库结构声明：日期差0–30；计数非负、总数不超过日期范围且等于三种状态数之和；JSON顶层为对象；哈希为64位小写十六进制。
完整样本无拒收原因，其余状态必须有原因；非DATA_INSUFFICIENT要求有效可得日期与已确定口径。
拒收行允许公告为空或早于业务日，不能为了满足约束而改写错误事实；标签收益须有限且大于-1，上涨1与非上涨0必须与收益符号相符。
外键不配置级联删除。`feature_hash`只覆盖已有特征包，不含标签、批次编号或独立筛选规则版本；不保证整个批次内容相同。

以下为**下一步保存服务必验规则，当前未实现**：题目日期在批次范围；基金/来源运行归属/三个版本一致；特征不混答案且哈希匹配；数量/原因统计与明细相符；仅SCORABLE有且必须有答案，标签终点晚于起点且标签可得日晚于输入截止；输入与标签口径一致。当前读库保存场景仍应拒绝未就绪来源，不因水位列可空而放宽现有准入。
重试与重算区别：同`request_key`先校验原请求身份，同请求返回旧批次，即使来源随后更新也不重新计算；请求身份不含`pageSize`。想使用新源数据或新规则，必须明确提交新凭证。凭证相同但基金/日期/规则不同应冲突拒绝，而非静默返回错误批次。
整批提交或整批回滚、不覆盖旧批次、未来标签成熟生成新批次，由后续事务代码实现；当前没有触发器/权限来提供数据库层绝对不可变保证。

迁移位置在Python仓库`alembic/versions/20260907_13_add_historical_nav_sample_storage.py`；原生DDL在`docs_zhx/sql/20260907_13_historical_nav_sample_storage_upgrade.sql`及`downgrade.sql`，无业务DML。
常规升级使用Alembic，不能再重复执行原生SQL；后者不更新迁移版本表。迁移前确认fund_ai、前置版本、备份、权限和执行窗口，并先在隔离PostgreSQL库验收。
回退仅允许三表全部0行：先NOWAIT加排他锁，再检查，非空/锁不可得则拒绝；事务内按答案、题目、批次逆序删除，不CASCADE。有数据时保留表，另行评审备份与回退方案，禁止绕过保护。

验收见TC-FDP-13。通俗字段字典、代码阅读顺序与术语解释见Python仓库`docs_zhx/implementation/historical-nav-http-preview.md`第8节。

### v1.7｜样本保存闭环（2026-09-07，L2）

此前v1.6仅定义表结构；用户现已授权执行建表并直接实现功能。本版完成三表事务保存及按编号读回，不改变收益公式或训练准入。
`fund_ai`已从`20260905_12`升级至`20260907_13`，旧迁移未改，无后续结构变动。等价原生DDL仍沿用v1.6所列脚本，不与Alembic重复执行。
没有额外数据迁移DML；业务新增通过下面的参数化事务服务完成，不手工填造样本。验收读取SQL见Python接口手册第9节。

#### 接口与重试契约

| 入口 | 请求与结果 |
| --- | --- |
| `POST /internal/v1/features/historical-nav-samples/batches` | JSON：`fundCode`、`startDate`、`endDate`、`pageSize`（整数1–30，默认10）、`requestKey`（必填UUID）。首次201，重试200。 |
| `GET /internal/v1/features/historical-nav-samples/batches/{batch_id}` | UUID编号，只读保存快照，成功200、不存在404。 |

响应统一为`HistoricalNavStoredBatch`：`mode=STORED`、批次全部元数据和沿用原结构的`items`；最多31条样本，无分页过程字段。
`created_at`输出带`+08:00`时区，纯业务日期不变。新建、同请求重试、GET读回的完整JSON一致。
两入口继续服务Token校验、拒绝Origin；不接收个人字段、任意特征或标签。鉴权失败403；参数或新样本一致性错误422；业务冲突409；数据库或超时503。响应不暴露SQL和连接细节。

在再次读取净值之前，按唯一索引查`request_key`。身份由基金、日期范围、三个规则版本、用途及品类组成，不包含页大小或最新来源水位。
原身份相同则直接读回旧批次，即使来源已更新或现在停用也不重算。身份不同返回`REQUEST_KEY_CONFLICT`；新规则或主动重算须换新凭证。
GET不依赖当前规则常量来判断请求身份；读回仍校验保存契约。未来新增不兼容载荷格式时需显式增加版本兼容，不能把旧批次悄悄迁成新结果。

#### 事务与七维筛查

| 维度 | 实施结果与约束 |
| --- | --- |
| 业务 | 单基金小窗口、20区间、LEARNING_ONLY；保存不代表模型预测，也不改变旧GET只读语义。 |
| 数据 | 保存与dry-run共用来源准入、分页及构建器；来源须有成功同步水位。写前/读后核对范围、来源、规则、计数、原因、特征白名单与哈希、答案对应和时间先后。缺答案不写行。 |
| 规模 | 内部分页，每个非空页最多2条净值SQL；样本上限31。三阶段写入批次/批量题目/批量答案，不逐行查库；读回按批次关联一次，最多取32条以发现非法超量，无全历史扫描。 |
| 稳定性 | 同一REPEATABLE READ可写事务覆盖来源读取、计算、三表写入、读回校验和提交；失败整体回滚。唯一约束仲裁跨进程并发，仅请求凭证唯一冲突或40001快照冲突自动重试一次，使用新事务与快照。 |
| 安全 | 仅内部服务接口，不改变关注权限；独立写连接池，预览继续只读。只INSERT，不提供更新/删除；这不是数据库防篡改保证，不扩大为个人训练数据或公开查询。 |
| 可观测 | 日志按模块/方法前缀记录TraceID、凭证/批次、基金、数量和耗时；异常保留堆栈但不输出凭据、完整样本或SQL绑定参数。GET发现不一致返回STORED_BATCH_INCONSISTENT，不自动修复。 |
| 兼容与扩展 | 无新依赖、无Java/Vue改动、无训练或定时任务。存储隔离为三表；原净值、特征快照和公式不改。来源水位仍不是首次公告版本证明。 |

保存连接池上限2、无溢出；连接等待/建连/单条SQL各5秒，锁等待3秒，事务空闲30秒。
保存有跨两次尝试共享的15秒阶段预算，不宣称是严格15秒HTTP硬中断；数据库另有限时保护。
读回采用REPEATABLE READ、READ ONLY，不读取`nav_daily`或触发同步。连接级限时保护覆盖读取。

验收见TC-FDP-14。真实库已保留一份最小样本批次，非空回退保护现会拒绝降级；不删除数据或跳过保护。
模型训练、回测、严格交易日历与历史首次可得追溯仍需后续实现/核验，不能因已存库直接发布概率。

### v1.8｜候选矩阵与固定时间划分基线验证（L2）

新增`POST /internal/v1/features/historical-nav-samples/baseline-evaluation`，POST只容纳批次清单，整个评估入口只读。
请求为`batchIds`（1–512个不重复UUID）、`trainStartDate <= trainEndDate < validationEndDate < testEndDate`、`previewSize`（整数0–20）。不接受外部X/y、个人字段、降低门槛或查看测试成绩参数。
响应为带协议版本的`HistoricalNavEvaluationResponse`，含完整批次清单/来源水位/三个规则版本、数据和训练指纹、逐基金数量及缺口、剔除原因、四种验证成绩和少量训练/验证X/y；不持久化报告或模型。

| 维度 | 约定与防护 |
| --- | --- |
| 业务 | 四种对照先做单次固定划分验证，不训练逻辑回归、不选赢家、不算持仓收益、不作为滚动回测或发布结论。用途LEARNING_ONLY，training_eligible=false，MODEL_NOT_RELEASED。 |
| 数据 | 同来源编码/当前三个规则；逐批复用保存完整性校验。按基金/净值日完整内容去重（含标签与来源水位），冲突409。固定7列X、独立y；只用SCORABLE累计净值样本，拒绝混入单位净值回退。 |
| 时间与规模 | 全基金统一按available_at划段，各段label_available_at须不晚于段末；不是随机拆分，不机械加20自然日。每基金过滤后252/120/120条，任何基金不足均不给成绩。最多15872份输入、预览20份。 |
| 稳定性 | REPEATABLE READ/READ ONLY一次快照；批次查询一次、每32批明细一次有界关联，不N+1。15秒阶段预算、现有5秒单SQL保护；缺批次、损坏、冲突或超时整次失败，不部分出分。 |
| 安全 | 继续内部Token与Origin限制；无用户资产、Java/Vue或新权限入口；不用旧评分/发布服务执行评估，仅提取复用无副作用的固定公式。 |
| 可观测 | TraceID、批次数、输入/纳入数、状态、数据指纹、耗时；异常堆栈不带Token或完整样本。报告明确字段语义、原因统计、逐基金指标与限制，不日志化全矩阵。 |
| 工程与复现 | 不增依赖或表；显式批次及规则/完整内容确定dataset_hash，previewSize和输入顺序不影响。train_hash仅覆盖训练行；测试标签变化不影响训练和验证。保留请求/报告为本地忽略文件，不写模型运行表。 |

基线统一以分数严格大于0.5判上涨：永远上涨分数1；训练上涨频率按基金仅由训练答案计算并冻结；20日趋势按历史收益正负给0/1；固定公式复用`clip(0.5+2r,0.05,0.95)`四位HALF_UP。
原评分流程0.45/0.55三态阈值不改；本次仅为二分类比较使用统一阈值。验证报告给样本加权总体及逐基金准确率、两类召回率平均、Brier平方误差；单类验证的平衡准确率为null。分数未校准，不能宣称有效概率。
测试段只在内存准备与完整性核对，不输出成绩、类别率或样本答案；尚非权限层封存。未来正式训练前仍需冻结模型方案、发布阈值并设计滚动回测，禁止按本报告调出“已达标”的测试成绩。

本轮将早期“原始净值点数门槛”明确为质量过滤及标签隔离后的**样本数**，并默认排除单位净值回退，这是候选集的更保守约束，不修改旧构建器或存储结果。
真实操作范围经用户确认：三基金001632/006730/008888，2022–2025年，共144个月度保存请求；使用独立脚本，默认只读预检，显式执行后每月独立事务，稳定请求凭证支持中断续跑。不包装成跨144月原子事务，不删除此前已完成批次。
无新增DDL、迁移DML；业务新增复用保存服务，不修改原始净值或外部来源。验收与真实结果见TC-FDP-15，手工调用见Python接口手册第10节。

### v1.9｜首个离线逻辑回归候选模型（L2，2026-09-08）

本节在查看候选模型真实验证成绩前固定方案。本轮只训练一个研究候选，不调参选赢家，不读取测试成绩，不激活旧发布流程。

- 复用v1.8的批次校验、去重、时间划分、标签隔离及每基金数量门槛。训练请求额外要求`expectedDatasetHash`，必须与已确认的基线报告指纹相同，变化则409；不接受外部X/y、参数、输出路径或测试开关。
- 固定7列输入，三基金合并训练一个模型，基金代码不作为特征。每条训练行权重为`总训练数 / (基金数 × 该基金训练数)`，使每基金总权重相同；标准化和模型拟合均使用这些权重。不是按涨跌类别重采样。
- 固定`StandardScaler`与L2逻辑回归：`C=1.0, l1_ratio=0, solver=lbfgs, tol=1e-8, max_iter=1000, fit_intercept=true, class_weight=null, random_state=0`。不搜索参数；lbfgs本身不使用随机种子，保留0便于配置追溯。仅使用TRAIN拟合均值、尺度和模型，不做缺失值填补；缺失或非有限值拒绝。常量列尺度由标准化器设为1。
- 本轮校准器明确为`NONE`，模型输出称“未校准上涨分数”，不宣称可用于投资的概率。以后如增加概率校准，只能在训练段内另做时间隔离，不占用本轮验证/保留测试答案。
- 验证统一严格大于0.5判上涨；总体及逐基金返回与四种基线相同的准确率、平衡准确率、Brier及差值。2025仅沿用原候选集准备/完整性核对，不输出测试分数或答案；仍非数据库权限封存。
- 新内部POST `historical-nav-samples/candidate-training`仅只读数据库并在内存训练，返回模型JSON和验证报告；不写服务器文件或模型表。继续Token/Origin限制；每进程只接受一个训练，忙时429；数值线程限制1、最大输入15872行、迭代上限1000。数据准备15秒及训练阶段预算是软检查，不宣称硬HTTP截止或跨进程限流。
- 本机CLI复用同一入口，把请求、报告、模型JSON和完成清单保存到忽略目录`.local-runs/nav-candidate-<run-key>/`。只新建目录，不覆盖已有批次/文件；失败目录无完成清单，保留供排查，换新凭证重跑。不创建表、不写原始净值/样本，不自动推送产物。
- 模型JSON包含固定列顺序、标准化均值/尺度、系数/截距、训练指纹/时间/数量/权重、规则版本、依赖版本、协议和内容哈希。模型哈希只依赖训练相关内容；完整数据指纹另放报告中。JSON回读校验格式与哈希，并与训练器输出交叉核验；不使用pickle，不开放任意上传模型或真实预测路由。哈希校验不是签名或防恶意篡改权限。

验收重点：验证/测试答案变化不改变模型；验证输入变化不改变标准化与系数；训练输入变化可改变模型；顺序/预览变化不改模型或成绩；单类训练、未收敛、指纹冲突、忙碌和存储失败不伪造成功。`LEARNING_ONLY / training_eligible=false / MODEL_NOT_RELEASED`保留，正式历史数据准入、滚动回测和发布门槛不在本轮解除。

### v1.10｜训练期内部校准与滚动研究验证（L2，方案预先冻结）

本节先于本轮模型成绩固定。仅使用既有2022–2025批次，2024仍只验收、2025不评分。日期窗口仅按时间和样本量预检确定，不根据收益/准确率移动。

| 窗口 | 基础模型拟合（起点均2022-01-01） | 独立校准段 | 考试段 | 每基金最少拟合/校准/考试样本 |
| --- | --- | --- | --- | --- |
| DEV_2023_Q3 | 至2023-02-28 | 2023-03-01至06-30 | 2023-07-01至09-30 | 252/60/40 |
| DEV_2023_Q4 | 至2023-05-31 | 2023-06-01至09-30 | 2023-10-01至12-31 | 252/60/40 |
| VALIDATION_2024 | 至2023-06-30 | 2023-07-01至12-31 | 2024-01-01至12-31 | 252/60/120 |

两轮DEV是原TRAIN内部的扩展窗口研究：后轮可使用前轮已成熟的历史信息，不能用自己考试段答案拟合。本轮新增的季度考试最低40只是短窗口诊断下限，不替代原完整数据准入的252/120/120；全局门槛仍先检查，任何窗口缺口如实报告，不自动缩短时间或降低门槛。
所有分组按available_at，每段标签label_available_at必须不晚于该段末；跨界答案剔除、逐基金计数。只从原TRAIN划拟合/校准/DEV，从原VALIDATION取得2024考试；训练代码不访问原TEST对象。

基础模型沿用v1.9，不搜索参数。校准固定为一维L2逻辑回归`sigmoid(a*z+b)`：z为冻结基础模型的原始线性得分，a/b仅由独立校准段拟合；`C=1, l1_ratio=0, lbfgs, tol=1e-8, max_iter=1000`，按基金等总权重。它是正则化sigmoid映射，不冒充带软标签的完整Platt实现，不比较多种校准器挑最优。基础模型和校准器均要求出现0/1两类；未收敛拒绝该窗口。斜率非正不悄悄翻转或夹值，保留研究结果并显式风险提示，仍禁止发布。
基础模型在校准后保持冻结，绝不再用拟合+校准全集重训然后沿用旧映射；那会改变校准器面对的分数分布。与上一步1377行全TRAIN模型不是同一个模型。校准效果必须比较**同一个基础模型**的校准前后，不把换训练范围的变化归功于校准。

每窗返回校准前/后总体及逐基金准确率、平衡准确率、Brier、固定5档可靠性分箱及ECE。区间[0,.2)、[.2,.4)、[.4,.6)、[.6,.8)、[.8,1]；空档null，少于30条提示样本少。ECE为各档平均分数与真实上涨比例差的绝对值按数量加权；只是分箱诊断，受分箱/样本量影响，不能独立证明概率可靠。Brier同时受区分能力等因素影响，不能仅凭它判断校准好坏；20日重叠标签不提供独立样本置信区间。
四种基线继续按每窗考试样本比较，其中历史上涨频率只使用校准段末已经可得的原TRAIN历史答案（包含该时点已成熟的拟合边界行），不使用考试答案。返回窗口清单和逐窗诊断，不混合DEV/2024算一个夸大的总命中率、不自动挑赢家或设置发布通过标志。

内部POST `historical-nav-samples/calibration-evaluation`沿用候选请求字段及expectedDatasetHash，V1固定日期边界为2022-01-01/2023-12-31/2024-12-31/2025-12-31；不允许外部折叠窗口、调参、降门槛、includeTest或文件路径。复用一次只读一致性快照；与候选训练共用进程内单槽，忙碌429；拟合数值线程1、固定最多3窗、15秒准备及60秒计算阶段软预算，不新增依赖或库表。
HTTP不写文件/数据库；本机CLI新建`.local-runs/nav-calibration-<run-key>/`保存请求、报告、含3窗基础模型+校准参数的JSON以及最后完成清单；不覆盖旧实验。每个组合模型哈希只含对应FIT/CAL内容、截止、参数与运行版本，不含考试答案或完整数据哈希；回读核验基础模型关联、维度/有限值/哈希，公式输出与拟合库交叉核验。不解除LEARNING_ONLY、正式数据准入或MODEL_NOT_RELEASED。

### v1.11｜独立交易日窗口预览（L2，2026-09-08）

本步解决“日期应该怎么数”，不把日期核验包装成新样本已完成。新增内部GET `historical-nav-samples/trading-window-preview`，参数仅`fundCode`与`cutoffDate`；基金固定既有001632、006730、008888。不修改旧preview/dry-run/storage、特征/标签版本、批次、模型或评估入口。净值口径仍待核准，因此本接口不读取净值数值、不生成特征和标签。

#### 固定日期契约

- `cutoffDate`表示站在该自然日结束时可得的信息，不是旧`asOfDate`净值业务日，也不证明盘中已经公告。
- 起点取截止时已公告的最新交易日净值，必须满足`nav_date <= ann_date <= cutoffDate`。相对截止日及以前的最近交易日，最多滞后1个交易日；超过即`STALE_ANCHOR_OVER_ONE_SESSION`，不存在则`NO_KNOWN_TRADING_NAV`。过旧起点不继续拼接历史，避免将读取范围外的未查询数据伪报成缺数。
- 历史要求截至起点的准确61个连续交易日；对应60段相邻变化，便于以后计算60日指标。缺失、缺公告、公告早于净值日、截止时未公告分别列出，不能向前多找一天顶替。
- 未来要求严格在`cutoffDate`之后的20个交易日，不包含截止日。终点先由日历锁定，缺净值不顺延；未来数据/公告检查只是离线诊断，不能反过来决定起点、历史日期或未来的特征。
- 非交易日净值原样保留在源表；仅列入`ignored_non_trading_nav_dates`，不占窗口位置。对照字段分别展示“从起点净值日按交易日数”和“从起点按旧20条源记录数”的终点，便于定位两个独立的日期差异，不执行旧构建器或计算旧收益。
- 三种状态仅表示日期齐备程度：先判断历史起点/日期，再判断未来日期。`WINDOW_DATES_COMPLETE`不代表可训练；`training_eligible/feature_generated/label_generated/nav_values_verified/database_written`始终false，`MODEL_NOT_RELEASED`不变。

#### 日历、来源和历史可得边界

日历文件为Python仓`app/data/calendars/cn_a_share_2021_2025_v1.json`，版本`CN_A_SHARE_2021_2025_V1`，独立窗口规则`TRADING_WINDOW_AFTER_CUTOFF_V1`。覆盖2021–2025的沪深股票市场，按周末与官方节假日休市事实派生，不根据数据库有没有净值倒推交易日；普通调休上班的周末仍不算开市。逐年开市日243/242/242/242/243。

每年保留沪深交易所两份公告链接及公布日。例如[上交所2025年安排](https://big5.sse.com.cn/site/cht/www.sse.com.cn/disclosure/announcement/general/c/c_20241223_10767108.shtml)、[深交所2025年安排](https://investor.szse.cn/disclosure/notice/general/t20241223_611283.html)。只提取日历事实，不复制公告全文；运行时不抓网页，不调用或新增授权给任何第三方日历API。

这是2026-09-08核验的静态研究快照，不是实时临时停市、基金申赎、港股或境外市场日历。跨年时若未来年度公告在cutoff后才公布，`future_schedule_known_at_cutoff=false`，明确这部分是事后已知的日期安排，不宣称历史当时已知。仍允许只读日期诊断，但未获训练资格。日期范围不足以覆盖完整历史和未来时，在读库前返回409，不猜2026日期或自动缩小窗口。

规范化JSON的SHA256固定为`a7258f7368a071b9fd1df2b2ac039a60bb2be0b4e5c9bde51e836c76ca3c75fa`，不受CRLF换行影响。固定文件最多64KiB，加载后校验指纹并在进程内缓存；损坏失败，不自动兜底。修订须另出已核验日历版本并重载进程，不能同版本无声修改。该指纹用于一致性检测，不是安全签名或供应商首次历史版本证明。

#### 查询、异常及验收边界

沿用`X-Service-Token`并拒绝Origin，不接Java/Vue。一次REPEATABLE READ、READ ONLY事务检查基金/启用来源/成功同步水位，随后按单基金、同来源和日历范围，仅SELECT `nav_date, ann_date`。单次查询范围不足192个自然日、最多192行，LIMIT193作超量哨兵；日期规则只需61日历史及最多1日公告滞后、未来20日，不需要无界分页。复用2连接只读池及5秒建连/等待/语句超时，15秒阶段软预算，不承诺整个请求的硬实时上限。

无Token或Origin403，参数/非试点基金422；基金不存在404，来源不就绪/品类不适用/日历范围不足409；数据库或日历损坏503并隐藏内部错误。日志只记录trace、基金、截止、状态、耗时及异常堆栈，不输出Token或净值值域。

2025只检查日期元数据，不读取数值/答案或评分。真实净值完整性指纹检查仅用于确认数据未被改动，不作为模型输入。无表结构或数据写入，无外部采集、新依赖、训练、发布、历史样本覆盖或测试期调参；既有三基金的股票型登记不代表全股票型基金样本覆盖。实际验收见TC-FDP-19。下一步先核准净值含义，再把已核验日期规则接入独立版本构建器，不能直接把旧标签改名为新标签。

### v1.12｜净值口径审计，不把不同收益定义互相替换（L2）

本步检查原定“复权优先”是否能直接作为新版收益含义。结论：当前资料不足以直接准入；新增可复算审计，未制作新版特征/标签、未修改旧构建器或训练流程。新内部GET `historical-nav-samples/nav-basis-audit`仅接收`fundCode/startDate/endDate`，三只既有试点，2022–2024范围内最多366个自然日；首个交易日前额外读取一个准确交易日作为日收益分母，可能读到2021年末。2025数值请求在参数校验阶段拒绝，不提供includeTest或口径选择开关。

#### 字段证据与决策

已追踪`fund_nav`响应→`_to_fund_nav`→同步仓储→`nav_daily`：`unit_nav/accum_nav/adj_nav/accum_div`分别原样映射单位净值、累计净值、来源复权净值、累计分红。没有本地重算复权。分红`fund_div.div_cash`原样映射`fund_dividend.cash_dividend`，官方描述为每股派息（元），不是每10份金额；`net_ex_date/ex_date/div_proc`映射净值除权日/除息日/进度。

依据：[官方基金净值字段](https://tushare.pro/document/2?doc_id=119)、[官方分红字段](https://tushare.pro/document/2?doc_id=120)。前者只注明`adj_nav`的字段含义，没有给出该列的完整复权公式；不能借用股票`pro_bar/adj_factor`文档来证明基金`fund_nav.adj_nav`的算法。网页只用于核对字段，本轮不调用供应商数据API、不改授权或新增数据源。

006730三个实施分红日的来源复权日收益，与显式现金算式`(当日单位净值+每份现金分红)/前日单位净值-1`差异为1.348241、17.982339、26.152031基点。`当日单位净值/(前日单位净值-分红)-1`在这三天数值接近来源复权；这只是公式假设匹配，不是供应商确认，也不是全历史算法证明。因此本步不以“字段齐全”或“成绩更好”选择一种口径作为正式标签，不进行新模型试训。

#### 计算与缺失规则

- 日期使用v1.11独立日历，净值及分红来自同一启用来源，来源固定为已核验的`TUSHARE_PRO_FUND`。源非交易日净值保留但不计入日收益链；缺交易日不跳过、不顺延。
- 单位、累计、复权三列分别检查缺失、非有限、非正值，不将一列回退为另一列；同端点比值是数学对照，不授予经济含义。无效公告日也报告，但本入口是当前快照事后审计，不是cutoff历史回放。
- 分红仅用唯一、已实施且现金有限非负的事件；净值除权日优先，无值才使用除息日；两日期同时存在却不同则报告冲突，两天都不按现金0计算。多条同日事件不擅自求和；缺生效日、非交易日、非实施、缺/晚公告、异常现金均报错。
- 无现有事件日按现金0仅为条件性对照，并不证明没有分红。没有事件、累计分红缺失、当前同步成功均不构成分红历史完整性证明。
- 现金日算式连乘得到条件性`cash_reinvestment_candidate_return`，假设现金在当日按单位净值再投；不等于现金一直留在账户，不含申赎费用、到账延迟、个人份额或交易可执行性。日序列、三列净值、公告或事件有阻断问题则候选值null；不完整范围仍可展示合法端点的源列比值，但状态明确为`AUDIT_INCOMPLETE`。
- 日差异的1基点阈值在看本轮结果前固定为诊断展示规则，不是发布阈值。没有事件解释却超过阈值，报告`UNEXPLAINED_ADJUSTMENT_WITHOUT_DIVIDEND`并阻断现金候选；不能自动推断是分红或拆分并补数。
- Decimal计算固定40位内部精度，输出12位小数字符串；日比较与连乘使用未舍入值。返回当前审计字段+范围+来源+日历指纹的快照哈希；不是旧特征哈希，不用于证明没有未来泄漏。

#### 资源、状态与边界

单次最多386条净值、100条分红事件，分别LIMIT387/101作哨兵，超量拒绝而非截断。两次有界查询使用既有索引和只读2连接池；基金/来源就绪检查与净值/事件在同一REPEATABLE READ、READ ONLY事务读取，15秒读取阶段软预算，既有5秒连接/查询超时不变。无新增表或迁移。继承服务Token并拒绝Origin，参数422、鉴权403、无交易日/来源不适用409、缺基金404、内部错误503并脱敏，日志带TraceID和范围摘要。

状态分别为`DIFFERENCES_FOUND`、`NO_LARGE_DAILY_DIFFERENCE_FOUND`、`AUDIT_INCOMPLETE`。前两者也一律`admission_status=NOT_APPROVED`：没有发现较大的现金/复权日差异，不代表累计净值端点比值也相等，更不证明首次版本或复权算法完整。`automatic_basis_fallback_allowed/database_written/feature_generated/label_generated/model_fitted/test_scored=false`，`MODEL_NOT_RELEASED`不变。这些是本审计入口边界；旧学习样本规则没有被暗中修改。

正式净值口径未准入的原因是本次取得的字段/数值证据，不是工程Skill要求等待批准。后续建议先明确采用“现金分红当日再投”的可解释研究目标，并以独立版本构造回报序列；来源事件完整性、拆分/折算和历史首次版本仍需另行证据，不把缺字段或未发现异常伪装成已经解决。验收见TC-FDP-20。

### v1.13｜现金再投资研究样本：先冻结输入，再独立附答案（L2）

用户在上一轮建议后要求“继续下一步”，本轮采用“现金分红计入并假设生效当日按单位净值再投资”的**研究目标**。这是明确经济含义，不是根据2024成绩挑一种净值，也不是确认供应商复权算法。新增单日内部GET `historical-nav-samples/cash-reinvestment-preview`，参数仅`fundCode/cutoffDate`；三试点、2022–2024。旧preview、dry-run、保存、标签、模型及其版本均保留。本轮只计算新版预览，不把结果送入旧存储或训练器。

#### 收益与日期契约

1. 对每段相邻交易日，`日收益 = (当日单位净值 + 当日每份现金分红) / 前一交易日单位净值 - 1`。指数从100开始，逐日乘`1+日收益`；多次分红按再投份额自然连乘，不把现金简单加到期末。不使用累计净值、来源`adj_nav`或隐式列回退。净值单位和分红金额均按来源每份“元”，不再除以10。
2. 特征日期沿用独立日历：站在`cutoffDate`自然日结束，最新已知净值满足`nav_date <= ann_date <= cutoff`且最多落后1个交易日；取截至它的61个精确交易日。返回7项原有数学指标，但输入改为独立现金研究指数。5/20/60收益看对应区间；20日波动是总体日收益标准差、不年化；回撤和相对位置看最后60点；相对位置无分母的平坦窗口拒收，不伪造0.5。
3. 答案分母为**cutoff当日及以前的最近交易日**；终点为严格晚于cutoff的第20个交易日，合计21点、20段。分母日净值若当时未公告，只能用于后来答案，绝不进入特征。不能从更早的已知净值起点算到该终点，否则公告滞后1日时会变成21段。
4. 周末截止用前一交易日为答案分母，未来仍数20日。每段第一点只是基准，基准日分红属于之前一段，不再计入。缺任何必需交易日不补值、不跳过、不顺延；源非交易日记录仅放在顶层忽略列表，不删除。
5. 本入口不开放2025数值；即使cutoff在2024，完整未来窗口进入2025也在开数据库前409 `TEST_PERIOD_PROTECTED`。2023跨2024但未来年度安排当时尚未公告，可保留历史特征，标签明确`FUTURE_CALENDAR_NOT_KNOWN_AT_CUTOFF`。不以事后安排伪装当时已知。

#### 事件可得性与拒收

`fund_div`的`ann_date/imp_anndate`分别映射方案公告和实施公告日期，字段依据仍是[官方分红文档](https://tushare.pro/document/2?doc_id=120)。历史事件可得日取方案公告与已提供实施公告的较晚者，两者都必须已到cutoff；方案公告缺失或晚于cutoff的事件不进入历史阶段。之后才公告的旧分红不追溯改写旧输入。本版不把未来现金额、未来事件质量或答案是否完整作为指标。

相关事件仅影响`(序列基准日, 序列末日]`：净值除权日优先、缺失才用除息日；两者同时存在却不同、有效日未知/非交易日、同日多事件、非实施状态、缺公告或现金缺失/负值/非有限，均拒收对应整个序列，不求和或静默补0。已公告但无有效日的事件无法判断与历史是否无关，历史也拒收。标签允许使用cutoff后、2024结束前才可得的相关事件，其最晚可得日期同时包含21个净值公告与所有计入事件；不要求标签事件一定在除息前公告。

**没有当时可见事件时按现金0，只是可复算的研究假设，不是没有分红的证明。** 本版不额外查询复权列来推断遗漏事件，也未覆盖拆分/折算。当前源会覆盖修订，当前“实施”状态不等于已恢复历史状态档案；同步水位不能证明净值/事件首次公开版本。故即使预览齐全，`training_eligible/historical_versions_verified/dividend_history_complete_verified`始终false，保留`LEARNING_ONLY`。这是明确的正式准入缺口，不是用新公式绕过上一轮审计。

#### 版本、确定性与服务边界

- 样本选择规则`CASH_REINVESTMENT_SAMPLE_RULE_V1`；特征`CASH_REINVESTMENT_FEATURE_V1`；口径`CASH_REINVESTED_UNIT_NAV_V1`；标签`CASH_REINVESTMENT_FORWARD_20TD_V1`。旧版本不能仅改名字混入。
- Decimal内部精度40、ROUND_HALF_UP；连乘不逐步舍入，输出净值/现金/日收益/指数固定12位。7项指标使用显示的12位指数计算，小数字段仍为8位小数字符串、连续下跌为整数。标签使用最终显示指数计算后再保留12位，并以**同一个返回值>0**判1，避免显示0却判涨；这是研究数值精度约定，不是交易执行精度。
- `feature_hash`只对历史payload的规范化JSON做SHA256，含当前来源水位及日历版本；`label_hash`只对独立答案做SHA256。先完成特征，再核对标签。未来价格、公告、缺数或未来事件变化不影响特征内容及其哈希；来源水位或历史内容变化则可能改变，不宣称是来源历史快照签名。结构性乱序、重复主键、越界或超量资料整体拒收。
- 单基金、同来源，既有2连接只读池；一个REPEATABLE READ、READ ONLY事务完成来源检查+2条有界数据SELECT。净值只取日期/公告/单位净值，跨度不足192自然日、最多192行、LIMIT193；分红最多100行、LIMIT101，多取一行拒收而非截断。未知生效日的旧事件也纳入有界查询；方案公告上限2024年末。沿用15秒读取阶段软预算及5秒连接/语句超时，无新表、迁移、依赖或第三方同步。
- `RESEARCH_SAMPLE_READY`表示当前假设下输入与答案算出；`INPUT_UNAVAILABLE`无输入；`LABEL_UNAVAILABLE`有输入、答案不可用。均未发布。无Token/错Token/Origin403，非法参数422，缺基金404，来源/日历/测试期保护409，内部错误503并脱敏；日志只有TraceID、基金/截止、状态和耗时摘要。

验收见TC-FDP-21、实施第19节及Python HTTP手册第16节。下一步是新版本的有界批量dry-run，先验证单日/批量一致及样本可用分布；批量完成也不会自动解除正式准入或进入旧训练器。

### v1.14｜现金再投批量dry-run：交易日选题、同快照分页、逐条等价（L2）

本轮用户要求继续上一节下一步，新增内部GET `historical-nav-samples/cash-reinvestment-dry-run`。参数仅`fundCode/startDate/endDate/pageSize`，不需要Body或原始数据；单基金固定三试点，日期均在2022–2024，含首尾最多31自然日；页大小1–30，默认10。沿用服务Token并拒绝Origin，不增加Java/Vue、同步、保存或训练入口。

#### 选题和样本契约

- `startDate/endDate`限制的是**信息截止日期**，不是净值读取范围。固定日历中属于范围内的交易日各出一道题，`cutoff_selection=TRADING_DAYS_ONLY`；周末及休市日列入`skipped_cutoff_dates`。这是批量采样规则，单日接口仍允许自然日截止；不由数据库是否有净值来决定题目是否存在。
- 缺数的交易日仍返回不可用样本，不把有问题的题目默默删除。所有计划先验算完整61历史日、公告滞后和未来20交易日；任何一题未来窗口进入2025则整次409，在开数据库前拒绝，不返回前半段成功结果。
- 逐条直接调用既有`build_cash_reinvestment_sample`，单日规则/输入/口径/标签版本不变。每题传入的净值、分红均裁回单日GET相同读取边界，包括无生效日及日期冲突事件、非交易日诊断行；不能把整页资料混进每道题。
- 范围内全是非交易日则`NO_TRADING_CUTOFFS`、0题0页，仍先核验基金与启用来源，不读取净值或事件。正常处理完范围为`DRY_RUN_COMPLETED`，即使题目均不可用也可完成处理；该状态不代表正式训练或发布通过。

#### 读取与失败边界

整次一个REPEATABLE READ、READ ONLY事务，基金及共同来源水位只检查一次。分红按整批所需窗口先读取一次、最多100条、LIMIT101；该上限不随页大小改变。每页按日历题目分组，合并读取其所需净值窗口，一页一条SELECT，不逐题重复查询基金/来源/分红，不用OFFSET跳记录。

共享仓储拆出净值/分红有界读取函数，单日组合入口继续调用二者。净值只取日期、公告日、单位净值；每页最多192行、LIMIT193，重复/乱序/越界/超量拒绝。全批合并资料跨度也须不足192自然日，开库前校验；这与31自然日题目范围不同，前后辅助资料可超出请求范围，但不能进入2025数值期。只读取同基金、同来源，不查旧样本表；没有DDL/DML或新依赖。

页大小表示每页题目数，不是原始净值行数或HTTP返回条数。响应一次返回全部有界样本，最多31题；页间及题间检查15秒请求阶段软预算，保留5秒连接/语句超时及2连接池。任一SQL/计算/超时异常整次503，不返回部分items；不新增任务调度、重试写入或成功半批状态。

#### 汇总与一致性

`sample_count=ready_count+input_unavailable_count+label_unavailable_count`；`input_available_count=ready_count+label_unavailable_count`；`label_available_count=ready_count`。输入/答案问题按代码分别统计**受影响的题数**：同题相同代码出现多个日期只计一次，多原因可以同时计数，不要求原因数量之和等于题数。

`CASH_REINVESTMENT_BATCH_RULE_V1`为新编排版本，逐题保留`CASH_REINVESTMENT_SAMPLE_RULE_V1`。`batch_hash`为响应规范化JSON（除page_size/page_count/batch_hash本身）的SHA256，因此在来源/数据/日历/规则相同的前提下，换分页方式只改变page_size及page_count，items、汇总和batch_hash不变。整批哈希包含后来答案，不能当作历史特征；跨请求遇到源数据或水位更新也可能改变，不能宣称任何时刻调用都相同。

鉴权403、参数422、缺基金404、来源/日历/测试期保护409、内部故障503脱敏，日志含TraceID、基金/范围、页数、样本/拒收计数和耗时，不输出Token或整批净值。`LEARNING_ONLY/training_eligible=false/database_written=false/model_fitted=false/test_scored=false/MODEL_NOT_RELEASED`固定；事件完整性、当前状态历史版本和非现金折算缺口仍沿用单日限制。

实际验收见TC-FDP-22、实施第20节及Python HTTP手册第17节。下一步可接独立新版样本的保存与按批次读回；旧累计净值批次和训练器继续隔离，本轮没有保存、重训或发布。

## v1.15 连续交付契约：新版资料到关注详情

用户授权连续实施到页面及验收。本节是实现前冻结的边界，不是已完成或已发布声明。

- 新增独立现金样本批次、样本、答案及研究运行表；旧表、原净值、旧实验不更新。每批最多31自然日，保存和源读取共用一次一致性事务；requestKey重试只读原批次，新key才重新计算。特征和答案分列/分表，不把整条样本JSON喂给模型。
- 内部保存入口 `POST /internal/v1/features/cash-reinvestment/batches`，Body沿用fundCode/startDate/endDate/pageSize并加requestKey；GET同路径加batchId读回。返回外层说明已经保存，内层preview是当时计算快照，逐条内容和既有dry-run相同。
- 研究入口明确选择已存batchIds。准备报告固定七列指标，按cutoff（当时信息截止日）分段；答案可得日跨段则剔除。重复基金/截止日必须内容相同才合并，冲突拒绝。至多128批；仅2022–2024，不读取2025数值。
- 沿用已冻结的三轮时间窗口和L2逻辑回归/独立sigmoid校准公式，不搜索参数。新版数据口径有独立版本和指纹，复用的是纯数值算法，不复用旧累计净值筛选。研究数量检查与正式发布准入分开，不能用研究成功替代正式准入。
- 正式发布检查失败关闭：历史首次版本、分红及非现金折算完整性未证实，2025独立测试未授权解封/未完成，均阻止产品概率；不提供强制发布或客户端传入通过标记的入口。研究分数只在受Token保护的研究接口中展示。未来启用必须提供完整证据及独立测试实现，不能靠改数据库ACTIVE字样绕过。
- Java新增本人关注预测只读接口 `GET /api/v1/watchlist/{fundCode}/prediction`，先校验登录、FUND_READ、WATCHLIST_SELF_READ及本人关注关系，再请求Python；浏览器不获得服务Token，也不能指定userId。服务失败独立降级，不拖垮基金基本详情。
- Vue仅在WatchlistFundDetailPage加入预测卡片；不在市场、公开详情、全局列表展示或预取。未发布、不适用、数据不足、服务不可用分别显示中文原因，概率为空时不填0/50，不误称预测下跌。明确20交易日整体方向，不是逐日走势；支持刷新、加载、错误、窄屏和键盘访问。
- 验收分层：纯计算/存储/API测试，独立本机PostgreSQL schema测试，真实试点资料研究运行，Java权限与客户端契约，前端类型/构建及浏览器链路。页面接通与模型达到可用预测条件分别交付，后者未满足不得宣称全部可预测。

## v1.16 生成前检查与完成边界复核（2026-09-09）

新增内部POST `/internal/v1/predictions/generation-check`，Body只有fundCode、researchRunId、expectedReportHash，继承服务Token/拒绝Origin及no-store。明确主键选择报告，不任意选择最新或最优模型；基金必须属于已授权三试点、股票型、启用且来源为TUSHARE_PRO_FUND。资料/指纹不匹配409、损坏503，缺基金/研究404，非法参数422。一次READ ONLY/REPEATABLE READ事务读取基金、来源、净值日期元数据和指定研究；不读取净值数值、样本/标签或旧发布表，不执行推理/写入。

按既有固定三个窗口检查结构与成绩，总体及三基金分别对照四种原有规则。缺窗、缺对照、样本群体/真实上涨数错配、非法数值均不能误报改善。accuracy_delta正、brier_delta负仅表述该组观察到的严格改善；不能替代多市场覆盖、正式校准标准或2025独立测试。当前研究即便全部组改善仍未获正式准入，输出固定GENERATION_BLOCKED、无概率/方向、未推理、未创建预测。

本次只把已有失败证据变成可检查契约，不根据2024观察成绩新设通过阈值。页面使用同一比较检查解释未发布原因，旧报告内容和指纹不改。完整数值生成、正式发布与失效链路尚未实现；完整目标不因状态页面通过而自动完成，实施第22节逐项记录剩余工作。

## v1.17 仅历史输入与2026日历契约（2026-09-09）

新增服务内`read_cash_prediction_feature`，不是HTTP或页面入口，不接受X、标签、模型状态或发布通过参数。仅三试点、已经结束的cutoff自然日；先确定日历窗口及2025保护再开库。两条有界查询只取历史NAV和已知分红，未公布的净值价格在SQL CASE中置空；日期元数据可用于解释缺口。至多192条NAV/100条分红，越界、重复、乱序或隐藏价格未被屏蔽均拒绝，不静默截断。

训练样本和预测输入共用历史选窗、现金序列及七指标函数；纯输入契约没有未来日期、答案、上涨概率字段。INPUT_READY仅表示输入可计算，仍标明training_eligible=false、publication_status=MODEL_NOT_RELEASED；没有推理、持久化或发布行为。

新增固定`CN_A_SHARE_2026_V1`，仅2026，指纹`87644e2cb7db2971ee6748f1708c985a8cd89854f76a3527738c98da6d02b86d`；来源与事实见随代码发布的`cn_a_share_2026_v1.json`。旧日历文件、指纹及所有旧端点日期范围不变。当前输入选择该年度日历，2026年初不足历史则拒绝，不向2025取数；2027仍无覆盖。日历更新不自动批准模型兼容或发布。验收见TC-FDP-25、实施第23节。
